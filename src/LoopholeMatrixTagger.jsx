import React, { useState, useRef, useCallback, useEffect } from 'react';
import { Mail } from 'lucide-react';

// Brand asset: Loophohl Diamond Fingerprint mark, sport-coded.
// Inlined as base64 data URIs so the tagger is self-contained — no external asset
// dependencies during deployment. Red on transparent for baseball, red-on-yellow
// for softball (matches the PNG export header treatment).
const FINGERPRINT_RED = 'data:image/png;base64,' + '';
const FINGERPRINT_SOFTBALL = 'data:image/png;base64,' + '';

// Base-out state ordering — Loophole format leads with loaded, traditional leads with empty
const BASE_STATES_LOOPHOLE = [
  { key: 'loaded',   label: 'Loaded',     bases: [1, 1, 1] },
  { key: '23',       label: '2nd & 3rd',  bases: [0, 1, 1] },
  { key: '13',       label: '1st & 3rd',  bases: [1, 0, 1] },
  { key: '12',       label: '1st & 2nd',  bases: [1, 1, 0] },
  { key: '3',        label: '3rd',        bases: [0, 0, 1] },
  { key: '2',        label: '2nd',        bases: [0, 1, 0] },
  { key: '1',        label: '1st',        bases: [1, 0, 0] },
  { key: 'empty',    label: 'Empty',      bases: [0, 0, 0] },
];

const BASE_STATES_TRADITIONAL = [...BASE_STATES_LOOPHOLE].reverse();

// Default fallback (used in canvas export and email cell summary if no game meta is present)
const BASE_STATES = BASE_STATES_LOOPHOLE;

const getBaseStatesForFormat = (format) =>
  format === 'Traditional' ? BASE_STATES_TRADITIONAL : BASE_STATES_LOOPHOLE;

const OUT_COLUMNS = [0, 1, 2];

// Format an inning number with the right ordinal suffix (1st, 2nd, 3rd, 4th, ...)
// Handles teen exceptions correctly (11th, 12th, 13th).
const ordinalInning = (n) => {
  const mod100 = n % 100;
  if (mod100 >= 11 && mod100 <= 13) return `${n}th`;
  const mod10 = n % 10;
  if (mod10 === 1) return `${n}st`;
  if (mod10 === 2) return `${n}nd`;
  if (mod10 === 3) return `${n}rd`;
  return `${n}th`;
};

// ---------------------------------------------------------------------------
// Graphite tokens.
//
// Near-zero chroma: hierarchy is carried by weight and whitespace, not colour.
// The interface recedes so the data is the only thing present. Exactly one
// filled element should be visible on any given screen -- whatever the scorer's
// next tap most likely is; everything else is hairlines and type.
//
// Swapping this one object reskins the whole app, so alternate directions
// (Ace, Daylight, Night) ship without touching component code.
// ---------------------------------------------------------------------------
const T = {
  paper:          '#FAFAF9',  // page ground
  paperRaised:    '#FFFFFF',  // modals, popovers
  paperSunk:      '#F2F2F0',  // pressed / inset states
  ink:            '#2B2B2E',  // primary text, filled elements
  inkSecondary:   '#56565A',  // supporting text
  inkMuted:       '#8A8A87',  // labels, metadata
  inkFaint:       '#A8A8A5',  // hints
  inkPlaceholder: '#C9C9C6',  // empty slots, disabled
  rule:           '#E2E2E0',  // default hairline
  ruleStrong:     '#C9C9C6',  // emphasis divider, diamond outline
  scrim:          'rgba(20,20,22,0.38)', // modal backdrop
  pulse:          'rgba(43,43,46,0.30)',  // armed-token pulse, ink-derived
  pulseFade:      'rgba(43,43,46,0)',     // pulse keyframe end
};

// One family, weights 300-600 only -- no 700/800 anywhere.
const TYPE = {
  scoreNumeral:  { fontSize: '32px', fontWeight: 300 },
  count:         { fontSize: '19px', fontWeight: 400 },
  sectionLabel:  { fontSize: '13px', fontWeight: 500 },
  button:        { fontSize: '15px', fontWeight: 400 },
  buttonPrimary: { fontSize: '15px', fontWeight: 300 },
  body:          { fontSize: '15px', fontWeight: 400 },
  meta:          { fontSize: '11px', fontWeight: 400 },
};

// Numbers that change in place must not reflow their neighbours.
const TABULAR = { fontVariantNumeric: 'tabular-nums' };

// Color scale tuned for the warm off-white card-stock background
const getCellColor = (pct) => {
  if (pct === 0) return { bg: T.rule, text: T.inkFaint, border: T.rule };
  if (pct < 2)   return { bg: T.rule, text: T.ink, border: T.rule };
  if (pct < 5)   return { bg: T.rule, text: T.ink, border: T.rule };
  if (pct < 8)   return { bg: T.rule, text: T.ink, border: T.rule };
  if (pct < 12)  return { bg: T.ink, text: T.ink, border: T.rule };
  return { bg: T.ink, text: T.paperRaised, border: T.ruleStrong };
};

// Diamond SVG marker for the base occupancy row labels
const BaseDiamond = ({ filled, size, large }) => {
  const effSize = size != null ? size : (large ? 16 : 10);
  return (
    <svg width={effSize} height={effSize} viewBox="0 0 10 10" style={{ display: 'inline-block' }}>
      <rect
        x="1" y="1" width="8" height="8"
        transform="rotate(45 5 5)"
        fill={filled ? T.ink : 'none'}
        stroke={filled ? T.ink : T.ruleStrong}
        strokeWidth="1"
      />
    </svg>
  );
};

const BaseIndicator = ({ bases, size }) => {
  // Render as 2nd on top, 1st and 3rd on sides (diamond layout)
  const [first, second, third] = bases;
  const isLarge = size === 'large';
  const cellSize = isLarge ? '16px' : '10px';
  const gap = isLarge ? '3px' : '2px';
  const totalWidth = isLarge ? '54px' : '36px';
  return (
    <div style={{
      display: 'grid',
      gridTemplateColumns: `${cellSize} ${cellSize} ${cellSize}`,
      gridTemplateRows: `${cellSize} ${cellSize}`,
      gap,
      alignItems: 'center',
      justifyItems: 'center',
      width: totalWidth
    }}>
      <div />
      <BaseDiamond filled={second === 1} large={isLarge} />
      <div />
      <BaseDiamond filled={third === 1} large={isLarge} />
      <div />
      <BaseDiamond filled={first === 1} large={isLarge} />
    </div>
  );
};

// ============================================================================
// FieldDiamond — the interactive diamond surface (Phase 1 extraction).
// Lifted verbatim from the bip_fielders cascade branch so it can render in BOTH
// the cascade AND the always-on idle surface. Zero behavior change: same SVG,
// same fielder buttons, same runner-token arm/place logic, same destination
// targets and Out chips. All state is passed in via props; this component is
// purely presentational + event-forwarding.
//
// Props:
//   fielderOptions     — array of {code, num, ...} for the 9 fielder buttons
//   fielderSequence    — array of fielder codes tapped so far (for badges)
//   onFielderTap       — (code) => void
//   runnerPlacements   — { batter|1B|2B|3B: destination|null }
//   armedRunner        — source key currently armed, or null
//   touchedRunners     — Set of source keys the scorer has tapped
//   placementHistory   — array for undo
//   onPlacementUndo    — () => void
//   onRunnerTokenTap   — (source) => void
//   onDestinationTap   — (destination) => void
// ============================================================================
// One-time onboarding flare: set true after the diamond's first appearance so the
// "what's tappable" flare shows only once per session, not on every play.
let hasShownDiamondFlare = false;

const FieldDiamond = ({
  fielderOptions,
  fielderSequence,
  onFielderTap,
  runnerPlacements,
  armedRunner,
  touchedRunners,
  placementHistory,
  onPlacementUndo,
  onRunnerTokenTap,
  onDestinationTap,
  allowOwnBaseOut,
  onMoundTap,
  onUndo,
  canUndo,
  batterLabel,
  onBatterTap,
  batterEditing,
  batterDraft,
  onBatterDraftChange,
  onBatterEditCommit,
  onBatterEditCancel,
  pitcherLabel,
  onPitcherTap,
  pitcherEditing,
  pitcherDraft,
  onPitcherDraftChange,
  onPitcherEditCommit,
  onPitcherEditCancel,
  baseJerseys,
  batterJersey,
}) => {
  // When a runner is armed we know a placement is coming. Lift the SAFE/OUT bins
  // into a row ABOVE the field (so the runner token can't cover them, and the
  // own-base OUT is reachable), and dim the field below to focus the choice.
  // Bins are oversized — the dimmed field can't be mis-tapped, so we can afford it.
  const armedSrcNum = armedRunner != null
    ? ({ 'batter': 0, '1B': 1, '2B': 2, '3B': 3 })[armedRunner]
    : null;
  const binDestBaseNum = { '1B': 1, '2B': 2, '3B': 3, 'home': 4 };
  const binOwnBaseKey = armedRunner;
  const binDestinations = armedSrcNum != null
    ? Object.entries(binDestBaseNum)
        .filter(([key, num]) => {
          if (num > armedSrcNum) return true;
          if (allowOwnBaseOut && key === binOwnBaseKey) return true;
          return false;
        })
        .map(([key]) => key)
    : [];

  // ── Path-stepping runner animation ────────────────────────────────────────
  // Tokens step base-to-base toward their target (following the basepath) instead
  // of sliding diagonally across the infield. A runner going 1B→3B slides 1B→2B
  // then 2B→3B. The batter runs home→1B even on an out (then fades). Index model:
  // home/origin = 0, 1B = 1, 2B = 2, 3B = 3, home(score) = 4.
  const ORIGIN_INDEX = { batter: 0, '1B': 1, '2B': 2, '3B': 3 };
  const INDEX_POS_KEY = { 0: 'home', 1: '1B', 2: '2B', 3: '3B', 4: 'home' };
  const placementTargetIndex = (src, placement) => {
    if (placement == null) return ORIGIN_INDEX[src];
    if (placement === 'home') return 4;
    if (typeof placement === 'string' && placement.startsWith('out_at_')) {
      const b = placement.replace('out_at_', '');
      if (b === 'home') {
        // The batter is out AT THE PLATE (strikeout / dropped 3rd / interference) —
        // they're standing on home, so index 0 (no travel). Only a RUNNER reaching
        // home actually traveled around to it (index 4).
        return src === 'batter' ? 0 : 4;
      }
      return ({ '1B': 1, '2B': 2, '3B': 3 }[b] ?? ORIGIN_INDEX[src]);
    }
    return ({ '1B': 1, '2B': 2, '3B': 3 }[placement]) ?? ORIGIN_INDEX[src];
  };
  // Batter-hold removed: forced runners now auto-advance out of the batter's path,
  // so the batter no longer stacks under a runner. The batter slides to its base
  // alongside the runners, even with runners on.
  const batterIsWaiting = false;

  const [displayBases, setDisplayBases] = React.useState({});
  React.useEffect(() => {
    if (!runnerPlacements) { setDisplayBases({}); return; }
    const targets = {};
    for (const src of ['batter', '1B', '2B', '3B']) {
      if (src in runnerPlacements) {
        targets[src] = (src === 'batter' && batterIsWaiting)
          ? 0 // park batter at home while waiting
          : placementTargetIndex(src, runnerPlacements[src]);
      }
    }
    // Seed any new source at its ORIGIN so it animates in; drop departed sources.
    setDisplayBases(prev => {
      const next = { ...prev };
      let changed = false;
      for (const src in targets) {
        if (!(src in next)) { next[src] = ORIGIN_INDEX[src]; changed = true; }
      }
      for (const src in next) {
        if (!(src in targets)) { delete next[src]; changed = true; }
      }
      return changed ? next : prev;
    });
    // Step every token one base toward its target each tick (each leg ~ the interval).
    const interval = setInterval(() => {
      setDisplayBases(prev => {
        let changed = false;
        const next = { ...prev };
        for (const src in targets) {
          const cur = next[src] == null ? ORIGIN_INDEX[src] : next[src];
          if (cur < targets[src]) { next[src] = cur + 1; changed = true; }
          else if (cur > targets[src]) { next[src] = cur - 1; changed = true; }
        }
        return changed ? next : prev;
      });
    }, 430);
    return () => clearInterval(interval);
  }, [runnerPlacements, batterIsWaiting]);
  // ───────────────────────────────────────────────────────────────────────────

  // Flare cue: the FIRST time the diamond appears in a session, briefly flare the
  // action buttons / spheres / runners to orient a new user to what's tappable.
  // One-time only (module flag persists across the idle↔cascade remounts), so it
  // doesn't replay on every play.
  const [flaring, setFlaring] = React.useState(false);
  React.useEffect(() => {
    if (!hasShownDiamondFlare) {
      hasShownDiamondFlare = true;
      setFlaring(true);
      const t = setTimeout(() => setFlaring(false), 2200);
      return () => clearTimeout(t);
    }
  }, []);

  // Base screen positions (% within the square field) for the SAFE/OUT overlay.
  // Match the in-field basePositions used by the runner layer.
  const binBasePositions = {
    '1B':   { left: '82%', top: '50%' },
    '2B':   { left: '50%', top: '18%' },
    '3B':   { left: '18%', top: '50%' },
    'home': { left: '50%', top: '80%' },
  };

  return (
        <div style={{ width: '100%', maxWidth: '460px', margin: '24px auto 0', position: 'relative' }}>
        {/* Keyframes used by both the Pitch CTA button and the untouched-runner
            pulse. Defined at the top of the component so the animations work
            whether or not the runner layer renders. */}
        <style>{`
          @keyframes wildcard-runner-untouched {
            0%, 100% {
              box-shadow: 0 0 0 0 var(--wildcard-pulse-color, ${T.pulse}),
                          0 2px 4px ${T.ruleStrong};
            }
            50% {
              box-shadow: 0 0 0 8px transparent,
                          0 2px 4px ${T.ruleStrong};
            }
          }
        `}</style>
        {/* Field. When a runner is armed, the field dims (same treatment as the
            action buttons) and a tap anywhere on it (that isn't a runner token)
            disarms the runner. The SAFE/OUT split-circle bins render in a bright
            overlay layer ABOVE this dimmed field (sibling below), positioned on
            each base. */}
        <div
          onClick={() => { if (armedRunner && onRunnerTokenTap) onRunnerTokenTap(armedRunner); }}
          style={{
            position: 'relative',
            width: '100%',
            aspectRatio: '0.86',
            opacity: armedRunner ? 0.55 : 1,
            transition: 'opacity 120ms ease',
          }}>
          <svg viewBox="0 0 320 320" preserveAspectRatio="none" style={{ width: '100%', height: '100%', display: 'block' }}>
            {/* Hairline geometry only — no dirt or grass rendering. The field is
                implied by the basepaths; anything filled here would compete with the
                one filled element the screen is allowed. */}
            <polygon points="160,260 60,160 160,60 260,160"
                     fill="none" stroke={T.ruleStrong} strokeWidth="1"/>
            {/* Foul lines, home out past the corner bags. The arc has to land on
                these to read correctly, so they are drawn first. */}
            <line x1="160" y1="260" x2="0"   y2="100" stroke={T.ruleStrong} strokeWidth="1"/>
            <line x1="160" y1="260" x2="320" y2="100" stroke={T.ruleStrong} strokeWidth="1"/>
            {/* Infield arc — the grass line between infield and outfield. Struck
                from the mound and stopping at the foul lines, behind the corner
                infielders, so 1B and 3B fall inside the dirt the way they do on a
                real field. Endpoints sit exactly on the foul lines above. */}
            <path d="M 14 114 A 156 156 0 0 1 306 114"
                  fill="none" stroke={T.rule} strokeWidth="1"/>
            {/* Base markers — hollow hairline squares; occupancy is carried by the
                runner tokens, not by the bags. */}
            <rect x="155" y="255" width="10" height="10" fill="none" stroke={T.ruleStrong} strokeWidth="1"/>
            <rect x="255" y="155" width="10" height="10" fill="none" stroke={T.ruleStrong} strokeWidth="1"/>
            <rect x="155" y="55"  width="10" height="10" fill="none" stroke={T.ruleStrong} strokeWidth="1"/>
            <rect x="55"  y="155" width="10" height="10" fill="none" stroke={T.ruleStrong} strokeWidth="1"/>
          </svg>

          {/* PITCH button on the mound (mirrors GameChanger). Circle matching the
              fielder buttons (50px, same border treatment), in brand red so it reads
              as the active call-to-action, with a continuous pulse driven by the
              same keyframes the untouched-runner tokens use. Opens the catch-all
              cascade with Bunt attempt / Balk / Hit by pitch / Intentional ball.
              Hidden while a runner is armed — the bins overlay owns the surface. */}

          {/* Undo button — lives in the diamond's bottom-right foul territory,
              right of the 1B line. Always visible (dims with the field when a
              runner is armed). Replaces the old action-bar Undo. */}
          {onUndo && (
            <button
              type="button"
              onClick={(e) => { e.stopPropagation(); if (canUndo) onUndo(); }}
              disabled={!canUndo}
              aria-label="Undo"
              style={{
                position: 'absolute',
                right: '4%',
                bottom: '4%',
                background: 'transparent',
                border: `1px solid ${canUndo ? T.ruleStrong : T.rule}`,
                borderRadius: 0,
                padding: '6px 10px',
                fontFamily: 'inherit',
                fontSize: '11px',
                fontWeight: 400,
                color: canUndo ? T.inkMuted : T.inkPlaceholder,
                cursor: canUndo ? 'pointer' : 'not-allowed',
                boxShadow: 'none',
                zIndex: 5,
                display: 'flex',
                alignItems: 'center',
                gap: '4px',
              }}
            >
              ↩ Undo
            </button>
          )}

          {/* Batter sphere at home plate — shows jersey number (or slot #).
              Tappable to edit the batter's jersey number. Uses the batter accent
              color (orange) to distinguish from runner spheres (blue) and fielders
              (black). When tapped while editing, shows an inline input. */}
          {batterLabel && !armedRunner && !('batter' in (runnerPlacements || {})) && (
            batterEditing ? (
              <div style={{
                position: 'absolute',
                left: '38%',
                top: '82%',
                transform: 'translate(-50%, -50%)',
                zIndex: 7,
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                gap: '4px',
              }}>
                <input
                  autoFocus
                  type="text"
                  inputMode="numeric"
                  value={batterDraft}
                  onChange={(e) => onBatterDraftChange && onBatterDraftChange(e.target.value.replace(/[^0-9]/g, '').slice(0, 3))}
                  onBlur={() => onBatterEditCommit && onBatterEditCommit()}
                  onKeyDown={(e) => {
                    if (e.key === 'Enter') onBatterEditCommit && onBatterEditCommit();
                    else if (e.key === 'Escape') onBatterEditCancel && onBatterEditCancel();
                  }}
                  placeholder="#"
                  style={{
                    width: '50px',
                    fontSize: '14px',
                    fontWeight: 400,
                    textAlign: 'center',
                    padding: '6px 4px',
                    border: `2px solid ${T.ink}`,
                    borderRadius: '25px',
                    background: T.paperRaised,
                    color: T.ink,
                    outline: 'none',
                    fontFamily: 'inherit',
                  }}
                />
              </div>
            ) : (
              <button
                type="button"
                onClick={(e) => { e.stopPropagation(); onBatterTap && onBatterTap(); }}
                style={{
                  position: 'absolute',
                  left: '38%',
                  top: '82%',
                  transform: 'translate(-50%, -50%)',
                  width: '50px',
                  height: '50px',
                  borderRadius: '50%',
                  background: T.ink,
                  border: `1px solid ${T.ink}`,
                  color: T.paper,
                  fontSize: '12px',
                  fontWeight: 500,
                  fontFamily: 'inherit',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  cursor: 'pointer',
                  boxShadow: 'none',
                  zIndex: 7,
                  padding: 0,
                  lineHeight: 1,
                  animation: flaring ? 'wildcard-flare 0.55s ease 2' : 'none',
                }}
              >
                {batterLabel}
              </button>
            )
          )}

          {/* Pitcher sphere at the mound — shows the pitcher's jersey number.
              Tappable to edit (same pattern as the batter sphere). Blue (defensive)
              to distinguish from the orange batter and the red PITCH button. Sits
              above the PITCH button so they don't overlap. */}
          {pitcherLabel != null && !armedRunner && (
            pitcherEditing ? (
              <div style={{
                position: 'absolute',
                left: '50%',
                top: '39%',
                transform: 'translate(-50%, -50%)',
                zIndex: 7,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
              }}>
                <input
                  autoFocus
                  type="text"
                  inputMode="numeric"
                  value={pitcherDraft}
                  onChange={(e) => onPitcherDraftChange && onPitcherDraftChange(e.target.value.replace(/[^0-9]/g, '').slice(0, 3))}
                  onBlur={() => onPitcherEditCommit && onPitcherEditCommit()}
                  onKeyDown={(e) => {
                    if (e.key === 'Enter') onPitcherEditCommit && onPitcherEditCommit();
                    else if (e.key === 'Escape') onPitcherEditCancel && onPitcherEditCancel();
                  }}
                  placeholder="#"
                  style={{
                    width: '50px',
                    fontSize: '14px',
                    fontWeight: 400,
                    textAlign: 'center',
                    padding: '6px 4px',
                    border: `2px solid ${T.ink}`,
                    borderRadius: '25px',
                    background: T.paperRaised,
                    color: T.ink,
                    outline: 'none',
                    fontFamily: 'inherit',
                  }}
                />
              </div>
            ) : (
              <button
                type="button"
                onClick={(e) => { e.stopPropagation(); onPitcherTap && onPitcherTap(); }}
                aria-label="Edit pitcher number"
                style={{
                  position: 'absolute',
                  left: '50%',
                  top: '53%',
                  transform: 'translate(-50%, -50%)',
                  width: '46px',
                  height: '46px',
                  borderRadius: '50%',
                  // Paper, not transparent: the chip sits over the pitcher fielder
                  // token and must occlude it. Paper-on-paper with a hairline keeps
                  // it out of the one-filled-element budget.
                  background: T.paper,
                  border: `1px solid ${T.ruleStrong}`,
                  color: T.ink,
                  fontSize: '12px',
                  fontWeight: 500,
                  fontFamily: 'inherit',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  cursor: 'pointer',
                  boxShadow: 'none',
                  zIndex: 6,
                  padding: 0,
                  lineHeight: 1,
                  animation: flaring ? 'wildcard-flare 0.55s ease 2' : 'none',
                }}
              >
                {pitcherLabel ? pitcherLabel : 'P#'}
              </button>
            )
          )}

          {(() => {
            // Position the 9 fielder buttons absolutely over the SVG. Coordinates are
            // percentages of the container — designed for a 320px-wide cell view.
            // Layout (matches the locked rendering):
            //   Pitcher (1)         (50, 39)
            //   Catcher (2)         (50, 90)
            //   1st Base (3)        (83, 42)
            //   2nd Base (4)        (67, 23.5) — midpoint of Pitcher→RF
            //   3rd Base (5)        (17, 42)
            //   Shortstop (6)       (33, 23.5) — midpoint of Pitcher→LF
            //   Left Field (7)      (16, 8)
            //   Center Field (8)    (50, 3)
            //   Right Field (9)     (84, 8)
            // v1.5 step 7 nudges (out of base zones):
            //   1B fielder pulled right + up from (83,42) to (88,36)
            //   3B fielder mirrored from (17,42) to (12,36)
            //   Catcher pulled below home plate from (50,90) to (50,94)
            const positionLayout = {
              // P sits on the mound and replaces the drawn mound circle.
              'P':  { left: '50%',   top: '53%' },
              // Catcher pulled up toward the plate, stopping short of touching it.
              'C':  { left: '50%',   top: '90%' },
              '1B': { left: '85%',   top: '36%' },
              // Middle infielders play deeper, up toward the arc.
              '2B': { left: '68%',   top: '17%' },
              '3B': { left: '15%',   top: '36%' },
              'SS': { left: '32%',   top: '17%' },
              // Corner outfielders open out toward the lines and off the top edge.
              'LF': { left: '10%',   top: '14%' },
              'CF': { left: '50%',   top: '6%' },
              'RF': { left: '90%',   top: '14%' },
            };
            return fielderOptions.map((f) => {
              const pos = positionLayout[f.code];
              if (!pos) return null;
              // Compute sequence-badge value: which tap number this fielder is in the
              // sequence. If a fielder appears multiple times (rare), the badge shows
              // the LATEST occurrence number.
              let badgeNum = null;
              if (fielderSequence) {
                for (let i = fielderSequence.length - 1; i >= 0; i--) {
                  if (fielderSequence[i] === f.code) {
                    badgeNum = i + 1;
                    break;
                  }
                }
              }
              const isTapped = badgeNum !== null;
              return (
                <button
                  key={f.code}
                  onClick={() => onFielderTap(f.code)}
                  style={{
                    position: 'absolute',
                    left: pos.left,
                    top: pos.top,
                    transform: 'translate(-50%, -50%)',
                    // Fielders default to bold dark — they're reference markers,
                    // not the primary interaction surface. The runners (which
                    // pulse) are what the scorer is acting on. When a fielder
                    // IS tapped (during sequence build), they flip to brand red
                    // so the active sequence stays visible.
                    // Paper, not transparent: the foul lines and the infield arc
                    // pass behind these discs and would otherwise show through them.
                    background: isTapped ? T.ink : T.paper,
                    border: `1px solid ${isTapped ? T.ink : T.ruleStrong}`,
                    borderRadius: '50%',
                    width: '50px',
                    height: '50px',
                    display: 'flex',
                    flexDirection: 'column',
                    alignItems: 'center',
                    justifyContent: 'center',
                    cursor: 'pointer',
                    boxShadow: 'none',
                    padding: 0,
                    fontFamily: 'inherit',
                    transition: 'transform 120ms ease, background 150ms ease, border-color 150ms ease',
                    // When a runner is armed, the SAFE/OUT bins overlay the field
                    // and the field dims visually. Disable pointer events on the
                    // fielder buttons so a near-miss tap from a bin can't hit a
                    // fielder underneath and cause unintended state changes.
                    pointerEvents: armedRunner ? 'none' : 'auto',
                  }}
                  onPointerDown={(e) => { e.currentTarget.style.transform = 'translate(-50%, -50%) scale(0.94)'; }}
                  onPointerUp={(e) => { e.currentTarget.style.transform = 'translate(-50%, -50%) scale(1)'; }}
                  onPointerLeave={(e) => { e.currentTarget.style.transform = 'translate(-50%, -50%) scale(1)'; }}
                >
                  <span style={{
                    fontSize: '15px',
                    fontWeight: 400,
                    color: isTapped ? T.paper : T.ink,
                    lineHeight: 1,
                  }}>{f.code}</span>
                  {isTapped && (
                    <span style={{
                      position: 'absolute',
                      top: '-4px',
                      right: '-4px',
                      background: T.ink,
                      color: T.paperRaised,
                      borderRadius: '50%',
                      width: '18px',
                      height: '18px',
                      fontSize: '11px',
                      fontWeight: 400,
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'center',
                      border: `2px solid ${T.paperRaised}`,
                    }}>{badgeNum}</span>
                  )}
                </button>
              );
            });
          })()}

          {/* v1.5 step 7 — Runner layer.
              Pre-populated based on hit type when the diamond opens. Runners with
              terminal destinations (home/out_at_X) disappear from the diamond.
              Per-base Out chips appear next to each base when a runner is armed.
              Destination targets are clockwise-only (relative to the runner's origin).
              Undo affordance pops the last placement change. */}
          {runnerPlacements && (() => {
            // Coordinates of each base destination and home. Order matters for the
            // clockwise filter: home=0, 1B=1, 2B=2, 3B=3.
            // Note: 'home' is the destination position for the safe-at-plate target
            // (and where scoring runner tokens would sit). Pulled up from 92% to 80%
            // so the safe target doesn't overlap the catcher button (top:94%).
            const basePositions = {
              '1B':   { left: '82%', top: '50%' },
              '2B':   { left: '50%', top: '18%' },
              '3B':   { left: '18%', top: '50%' },
              'home': { left: '50%', top: '80%' },
            };
            // Source → numeric base for clockwise comparison. Batter starts at home (0).
            const sourceBaseNum = { 'batter': 0, '1B': 1, '2B': 2, '3B': 3 };
            // Destination → numeric base. home=0, but for clockwise advance we use
            // home=4 (one past 3B) so a runner on 3B can advance to home (4).
            const destBaseNum = { '1B': 1, '2B': 2, '3B': 3, 'home': 4 };
            const ALL_SOURCES = ['batter', '1B', '2B', '3B'];

            // Build the visible runner tokens from the ANIMATED display position
            // (displayBases), which steps base-to-base toward each runner's target.
            // A token only fades (scored/out) once it has ARRIVED at its terminal base,
            // so the batter runs to first and then disappears, and a caught runner
            // slides to the bag before vanishing — matching what happens on the field.
            const liveTokens = [];
            for (const src of ALL_SOURCES) {
              if (!(src in runnerPlacements)) continue;
              const placement = runnerPlacements[src];
              const isHome = placement === 'home';
              const isOut = typeof placement === 'string' && placement.startsWith('out_at_');
              const isTerminal = isHome || isOut;
              const targetIdx = placementTargetIndex(src, placement);
              const curIdx = displayBases[src] == null ? ORIGIN_INDEX[src] : displayBases[src];
              // Batter waiting at home (invisible) until runners are touched.
              if (src === 'batter' && batterIsWaiting) {
                liveTokens.push({
                  source: 'batter',
                  position: basePositions['home'],
                  isArmed: false,
                  waiting: true,
                });
                continue;
              }
              const posKey = INDEX_POS_KEY[curIdx] || (src === 'batter' ? 'home' : src);
              const position = basePositions[posKey];
              if (!position) continue;
              const arrivedTerminal = isTerminal && curIdx >= targetIdx;
              liveTokens.push({
                source: src,
                position,
                isArmed: !isTerminal && armedRunner === src,
                exiting: arrivedTerminal ? (isHome ? 'scored' : 'out') : null,
              });
            }


            return (
              <>
                {/* Undo chip — left foul territory, below 3B. Out of the way of LF
                    (top:8%) and 3B (top:36%) and the 3B runner token (top:50%).
                    Only visible when there's history to undo. */}
                {placementHistory && placementHistory.length > 0 && (
                  <button
                    onClick={onPlacementUndo}
                    aria-label="Undo last placement"
                    style={{
                      position: 'absolute',
                      top: '67%',
                      left: '2%',
                      background: T.paperRaised,
                      color: '#555',
                      border: `1px solid ${T.ruleStrong}`,
                      borderRadius: '4px',
                      padding: '5px 9px',
                      fontSize: '11px',
                      fontWeight: 400,
                      cursor: 'pointer',
                      fontFamily: 'inherit',
                      zIndex: 6,
                      boxShadow: `0 1px 2px ${T.rule}`,
                    }}
                  >
                    ↶ Undo
                  </button>
                )}


                {/* Inline keyframes for the untouched-runner pulse. The runner
                    tokens read their accent color from --wildcard-pulse-color
                    inline-style var so we can share one keyframes block across
                    batter (orange) and base runners (blue). */}
                <style>{`
                  @keyframes wildcard-runner-untouched {
                    0%, 100% {
                      box-shadow: 0 0 0 0 var(--wildcard-pulse-color, ${T.pulse}),
                                  0 2px 4px ${T.ruleStrong};
                    }
                    50% {
                      box-shadow: 0 0 0 8px transparent,
                                  0 2px 4px ${T.ruleStrong};
                    }
                  }
                  @keyframes wildcard-flare {
                    0%, 100% {
                      box-shadow: 0 0 0 0 ${T.pulseFade}, 0 2px 4px ${T.ruleStrong};
                      transform: translate(-50%, -50%) scale(1);
                    }
                    50% {
                      box-shadow: 0 0 0 7px ${T.pulse}, 0 2px 6px ${T.ruleStrong};
                      transform: translate(-50%, -50%) scale(1.13);
                    }
                  }
                `}</style>

                {/* Live runner tokens. Tap to arm/disarm. Untouched runners
                    pulse to remind the scorer they still need to confirm the
                    runner's destination (even if pre-populated by rule). */}
                {liveTokens.map(({ source, position, isArmed, exiting, waiting }) => {
                  const isBatter = source === 'batter';
                  // All offensive tokens use the same orange accent (batter color).
                  const accent = T.ink;
                  const armedShadow = T.pulse;
                  const isTouched = touchedRunners && touchedRunners.has(source);
                  const shouldPulse = false; // pulse removed — runners stay static
                  const pulseColor = T.pulse;
                  // Exiting tokens (scored / out) — terminal. Slide to the spot, fade,
                  // recolor: green ring for a score, gray for an out. Non-interactive.
                  const exitColor = exiting === 'scored' ? T.ink : T.inkPlaceholder;
                  return (
                    <button
                      key={`runner-${source}`}
                      onClick={(e) => { e.stopPropagation(); if (!exiting && !waiting && onRunnerTokenTap) onRunnerTokenTap(source); }}
                      disabled={!!exiting || !!waiting}
                      aria-label={isBatter ? 'Batter' : `Runner from ${source}`}
                      style={{
                        position: 'absolute',
                        left: position.left,
                        top: position.top,
                        transform: exiting ? 'translate(-50%, -50%) scale(0.7)' : 'translate(-50%, -50%)',
                        width: '50px',
                        height: '50px',
                        borderRadius: '50%',
                        background: exiting ? T.paperRaised : (isArmed ? accent : T.paperRaised),
                        border: `2px solid ${exiting ? exitColor : accent}`,
                        color: exiting ? exitColor : (isArmed ? T.paperRaised : accent),
                        fontSize: '11px',
                        fontWeight: 400,
                        cursor: exiting ? 'default' : 'pointer',
                        padding: 0,
                        zIndex: (exiting || waiting) ? 3 : 5,
                        opacity: (exiting || waiting) ? 0 : 1,
                        boxShadow: isArmed
                          ? `0 0 0 4px ${armedShadow}, 0 2px 4px ${T.ruleStrong}`
                          : `0 2px 4px ${T.ruleStrong}`,
                        fontFamily: 'inherit',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        animation: (flaring && !exiting && !waiting && !isArmed)
                          ? 'wildcard-flare 0.55s ease 2'
                          : 'none',
                        // Per-leg slide: each base-to-base step (~430ms tick) animates
                        // position over 0.4s so it lands before the next step. Terminal
                        // tokens fade as they arrive; the batter fades in when released.
                        transition: 'left 0.4s ease, top 0.4s ease, opacity 0.4s ease, transform 0.4s ease, background 0.2s, border-color 0.2s, color 0.2s',
                        ['--wildcard-pulse-color']: pulseColor,
                      }}
                    >
                      {exiting === 'scored' ? '✓' : exiting === 'out' ? '✕'
                        : isBatter ? (batterJersey ? `#${batterJersey}` : 'Bat')
                        : ((baseJerseys && baseJerseys[source]) ? `#${baseJerseys[source]}` : source)}
                    </button>
                  );
                })}
              </>
            );
          })()}
        </div>
        {/* SAFE/OUT split-circle bins — bright overlay layer ABOVE the dimmed field.
            One per valid destination, positioned on its base. Original circle size.
            Top half SAFE (green), bottom half OUT (red); own base = OUT only with
            SAFE half disabled/grayed. */}
        {armedRunner && binDestinations.map((baseKey) => {
          const pos = binBasePositions[baseKey];
          if (!pos) return null;
          const isOwn = baseKey === binOwnBaseKey;
          const W = 76;       // bin width (was 64)
          const HALF = 34;    // each half-circle height
          const GAP = 7;      // gap between SAFE and OUT
          return (
            <div
              key={`binoverlay-${baseKey}`}
              style={{
                position: 'absolute',
                left: pos.left,
                top: pos.top,
                transform: 'translate(-50%, -50%)',
                width: `${W}px`,
                height: `${HALF * 2 + GAP}px`,
                zIndex: 20,
                display: 'flex',
                flexDirection: 'column',
                gap: `${GAP}px`,
              }}
            >
              {/* SAFE — top half-circle (green). On the runner's own/origin base it
                  acts as "Back" — send an auto-advanced runner back to where they
                  started (the safety net for auto-advance). */}
              <button
                onClick={() => { if (onDestinationTap) onDestinationTap(baseKey); }}
                aria-label={isOwn ? `Send runner back to ${baseKey}` : baseKey === 'home' ? 'Safe (score)' : `Safe at ${baseKey}`}
                style={{
                  width: '100%',
                  height: `${HALF}px`,
                  borderTopLeftRadius: `${W}px`,
                  borderTopRightRadius: `${W}px`,
                  borderBottomLeftRadius: '6px',
                  borderBottomRightRadius: '6px',
                  background: isOwn ? T.ink : T.ink,
                  color: T.paperRaised,
                  border: `2px solid ${T.paperRaised}`,
                  cursor: 'pointer',
                  fontFamily: 'inherit',
                  fontSize: '11px',
                  fontWeight: 400,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  boxShadow: `0 2px 6px ${T.scrim}`,
                  opacity: 1,
                }}
              >
                {isOwn ? 'Back' : 'Safe'}
              </button>
              {/* OUT — bottom half-circle (red); always active */}
              <button
                onClick={() => onDestinationTap && onDestinationTap('out_at_' + baseKey)}
                aria-label={`Out at ${baseKey}`}
                style={{
                  width: '100%',
                  height: `${HALF}px`,
                  borderTopLeftRadius: '6px',
                  borderTopRightRadius: '6px',
                  borderBottomLeftRadius: `${W}px`,
                  borderBottomRightRadius: `${W}px`,
                  background: T.inkMuted,
                  color: T.paperRaised,
                  border: `2px solid ${T.paperRaised}`,
                  cursor: 'pointer',
                  fontFamily: 'inherit',
                  fontSize: '11px',
                  fontWeight: 400,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  boxShadow: `0 2px 6px ${T.scrim}`,
                }}
              >
                Out
              </button>
            </div>
          );
        })}
        </div>
  );
};

// v1.4 — CascadeScreen
// Full-takeover screen for PA Result and Runner advance selections. Replaces the
// cell view's body (everything below the header) until the scorer either selects
// a leaf option or navigates back. GameChanger-style — large tap targets, one
// option per row, no menu re-opens.
//
// Stages:
//   'pa_top'         → top-level PA Result options
//   'bip_type'       → batted-ball type (Ground / Hard Ground / Fly / Pop fly / Line / Bunt)
//   'bip_outcome'    → BIP outcome (Hit / Out / Error / etc., type-specific)
//   'bip_batter_out' → Batter out sub-cascade (Out at 1st / DP / etc., type-specific)
//   'bip_fielders'   → Fielder picker (v1.5 step 6) — distinct layout with sequence + grid + Done
//   'runner'         → Runner advance options
const CascadeScreen = ({
  stage,
  battedBall,
  paOptions,
  battedBallOptions,
  bipOutcomeOptions,
  batterOutOptions,
  runnerOptions,
  fielderOptions,
  fielderSequence,
  pendingPa,
  runnerPlacements,
  touchedRunners,
  armedRunner,
  placementHistory,
  cellStateKey,
  pendingRunnerAdvance,
  onPaTopSelect,
  onTypeSelect,
  onOutcomeSelect,
  onBatterOutSelect,
  onFcForceoutSelect,
  onDroppedThirdOutcomeSelect,
  onPickoffOutcomeSelect,
  onRunnerPitchSelect,
  onFielderTap,
  onFielderDone,
  onRunnerTokenTap,
  onDestinationTap,
  onPlacementUndo,
  onRunnerSelect,
  onBack,
  baseJerseys,
  batterJersey,
}) => {
  // The fielder picker has its own layout — sequence display, 2-column grid + RF
  // centered below, Done button. Render it as a separate branch and return early.
  if (stage === 'bip_fielders') {
    const typeLabel = battedBall ? (battedBallOptions.find(o => o.key === battedBall)?.label || '') : '';
    const outcomeLabel = pendingPa
      ? (
          // Look up the friendly label across the menus the pending category might live in.
          (bipOutcomeOptions.find(o => o.key === pendingPa.category)?.label) ||
          (batterOutOptions.find(o => o.key === pendingPa.category)?.label) ||
          (pendingPa.extras?.hitType ? bipOutcomeOptions.find(o => o.hitType === pendingPa.extras.hitType)?.label : null) ||
          pendingPa.category
        )
      : '';
    // Sequence display: empty placeholder, or e.g. "5 — 3" for tapped sequence.
    const seqText = (fielderSequence && fielderSequence.length > 0)
      ? fielderSequence.map(code => {
          const f = fielderOptions.find(x => x.code === code);
          return f ? f.num : '?';
        }).join(' — ')
      : null;
    return (
      <div>
        {/* Breadcrumb + Back */}
        <div style={{
          display: 'flex', alignItems: 'center', justifyContent: 'space-between',
          marginBottom: '14px', paddingBottom: '10px',
          borderBottom: `1px solid ${T.rule}`,
        }}>
          <div style={{
            fontSize: '11px', fontWeight: 400, lineHeight: 1.3,
          }}>
            {pendingRunnerAdvance ? (() => {
              const categoryLabels = {
                stolen_base: 'Stolen Base',
                caught_stealing: 'Caught Stealing',
                pickoff: 'Pickoff',
                battery_error: 'Battery Error',
                other: 'Other',
              };
              const pitchLabels = {
                ball: 'Ball',
                called_strike: 'Called strike',
                swing_miss: 'Swing & miss',
                foul: 'Foul',
              };
              return (
                <>
                  <span style={{ color: T.inkMuted }}>Runner advance</span>
                  {'  '}<span style={{ color: T.inkMuted }}>→ {categoryLabels[pendingRunnerAdvance.category] || pendingRunnerAdvance.category}</span>
                  {pendingRunnerAdvance.pitch && (
                    <>{'  '}<span style={{ color: T.inkMuted }}>→ {pitchLabels[pendingRunnerAdvance.pitch] || pendingRunnerAdvance.pitch}</span></>
                  )}
                  {'  '}<span style={{ color: T.inkMuted }}>→ Diamond</span>
                </>
              );
            })() : pendingPa?.category === 'dropped_third_strike' ? (
              <>
                <span style={{ color: T.inkMuted }}>Dropped third strike</span>
                {'  '}<span style={{ color: T.inkMuted }}>→ {pendingPa.extras?.droppedThirdSafe ? 'Safe at 1st' : 'Out at 1st'}</span>
                {'  '}<span style={{ color: T.inkMuted }}>→ Diamond</span>
              </>
            ) : (
              <>
                <span style={{ color: T.inkMuted }}>{typeLabel}</span>
                {'  '}<span style={{ color: T.inkMuted }}>→ {outcomeLabel}</span>
                {'  '}<span style={{ color: T.inkMuted }}>→ Fielders</span>
              </>
            )}
          </div>
          <button
            onClick={onBack}
            style={{
              background: 'transparent', border: `1px solid ${T.ruleStrong}`,
              borderRadius: '3px', color: '#555', fontSize: '11px',
              fontWeight: 400, cursor: 'pointer', padding: '6px 10px', fontFamily: 'inherit',
            }}
          >
            ← Back
          </button>
        </div>

        {/* Sequence display — accumulates as scorer taps. Italic placeholder when empty. */}
        <div style={{
          background: T.paper,
          border: `1px dashed ${T.ruleStrong}`,
          borderRadius: '4px',
          padding: '10px 14px',
          marginBottom: '12px',
          textAlign: 'center',
          minHeight: '44px',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}>
          {seqText ? (
            <span style={{
              fontSize: '20px', fontWeight: 400, color: T.ink, fontVariantNumeric: 'tabular-nums',
            }}>
              {seqText}
            </span>
          ) : (
            <span style={{
              fontSize: '11px', color: T.inkFaint, fontStyle: 'italic',
            }}>
              Tap fielders in order…
            </span>
          )}
        </div>

        {/* Fielder diamond — positions mapped to where players stand on the field.
            Tapped fielders fill red with a small black sequence-badge in the top-right
            corner showing tap order (1, 2, 3...). Tap a tapped fielder again to toggle
            it off (removes the most recent occurrence). Tap a fresh fielder to append. */}
        <FieldDiamond
          fielderOptions={fielderOptions}
          fielderSequence={fielderSequence}
          onFielderTap={onFielderTap}
          runnerPlacements={runnerPlacements}
          armedRunner={armedRunner}
          touchedRunners={touchedRunners}
          placementHistory={placementHistory}
          onPlacementUndo={onPlacementUndo}
          onRunnerTokenTap={onRunnerTokenTap}
          onDestinationTap={onDestinationTap}
          baseJerseys={baseJerseys}
          batterJersey={batterJersey}
        />

        {/* Done button — disabled until at least one fielder is tapped.
            For runner-advance flow (pendingRunnerAdvance set), fielders are optional —
            steal/CS/passed ball doesn't always involve a logged defensive sequence. */}
        {(() => {
          const isRunnerAdvance = !!pendingRunnerAdvance;
          const hasFielders = fielderSequence && fielderSequence.length > 0;
          const enabled = isRunnerAdvance || hasFielders;
          return (
            <button
              onClick={onFielderDone}
              disabled={!enabled}
              style={{
                width: '100%',
                background: enabled ? T.ink : T.inkPlaceholder,
                border: 'none',
                color: enabled ? T.paperRaised : T.inkFaint,
                borderRadius: '5px',
                padding: '14px',
                fontFamily: 'inherit',
                fontSize: '13px',
                fontWeight: 500,
                marginTop: '10px',
                cursor: enabled ? 'pointer' : 'not-allowed',
              }}
            >
              Done ✓
            </button>
          );
        })()}
      </div>
    );
  }

  // Resolve options, prompt, handler, color theme by stage.
  let opts, prompt, onSelect, breadcrumb, accentBg, accentBorder;

  if (stage === 'pa_top') {
    opts = paOptions;
    prompt = 'Plate appearance';
    onSelect = onPaTopSelect;
    breadcrumb = (
      <span>
        <span style={{ color: T.inkMuted }}>Result</span>
      </span>
    );
    accentBg = T.paper;
    accentBorder = T.rule;
  } else if (stage === 'bip_type') {
    opts = battedBallOptions;
    prompt = 'What kind of ball in play?';
    onSelect = onTypeSelect;
    breadcrumb = (
      <span>
        <span style={{ color: T.inkMuted }}>Ball in play</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ Type</span>
      </span>
    );
    accentBg = T.paper;
    accentBorder = T.rule;
  } else if (stage === 'bip_outcome') {
    const typeLabel = battedBall ? (battedBallOptions.find(o => o.key === battedBall)?.label || '') : '';
    opts = bipOutcomeOptions;
    prompt = 'What was the outcome?';
    onSelect = onOutcomeSelect;
    breadcrumb = (
      <span>
        <span style={{ color: T.inkMuted }}>Ball in play</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ {typeLabel}</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ Outcome</span>
      </span>
    );
    accentBg = T.paper;
    accentBorder = T.rule;
  } else if (stage === 'bip_batter_out') {
    const typeLabel = battedBall ? (battedBallOptions.find(o => o.key === battedBall)?.label || '') : '';
    opts = batterOutOptions;
    prompt = 'How was the out made?';
    onSelect = onBatterOutSelect;
    breadcrumb = (
      <span>
        <span style={{ color: T.inkMuted }}>Ball in play</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ {typeLabel}</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ Batter out</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ Type</span>
      </span>
    );
    accentBg = T.paper;
    accentBorder = T.rule;
  } else if (stage === 'bip_fc_forceout') {
    // FC forceout-location sub-cascade. Options depend on the cell's stateKey:
    //   loaded → Home / 3B / 2B (forceout at home means 3B runner is out, no run scores)
    //   12     → 3B / 2B (no run-scoring question — nobody is forced home from a runner-on-2B-and-1B state)
    const typeLabel = battedBall ? (battedBallOptions.find(o => o.key === battedBall)?.label || '') : '';
    if (cellStateKey === 'loaded') {
      opts = [
        { key: 'home', label: 'Forceout at Home' },
        { key: '3B',   label: 'Forceout at 3rd' },
        { key: '2B',   label: 'Forceout at 2nd' },
      ];
    } else {
      // '12'
      opts = [
        { key: '3B',   label: 'Forceout at 3rd' },
        { key: '2B',   label: 'Forceout at 2nd' },
      ];
    }
    prompt = 'Where was the forceout?';
    onSelect = onFcForceoutSelect;
    breadcrumb = (
      <span>
        <span style={{ color: T.inkMuted }}>Ball in play</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ {typeLabel}</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ Fielder's choice</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ Forceout</span>
      </span>
    );
    accentBg = T.paper;
    accentBorder = T.rule;
  } else if (stage === 'dropped_third_outcome') {
    // Dropped third strike outcome sub-cascade. Choose whether the batter was
    // put out or reached safely. Either way the diamond opens next so the scorer
    // can capture runner movement and the catcher's recovery throw.
    opts = [
      { key: 'out',  label: 'Out at 1st' },
      { key: 'safe', label: 'Safe at 1st' },
    ];
    prompt = 'Dropped 3rd — was the batter out or safe?';
    onSelect = onDroppedThirdOutcomeSelect;
    breadcrumb = (
      <span>
        <span style={{ color: T.inkMuted }}>Dropped third strike</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ Outcome</span>
      </span>
    );
    accentBg = T.paper;
    accentBorder = T.rule;
  } else if (stage === 'runner_pitch') {
    // v1.5 — Pitch sub-cascade for runner-advance categories. Captures the pitch
    // that coincided with the steal/CS/BE so the pitch event is logged alongside
    // the between_pitch event. Battery Error excludes Foul (wild pitch / passed
    // ball isn't a foul ball). Defensive Indifference excludes Foul (DI can't
    // happen on a foul) and adds a "No Pitch" escape for when DI happens with
    // no pitch thrown.
    const categoryLabels = {
      stolen_base: 'Stolen Base',
      caught_stealing: 'Caught Stealing',
      battery_error: 'Battery Error',
      defensive_indifference: 'Defensive Indifference',
    };
    const cat = pendingRunnerAdvance?.category;
    const baseOpts = [
      { key: 'ball',          label: 'Ball' },
      { key: 'called_strike', label: 'Called strike' },
      { key: 'swing_miss',    label: 'Swing & miss' },
      { key: 'foul',          label: 'Foul' },
    ];
    if (cat === 'defensive_indifference') {
      opts = [
        { key: 'ball',          label: 'Ball' },
        { key: 'called_strike', label: 'Called strike' },
        { key: 'swing_miss',    label: 'Swing & miss' },
        { key: 'no_pitch',      label: 'No Pitch' },
      ];
    } else if (cat === 'battery_error') {
      opts = baseOpts.filter(o => o.key !== 'foul');
    } else {
      opts = baseOpts;
    }
    prompt = 'What was the pitch?';
    onSelect = onRunnerPitchSelect;
    breadcrumb = (
      <span>
        <span style={{ color: T.inkMuted }}>Runner advance</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ {categoryLabels[cat] || cat}</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ Pitch</span>
      </span>
    );
    accentBg = T.paper;
    accentBorder = T.ruleStrong;
  } else if (stage === 'pickoff_outcome') {
    // Pickoff outcome — pickoffs often go awry, so the result isn't always an out.
    opts = [
      { key: 'out',      label: 'Runner Out' },
      { key: 'safe',     label: 'Safe at Bag' },
      { key: 'advanced', label: 'Advanced (error)' },
    ];
    prompt = 'Pickoff — what happened?';
    onSelect = onPickoffOutcomeSelect;
    breadcrumb = (
      <span>
        <span style={{ color: T.inkMuted }}>Runner advance</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ Pickoff</span>
        {'  '}<span style={{ color: T.inkMuted }}>→ Outcome</span>
      </span>
    );
    accentBg = T.paper;
    accentBorder = T.rule;
  } else {
    // 'runner'
    opts = runnerOptions;
    prompt = 'Runner advance';
    onSelect = onRunnerSelect;
    breadcrumb = (
      <span>
        <span style={{ color: T.inkMuted }}>Runner advance</span>
      </span>
    );
    accentBg = T.paper;
    accentBorder = T.ruleStrong;
  }

  return (
    <div>
      {/* Breadcrumb + Back */}
      <div style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        marginBottom: '14px',
        paddingBottom: '10px',
        borderBottom: `1px solid ${T.rule}`,
      }}>
        <div style={{
          fontSize: '11px',
          fontWeight: 400,
          lineHeight: 1.3,
        }}>
          {breadcrumb}
        </div>
        <button
          onClick={onBack}
          style={{
            background: 'transparent',
            border: `1px solid ${T.ruleStrong}`,
            borderRadius: '4px',
            color: '#555',
            fontSize: '11px',
            fontWeight: 400,
            cursor: 'pointer',
            padding: '6px 10px',
            fontFamily: 'inherit',
          }}
        >
          ← Back
        </button>
      </div>

      {/* Stage prompt */}
      <div style={{
        fontSize: '11px',
        fontWeight: 400,
        color: T.inkMuted,
        margin: '0 2px 10px',
        textAlign: 'center',
      }}>
        {prompt}
      </div>

      {/* Full-bleed rows separated by hairlines, like a ruled page. No per-row
          borders, no cards, no tint -- a row reads as tappable because it is a row.
          Cascade options no longer carry an arrow; opening another screen is
          discovered by tapping, and the breadcrumb above says where you are. */}
      <div style={{
        display: 'flex', flexDirection: 'column',
        borderTop: `1px solid ${T.rule}`,
      }}>
        {opts.map(o => {
          const isCascade = !!o.cascade;
          return (
            <button
              key={o.key}
              onClick={() => onSelect(o.key)}
              style={{
                width: '100%',
                background: 'transparent',
                border: 'none',
                borderBottom: `1px solid ${T.rule}`,
                borderRadius: 0,
                minHeight: '52px',
                padding: '18px 14px',
                fontFamily: 'inherit',
                fontSize: '15px',
                fontWeight: 400,
                color: T.ink,
                cursor: 'pointer',
                textAlign: 'left',
                display: 'flex',
                justifyContent: 'space-between',
                alignItems: 'center',
                transition: 'transform 100ms ease, filter 100ms ease',
              }}
              onPointerDown={(e) => {
                e.currentTarget.style.transform = 'scale(0.99)';
                e.currentTarget.style.background = T.paperSunk;
              }}
              onPointerUp={(e) => {
                e.currentTarget.style.transform = 'scale(1)';
                e.currentTarget.style.background = 'transparent';
              }}
              onPointerLeave={(e) => {
                e.currentTarget.style.transform = 'scale(1)';
                e.currentTarget.style.background = 'transparent';
              }}
            >
              <span>{o.label}</span>
              
            </button>
          );
        })}
      </div>
    </div>
  );
};

// v1.2 — ExpandedCellView
// The capture surface that replaces the matrix grid when a cell is tapped.
// Lives inside the matrix panel footprint (same cream card, same max-width).
// Three button rows (Pitch / PA Result / Runner advance) plus an action bar (Undo / Pitching Change).
// Pitch taps stay in the cell; PA-end and Runner-advance taps flash the button ~150ms then auto-return.
// Velocity picker. A thumb-drag scroller rather than a keypad: at youth level the
// reading is an estimate from the stands, and typing digits between pitches is too
// slow. Uses native scroll with CSS snap so iOS gives real momentum flick physics —
// a custom touch handler would feel worse and stutter on long lists.
// Opens centered on the last velocity recorded for this pitcher, so consecutive
// pitches are a nudge rather than a full scroll.
const VeloScroller = ({ min, max, initial, onPick, onSkip, pitchTypeLabel }) => {
  const ITEM_H = 46;
  const VISIBLE = 5;
  const values = React.useMemo(() => {
    const out = [];
    for (let v = min; v <= max; v++) out.push(v);
    return out;
  }, [min, max]);

  const clampIdx = (i) => Math.max(0, Math.min(values.length - 1, i));
  const startIdx = clampIdx((initial != null ? initial : Math.round((min + max) / 2)) - min);
  const [idx, setIdx] = React.useState(startIdx);
  const scrollRef = React.useRef(null);

  // Jump to the starting value without animation on first paint.
  React.useEffect(() => {
    if (scrollRef.current) scrollRef.current.scrollTop = startIdx * ITEM_H;
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  const handleScroll = (e) => {
    const i = clampIdx(Math.round(e.target.scrollTop / ITEM_H));
    if (i !== idx) setIdx(i);
  };

  const nudge = (delta) => {
    const i = clampIdx(idx + delta);
    setIdx(i);
    if (scrollRef.current) scrollRef.current.scrollTo({ top: i * ITEM_H, behavior: 'smooth' });
  };

  const pad = ITEM_H * Math.floor(VISIBLE / 2);

  return (
    <div>
      <div style={{ fontSize: '11px', fontWeight: 400, color: T.inkMuted, textAlign: 'center', marginBottom: '2px' }}>
        {pitchTypeLabel}
      </div>
      <div style={{ fontSize: '12px', color: T.inkFaint, textAlign: 'center', marginBottom: '10px' }}>
        Drag to set speed
      </div>

      <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
        <button
          onClick={() => nudge(-1)}
          style={{
            width: '44px', height: '44px', flexShrink: 0, borderRadius: '22px',
            border: `1px solid ${T.ruleStrong}`, background: 'transparent',
            fontSize: '22px', fontWeight: 400, color: T.inkMuted, cursor: 'pointer', fontFamily: 'inherit',
          }}
          aria-label="Decrease"
        >−</button>

        <div style={{ position: 'relative', flex: 1, height: `${ITEM_H * VISIBLE}px` }}>
          {/* Center band marking the selected row */}
          <div style={{
            position: 'absolute', left: 0, right: 0, top: `${pad}px`, height: `${ITEM_H}px`,
            background: 'transparent',
            borderTop: `1px solid ${T.ruleStrong}`,
            borderBottom: `1px solid ${T.ruleStrong}`,
            borderRadius: 0, pointerEvents: 'none', zIndex: 1,
          }} />
          <div
            ref={scrollRef}
            onScroll={handleScroll}
            style={{
              height: '100%', overflowY: 'scroll', scrollSnapType: 'y mandatory',
              WebkitOverflowScrolling: 'touch', position: 'relative', zIndex: 2,
              scrollbarWidth: 'none', msOverflowStyle: 'none',
            }}
          >
            <div style={{ height: `${pad}px` }} />
            {values.map((v, i) => (
              <div
                key={v}
                style={{
                  height: `${ITEM_H}px`, scrollSnapAlign: 'center',
                  display: 'flex', alignItems: 'center', justifyContent: 'center',
                  fontSize: i === idx ? '30px' : '20px',
                  fontWeight: i === idx ? 500 : 400,
                  color: i === idx ? T.ink : T.inkPlaceholder,
                  fontVariantNumeric: 'tabular-nums',
                  fontVariantNumeric: 'tabular-nums',
                  transition: 'font-size 90ms linear, color 90ms linear',
                }}
              >{v}</div>
            ))}
            <div style={{ height: `${pad}px` }} />
          </div>
        </div>

        <button
          onClick={() => nudge(1)}
          style={{
            width: '44px', height: '44px', flexShrink: 0, borderRadius: '22px',
            border: `1px solid ${T.ruleStrong}`, background: 'transparent',
            fontSize: '22px', fontWeight: 400, color: T.inkMuted, cursor: 'pointer', fontFamily: 'inherit',
          }}
          aria-label="Increase"
        >+</button>
      </div>

      <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', marginTop: '14px' }}>
        <button
          onClick={() => onPick(values[idx])}
          style={{
            width: '100%', padding: '15px', background: T.ink, color: T.paperRaised, border: 'none',
            borderRadius: '8px', fontFamily: 'inherit', fontSize: '15px', fontWeight: 600, cursor: 'pointer',
          }}
        >Log {values[idx]} mph</button>
        <button
          onClick={onSkip}
          style={{
            width: '100%', padding: '11px', background: 'transparent', color: T.inkMuted, border: 'none',
            fontFamily: 'inherit', fontSize: '13px', fontWeight: 500, cursor: 'pointer',
          }}
        >No reading — skip speed</button>
      </div>
    </div>
  );
};

const ExpandedCellView = ({
  cell,
  count,
  batterSlot,
  lineupMarkerCount,
  onMarkLeadoff,
  jerseyNumber,
  onJerseyNumberChange,
  pitcherNumber,
  onPitcherNumberChange,
  flashingButton,
  onPitchTap,
  onPaEndTap,
  onRunnerAdvanceTap,
  onEndHalfInning,
  baseJerseys,
  onUndo,
  canUndo,
  pitchDetailOn,
  onTogglePitchDetail,
  sport,
  lastVelo,
  onVeloRecorded,
}) => {
  // Local flash state for buttons whose flash is purely visual (Pitch row, Undo).
  // PA Result and Runner advance use the parent-level `flashingButton` because their
  // flash is tied to the auto-return-to-matrix behavior.
  const [localFlash, setLocalFlash] = React.useState(null);
  const flashLocally = (key) => {
    setLocalFlash(key);
    setTimeout(() => setLocalFlash(null), 120);
  };

  // v1.5 — Jersey-number inline edit state. Activated by tapping the jersey chip
  // next to "Batter X of Y". Persists into gameMeta via onJerseyNumberChange.
  const [jerseyEditing, setJerseyEditing] = React.useState(false);
  const [jerseyDraft, setJerseyDraft] = React.useState('');
  const [pitcherEditing, setPitcherEditing] = React.useState(false);
  const [pitcherDraft, setPitcherDraft] = React.useState('');
  // Trailing-runner prompt — on a steal/battery error with a runner behind the mover,
  // ask whether the trailing runner(s) also advanced. { runnerMovements, category,
  // pitch, paEndCategory, trailingBases } | null
  const [trailRunnerPrompt, setTrailRunnerPrompt] = React.useState(null);

  // Leadoff-mark confirm dialog. Opens when scorer taps the batter header.
  // First confirm anchors slot 1; second confirm resolves lineup size.
  const [leadoffDialogOpen, setLeadoffDialogOpen] = React.useState(false);
  const openJerseyEdit = () => {
    setJerseyDraft(jerseyNumber || '');
    setJerseyEditing(true);
  };
  const commitJerseyEdit = () => {
    if (onJerseyNumberChange) onJerseyNumberChange(jerseyDraft);
    setJerseyEditing(false);
  };
  const cancelJerseyEdit = () => {
    setJerseyEditing(false);
    setJerseyDraft('');
  };
  const openPitcherEdit = () => {
    setPitcherDraft(pitcherNumber || '');
    setPitcherEditing(true);
  };
  const commitPitcherEdit = () => {
    if (onPitcherNumberChange) onPitcherNumberChange(pitcherDraft);
    setPitcherEditing(false);
  };
  const cancelPitcherEdit = () => {
    setPitcherEditing(false);
    setPitcherDraft('');
  };


  // Resolve cell state metadata. Both BASE_STATES arrays share keys, so either works.
  const stateMeta = BASE_STATES_LOOPHOLE.find(s => s.key === cell.stateKey)
                 || { label: cell.stateKey, bases: [0, 0, 0] };
  const stateLabel = stateMeta.label.toUpperCase();
  const outsLabel = `${cell.outs} OUT${cell.outs !== 1 ? 'S' : ''}`;

  // Common button styles
  const baseBtnStyle = {
    border: 'none',
    borderRight: `1px solid ${T.rule}`,
    borderBottom: `1px solid ${T.rule}`,
    borderRadius: 0,
    padding: '14px 4px',
    fontFamily: 'inherit',
    fontSize: '15px',
    fontWeight: 400,
    color: T.ink,
    cursor: 'pointer',
    textAlign: 'center',
    lineHeight: 1.15,
    minHeight: '56px',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    transition: 'transform 100ms ease, filter 100ms ease, box-shadow 200ms ease',
    background: 'transparent',
  };
  const flashStyle = {
    transform: 'scale(0.97)',
    filter: 'none',
    boxShadow: `0 0 14px ${T.rule}`,
    background: T.ink,
    color: T.paperRaised,
    borderColor: T.ink,
  };

  const isFlashing = (row, key) =>
    flashingButton && flashingButton.row === row && flashingButton.key === key;

  // Pitch row
  const pitchButtons = [
    { key: 'ball', label: 'Ball' },
    { key: 'called_strike', label: <>Called<br />strike</> },
    { key: 'swing_miss', label: <>Swing &amp;<br />miss</> },
    { key: 'foul', label: 'Foul' },
  ];
  // Top-level PA Result dropdown. v1.4 cascading model:
  //   - 'ball_in_play' cascades into a 2nd dropdown for batted-ball type, then a
  //     3rd dropdown for outcome (Hit / Out / Reached on error). The scorer
  //     never directly fires a ball_in_play event — only the leaf outcome fires.
  //   - Walk, Strikeout types, Dropped third strike, Other commit immediately.
  const paOptions = [
    { key: 'ball_in_play',          label: 'Ball in play',          cascade: true },
    { key: 'walk',                  label: 'Walk' },
    { key: 'strikeout_called',      label: 'Strikeout — called' },
    { key: 'strikeout_swinging',    label: 'Strikeout — swinging' },
    { key: 'hit_by_pitch',          label: 'Hit by pitch' },
    { key: 'intentional_walk',      label: 'Intentional walk' },
    { key: 'dropped_third_strike',  label: 'Dropped third strike' },
    { key: 'batter_out',            label: 'Batter out (Interference)' },
    { key: 'other',                 label: 'Other' },
  ];
  // 2nd-level cascade: batted-ball type. Selected after 'ball_in_play'.
  // v1.5: Pop fly added (infield pop-up; distinct from outfield Fly ball for defensive
  // credit and scoring — an IF pop-up caught by 2B is different from a CF flyout).
  // Hard Ground ball was dropped — over-granular for live tagging; existing events
  // with hard_ground_ball remain valid.
  const battedBallOptions = [
    { key: 'ground_ball',      label: 'Ground ball' },
    { key: 'fly_ball',         label: 'Fly ball' },
    { key: 'pop_fly',          label: 'Pop fly' },
    { key: 'line_drive',       label: 'Line drive' },
    { key: 'bunt',             label: 'Bunt' },
  ];
  // 3rd-level cascade: outcome of the batted ball. v1.5 — type-specific menus.
  // Different batted-ball types produce different outcome distributions, so each type
  // has its own menu. Common outcomes (single/double/triple/HR/error) appear across
  // most types; type-specific outcomes (Fielder's choice on grounders, etc.) appear
  // only where they're possible.
  //
  // Hits keep the unified category 'hit' on the pa_end event but gain a hitType field
  // that captures base advancement (single/double/triple/in_park_hr). This keeps
  // analytics simple — every hit groups by category === 'hit' — but preserves the
  // granularity the cascade collects.
  //
  // v1.5 step 5: Batter out now cascades to a type-specific sub-screen (Out at 1st,
  // Double Play, Fly Out, Sac Fly, Line Out, Sacrifice Bunt). Foul ball added where
  // relevant; it's not a PA-end — it fires the same path as the Foul pitch button.
  // B. Interference deliberately not in the cascade — too rare for a dedicated path;
  // scorers route through Other in the top-level PA Result menu.
  const bipOutcomesByType = {
    ground_ball: [
      { key: 'batter_out_cascade', label: 'Batter out',         cascade: true },
      { key: 'single',             label: 'Single',             hitType: 'single' },
      { key: 'double',             label: 'Double',             hitType: 'double' },
      { key: 'triple',             label: 'Triple',             hitType: 'triple' },
      { key: 'in_park_hr',         label: 'In-the-park home run',     hitType: 'in_park_hr' },
      { key: 'fielders_choice',    label: "Fielder's choice" },
      { key: 'reached_on_error',   label: 'Reached on error' },
      { key: 'foul_ball_bip',      label: 'Foul ball',          foulPitch: true },
    ],
    hard_ground_ball: [
      { key: 'batter_out_cascade', label: 'Batter out',         cascade: true },
      { key: 'single',             label: 'Single',             hitType: 'single' },
      { key: 'double',             label: 'Double',             hitType: 'double' },
      { key: 'triple',             label: 'Triple',             hitType: 'triple' },
      { key: 'in_park_hr',         label: 'In-the-park home run',     hitType: 'in_park_hr' },
      { key: 'fielders_choice',    label: "Fielder's choice" },
      { key: 'reached_on_error',   label: 'Reached on error' },
      { key: 'foul_ball_bip',      label: 'Foul ball',          foulPitch: true },
    ],
    fly_ball: [
      { key: 'batter_out_cascade', label: 'Batter out',         cascade: true },
      { key: 'single',             label: 'Single',             hitType: 'single' },
      { key: 'double',             label: 'Double',             hitType: 'double' },
      { key: 'triple',             label: 'Triple',             hitType: 'triple' },
      { key: 'home_run',           label: 'Home run',           hitType: 'home_run' },
      { key: 'in_park_hr',         label: 'In-the-park home run',     hitType: 'in_park_hr' },
      { key: 'reached_on_error',   label: 'Reached on error' },
      { key: 'foul_ball_bip',      label: 'Foul ball',          foulPitch: true },
    ],
    pop_fly: [
      { key: 'batter_out_cascade', label: 'Batter out',         cascade: true },
      { key: 'single',             label: 'Single',             hitType: 'single' },
      { key: 'double',             label: 'Double',             hitType: 'double' },
      { key: 'triple',             label: 'Triple',             hitType: 'triple' },
      { key: 'reached_on_error',   label: 'Reached on error' },
      { key: 'foul_ball_bip',      label: 'Foul ball',          foulPitch: true },
    ],
    line_drive: [
      { key: 'batter_out_cascade', label: 'Batter out',         cascade: true },
      { key: 'single',             label: 'Single',             hitType: 'single' },
      { key: 'double',             label: 'Double',             hitType: 'double' },
      { key: 'triple',             label: 'Triple',             hitType: 'triple' },
      { key: 'home_run',           label: 'Home run',           hitType: 'home_run' },
      { key: 'in_park_hr',         label: 'In-the-park home run',     hitType: 'in_park_hr' },
      { key: 'reached_on_error',   label: 'Reached on error' },
    ],
    bunt: [
      { key: 'batter_out_cascade', label: 'Batter out',         cascade: true },
      { key: 'single',             label: 'Single',             hitType: 'single' },
      { key: 'double',             label: 'Double',             hitType: 'double' },
      { key: 'reached_on_error',   label: 'Reached on error' },
      { key: 'foul_ball_bip',      label: 'Foul ball',          foulPitch: true },
    ],
  };
  // Type-specific Batter out sub-cascade menus. Each leaf fires a pa_end with a
  // specific out category. All carry the batted-ball type forward on the pa_end event.
  const batterOutOptionsByType = {
    ground_ball: [
      { key: 'out_at_1st',  label: 'Out at 1st' },
      { key: 'double_play', label: 'Double Play' },
    ],
    hard_ground_ball: [
      { key: 'out_at_1st',  label: 'Out at 1st' },
      { key: 'double_play', label: 'Double Play' },
    ],
    fly_ball: [
      { key: 'fly_out',     label: 'Fly Out' },
      { key: 'sac_fly',     label: 'Sac Fly' },
      { key: 'double_play', label: 'Double Play' },
    ],
    pop_fly: [
      { key: 'fly_out',     label: 'Fly Out' },
      { key: 'sac_fly',     label: 'Sac Fly' },
      { key: 'double_play', label: 'Double Play' },
    ],
    line_drive: [
      { key: 'line_out',    label: 'Line Out' },
      { key: 'sac_fly',     label: 'Sac Fly' },
      { key: 'double_play', label: 'Double Play' },
    ],
    bunt: [
      { key: 'sac_bunt',    label: 'Sacrifice Bunt' },
      { key: 'out_at_1st',  label: 'Out at 1st' },
      { key: 'double_play', label: 'Double Play' },
    ],
  };
  // Helper: resolve the current outcome list based on which batted-ball type the
  // scorer chose in stage 2. Falls back to a sensible default if the type is unknown
  // (shouldn't happen in normal flow but defensive coding).
  const getCurrentBipOutcomes = () => {
    if (cascadeBattedBall && bipOutcomesByType[cascadeBattedBall]) {
      return bipOutcomesByType[cascadeBattedBall];
    }
    return bipOutcomesByType.ground_ball;
  };
  // Helper: resolve the Batter out sub-cascade menu for the current batted-ball type.
  const getCurrentBatterOutOptions = () => {
    if (cascadeBattedBall && batterOutOptionsByType[cascadeBattedBall]) {
      return batterOutOptionsByType[cascadeBattedBall];
    }
    return batterOutOptionsByType.ground_ball;
  };
  // Runner advance dropdown options.
  // None of these log an implicit pitch — they're between-pitch events.
  const runnerOptions = [
    { key: 'stolen_base',           label: 'Stolen Base' },
    { key: 'caught_stealing',       label: 'Caught Stealing' },
    { key: 'pickoff',               label: 'Pickoff' },
    { key: 'battery_error',         label: 'Battery Error' },
    { key: 'defensive_indifference', label: 'Defensive Indifference' },
    { key: 'other',                 label: 'Other' },
  ];

  // Fielder positions for the picker (v1.5 step 6). Traditional baseball position
  // numbers 1-9 are preserved in the `num` field for scorebook display ("5 — 3" for
  // a third-base-to-first groundout). The `code` field is the analytic key stored
  // in the pa_end event's fielderSequence array.
  const fielderOptions = [
    { num: 1, code: 'P',  label: 'Pitcher' },
    { num: 2, code: 'C',  label: 'Catcher' },
    { num: 3, code: '1B', label: '1st Base' },
    { num: 4, code: '2B', label: '2nd Base' },
    { num: 5, code: '3B', label: '3rd Base' },
    { num: 6, code: 'SS', label: 'Shortstop' },
    { num: 7, code: 'LF', label: 'Left Field' },
    { num: 8, code: 'CF', label: 'Center Field' },
    { num: 9, code: 'RF', label: 'Right Field' },
  ];

  // Cascade state: which screen-takeover stage is active.
  //   null               = normal cell view (Pitch row + PA button + Runner button + Action bar)
  //   'pa_top'           = full takeover, all top-level PA Result options
  //   'bip_type'         = full takeover, BIP cascade level 2 (Ground / Fly / Line / Bunt / etc.)
  //   'bip_outcome'      = full takeover, BIP cascade level 3 (Hit / Out / Error / etc., type-specific)
  //   'bip_batter_out'   = full takeover, BIP cascade level 4 — Batter out sub-cascade (Out at 1st / DP / etc.)
  //   'bip_fielders'     = full takeover, BIP cascade level 5 — Fielder picker (v1.5 step 6)
  //   'runner'           = full takeover, Runner advance options
  const [cascadeStage, setCascadeStage] = React.useState(null);
  const [cascadeBattedBall, setCascadeBattedBall] = React.useState(null);
  // "More pitch" cascade — catch-all for low-frequency pitch events that don't
  // earn space on the main pitch row: Bunt attempt, Balk, Hit by pitch,
  // Intentional ball. Opened by tapping the mound. Closed by selecting an option
  // or tapping outside the popover.
  const [morePitchOpen, setMorePitchOpen] = React.useState(false);
  // Ball-4 / strike-3 confirmation. Tapping the pitch that would end the PA fires a
  // prompt (mistap guard) instead of auto-committing. null | {kind:'walk'} |
  // {kind:'strikeout', pitch}. Strike 3 also offers Dropped Third.
  const [pitchEndConfirm, setPitchEndConfirm] = React.useState(null);
  // Pitch Detail capture (mode-gated). Holds the already-determined outcome while the
  // scorer picks pitch type, then velocity.
  // null | { pitchKey, stage: 'type'|'velo', pitchType, paEnd }
  const [pitchDetail, setPitchDetail] = React.useState(null);
  // Bunt attempt is a two-step within the more-cascade: tap Bunt attempt → ask
  // "How did it resolve?" (Strike / Foul / In Play). Tracked as a sub-stage.
  const [moreSubStage, setMoreSubStage] = React.useState(null); // null | 'bunt_resolve'

  // Shared button style for items inside the more-pitch popover.
  const moreItemStyle = {
    background: 'transparent',
    border: 'none',
    borderBottom: `1px solid ${T.rule}`,
    borderRadius: 0,
    padding: '16px 14px',
    minHeight: '52px',
    fontFamily: 'inherit',
    fontSize: '15px',
    fontWeight: 400,
    color: T.ink,
    cursor: 'pointer',
    textAlign: 'left',
  };
  // Fielder picker holding state:
  //   pendingPa        = { category, extras } captured at the leaf-outcome stage, fired
  //                       only when the scorer taps Done. extras include battedBall, hitType.
  //   fielderSequence  = positions tapped so far in order, e.g. ['3B', '1B'] for a 5-3.
  const [pendingPa, setPendingPa] = React.useState(null);
  // Pitch type / velocity captured when Ball in play was tapped, held until the
  // cascade resolves and merged into whatever it produces. Null when detail is off,
  // skipped, or the scorer backed out of the cascade.
  const [bipPitchInfo, setBipPitchInfo] = React.useState(null);
  const [fielderSequence, setFielderSequence] = React.useState([]);

  // v1.5 step 7 — Runner placement state for the diamond.
  //   runnerPlacements = { batter, '1B', '2B', '3B' }
  //                       — keys are sources (where the runner started). 'batter' starts
  //                         at home; '1B'/'2B'/'3B' represent pre-existing runners.
  //                       — values are destinations ('1B'|'2B'|'3B'|'home'|
  //                         'out_at_1B'|'out_at_2B'|'out_at_3B'|'out_at_home') OR null
  //                         (runner not present, or batter not yet placed).
  //                       — Pre-populated based on hit type when the diamond opens; the
  //                         scorer revises specific runners who didn't follow the default.
  //   armedRunner      = which source key is currently armed for placement (null = nothing armed).
  //                       Tapping an armed runner disarms. Tapping a base/Out chip places.
  //   placementHistory = stack of { source, prevDestination } entries pushed on each
  //                       scorer-made placement change. Undo pops and restores. Pre-population
  //                       does NOT push to history (so Undo never reverts the initial state).
  const [runnerPlacements, setRunnerPlacements] = React.useState(null);
  const [armedRunner, setArmedRunner] = React.useState(null);
  // touchedRunners — Set of source keys ('batter', '1B', '2B', '3B') that the
  // scorer has tapped at least once during the current diamond session. Used
  // to drive the untouched-runner pulse animation. Reset to empty Set on each
  // diamond open; emptied / discarded on Done / Cancel.
  const [touchedRunners, setTouchedRunners] = React.useState(new Set());
  const [placementHistory, setPlacementHistory] = React.useState([]);

  // Helper: open the diamond with a fresh placement seed. Resets all the
  // per-session state — placement history, armed runner, and the touched-runner
  // tracker that drives the pulse animation. Mirror for closeDiamond below.
  // Both helpers exist purely to make every open/close site DRY; the seed value
  // varies (PA seed vs runner-advance seed), so callers pass it in.
  const openDiamond = (seed) => {
    setRunnerPlacements(seed);
    setPlacementHistory([]);
    setArmedRunner(null);
    setTouchedRunners(new Set());
  };
  const closeDiamond = () => {
    setRunnerPlacements(null);
    setPlacementHistory([]);
    setArmedRunner(null);
    setTouchedRunners(new Set());
  };

  // v1.5 — Runner advance flow state. When the scorer taps a runner-advance
  // category (Stolen Base / Caught Stealing / Battery Error / Pickoff / Other),
  // the flow now routes through:
  //   1. (SB/CS/BE only) pitch sub-cascade — capture the pitch that coincided
  //                                          with the runner action
  //   2. diamond                            — pre-populate via lead-runner default,
  //                                            let scorer revise; capture fielder
  //                                            sequence for the catcher's throw etc.
  //   3. Done                                — fire pitch event (if any) + between_pitch
  //                                            with runnerMovements, auto-advance
  // pendingRunnerAdvance holds { category, pitch? } across these stages.
  const [pendingRunnerAdvance, setPendingRunnerAdvance] = React.useState(null);

  // Tap on the "Plate appearance" button in the normal cell view → open pa_top.
  const onOpenPaTakeover = () => setCascadeStage('pa_top');
  // Tap on the "Ball in play" quick button in the pitch row → jump straight to bip_type,
  // skipping the pa_top takeover. Saves one tap on the most common cascade entry.
  // From bip_type, the Back button still lands on pa_top, so the scorer can correct
  // a mis-tap by backing up into the full PA Result menu.
  // Ball in play asks for pitch detail on the tap, same as Ball / Called strike /
  // Swing & miss / Foul, so the prompt order is identical everywhere in the app.
  // The answer is parked in bipPitchInfo and merged in when the cascade resolves.
  const onOpenBipQuick = () => {
    setBipPitchInfo(null);
    if (pitchDetailOn) {
      setPitchDetail({ pitchKey: 'in_play', stage: 'type', pitchType: null, paEnd: null, forBip: true });
      return;
    }
    setCascadeStage('bip_type');
  };
  // Tap on the "Runner advance" button in the normal cell view → open runner takeover.
  const onOpenRunnerTakeover = () => setCascadeStage('runner');

  // ── 2B: idle diamond (inverted runner-advance) ──────────────────────────────
  // The diamond is now always visible on the idle cell surface, seeded with the
  // current base runners derived from the cell's stateKey. Tapping a runner arms
  // it; tapping a destination records that move and opens the runner-advance
  // category cascade (placement-first inversion). The category cascade then runs
  // category → pitch (if SB/CS/BE) → commit, reusing the existing commit tail.
  const idleMode = cascadeStage === null;

  // Walk/HBP/IBB animation: these PA-ends commit directly (no diamond), so the idle
  // diamond would normally re-seed runners straight onto their new bases. When set,
  // idleAnimMovements seeds the runners at their OLD bases (batter at home) with their
  // forced destinations as placements, so the path-stepping animation slides them — the
  // same look as every other advance. Cleared after the slide plays.
  const [idleAnimMovements, setIdleAnimMovements] = React.useState(null);

  // Build a placements seed from the current base state. Runners sit at their own
  // base (placement === own base, i.e. "not yet moved"). No batter token on idle —
  // the batter is at the plate, which is the PA flow, not runner-advance.
  const seedIdlePlacementsFromState = React.useCallback(() => {
    if (idleAnimMovements) {
      // Animated forced advance (walk/HBP/IBB): seed at origins → forced targets.
      const seed = {};
      for (const m of idleAnimMovements) {
        const srcKey = m.from === 'home' ? 'batter' : m.from;
        seed[srcKey] = m.to;
      }
      return seed;
    }
    const sk = cell?.stateKey || 'empty';
    const has1 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('1'));
    const has2 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('2'));
    const has3 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('3'));
    const seed = {};
    if (has1) seed['1B'] = '1B';
    if (has2) seed['2B'] = '2B';
    if (has3) seed['3B'] = '3B';
    return seed;
  }, [cell?.stateKey, idleAnimMovements]);

  // Idle destination tap staging: holds the single runner move { from, to } the
  // scorer made on the idle diamond, carried into the category cascade and applied
  // at commit. Declared before the seeding effect that resets it.
  const [idleRunnerMove, setIdleRunnerMove] = React.useState(null); // { from, to } | null

  // When idle, keep the diamond seeded with the live base state. Re-seed whenever
  // the cell's stateKey changes (a new half-inning, an advance committed, etc.) so
  // the idle diamond always mirrors the matrix's current base-out cell. We only
  // seed in idle mode; once a cascade opens, the cascade owns runnerPlacements.
  React.useEffect(() => {
    if (!idleMode) return;
    setRunnerPlacements(seedIdlePlacementsFromState());
    setArmedRunner(null);
    setPlacementHistory([]);
    setTouchedRunners(new Set());
    setIdleRunnerMove(null);
  }, [idleMode, seedIdlePlacementsFromState]);

  // Idle destination tap: the scorer has armed a runner and tapped where they ended
  // up. Record that single runner's move into a pending hand-off, then open the
  // runner-advance category cascade. The placement is carried in idleRunnerMove and
  // applied at commit — manual placement always wins over the cascade's auto-seed.
  const onIdleDestinationTap = (destination) => {
    if (!armedRunner) return;
    const from = armedRunner;
    setIdleRunnerMove({ from, to: destination });
    setArmedRunner(null);
    // Slide the runner to the tapped destination on the diamond BEFORE the cascade
    // covers it, so the scorer sees the movement (the cascade is a full takeover).
    // The placement update animates via the token's CSS transition; we open the
    // category cascade after the slide has had a moment to play.
    setRunnerPlacements(prev => ({ ...(prev || {}), [from]: destination }));
    setTouchedRunners(prev => {
      const next = new Set(prev); next.add(from); return next;
    });
    // Hold the cascade until the slide finishes. Each base-to-base leg is ~430ms
    // (matching the runner animation), so scale the delay to the distance traveled.
    const bIdx = { '1B': 1, '2B': 2, '3B': 3, 'home': 4 };
    const fromIdx = bIdx[from] || 0;
    const destBase = typeof destination === 'string' && destination.startsWith('out_at_')
      ? destination.replace('out_at_', '') : destination;
    const toIdx = bIdx[destBase] || fromIdx;
    const legs = Math.max(1, Math.abs(toIdx - fromIdx));
    setTimeout(() => {
      // Open the category cascade. The runner stage asks SB/CS/Pickoff/BE/Other.
      setCascadeStage('runner');
    }, legs * 430 + 140);
  };


  // Selection from the top-level PA takeover. Ball in play cascades; everything else fires.
  // All leaves (Walk, HBP, Strikeout types, Other) fire as their category directly.
  // Home run is no longer a top-level leaf — it lives in the BIP cascade under Fly ball
  // and Line drive types.
  // Dropped third strike is the exception — it routes through the diamond so the scorer
  // can mark the batter safe at 1B (rare reach case) or capture the fielder sequence
  // when the catcher recovers and throws to 1B.
  const onPaTopLevelSelect = (key) => {
    if (!key) return;
    if (key === 'ball_in_play') {
      onOpenBipQuick();
      return;
    }
    if (key === 'dropped_third_strike') {
      // v1.5 — Dropped 3rd has two outcomes: batter out (catcher recovers in time)
      // or batter safe at 1B (catcher's throw late, or wild pitch / passed ball).
      // Route through a sub-cascade so the scorer picks the outcome BEFORE the
      // diamond opens; that way the batter token is pre-placed correctly and the
      // diamond focuses on runner movement and fielder sequence.
      setPendingPa({ category: 'dropped_third_strike', extras: {} });
      setCascadeStage('dropped_third_outcome');
      return;
    }
    if (key === 'walk' || key === 'hit_by_pitch' || key === 'intentional_walk') {
      // Animate the forced advance on the idle diamond: batter to first, forced
      // runners up one base. Compute the chain from the current state, seed the
      // animation, commit, then clear so the diamond settles into the new state.
      const sk = cell?.stateKey || 'empty';
      const has1 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('1'));
      const has2 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('2'));
      const has3 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('3'));
      const moves = [{ from: 'home', to: '1B' }];
      if (has1) moves.push({ from: '1B', to: '2B' });
      if (has2) moves.push({ from: '2B', to: has1 ? '3B' : '2B' });
      if (has3) moves.push({ from: '3B', to: (has1 && has2) ? 'home' : '3B' });
      setIdleAnimMovements(moves);
      setCascadeStage(null);
      onPaEndTap(key);
      setTimeout(() => setIdleAnimMovements(null), 1150);
      return;
    }
    setCascadeStage(null);
    onPaEndTap(key);
  };

  // Intercept pitch taps that would END the at-bat. On ball 4 / strike 3, fire a
  // confirm prompt rather than committing, so a mistap doesn't silently walk or K the
  // batter. Strike 3 also offers Dropped Third (which opens the diamond for the string).
  const handleCellPitchTap = (pitchKey) => {
    const b = count?.balls ?? 0;
    const s = count?.strikes ?? 0;
    if (pitchKey === 'ball' && b >= 3) {
      setPitchEndConfirm({ kind: 'walk' });
      return;
    }
    if ((pitchKey === 'called_strike' || pitchKey === 'swing_miss' || pitchKey === 'bunt_strike') && s >= 2) {
      setPitchEndConfirm({ kind: 'strikeout', pitch: pitchKey });
      return;
    }
    if (pitchDetailOn) {
      // Outcome is already known — now collect what was thrown. The outcome tap stays
      // the reflex action; detail is asked afterward so it never delays the read.
      setPitchDetail({ pitchKey, stage: 'type', pitchType: null, paEnd: null });
      return;
    }
    onPitchTap(pitchKey);
  };

  // Commit a pitch (and its PA-end, if this pitch ended the at-bat) with whatever
  // detail was gathered. Either field may be null — both steps are skippable.
  const commitPitchDetail = (pitchType, velo) => {
    const pd = pitchDetail;
    setPitchDetail(null);
    if (!pd) return;
    if (velo != null && onVeloRecorded) onVeloRecorded(velo);
    if (pd.forBip) {
      // Detail collected up front — hold it and open the batted-ball cascade. The
      // play itself is logged later, when the cascade resolves.
      setBipPitchInfo({ pitchType: pitchType || null, velo: velo == null ? null : velo });
      setCascadeStage('bip_type');
      return;
    }
    if (pd.paEnd) {
      // PA-ending pitch: the parent logs the implicit closing pitch, so detail rides
      // along in extras rather than through onPitchTap. paEndExtras carries whatever
      // the cascade already resolved (batted ball, fielder string, runner movements)
      // so a Ball in play keeps its whole play when the detail lands on top.
      onPaEndTap(pd.paEnd, {
        ...(pd.paEndExtras || {}),
        pitchType: pitchType || undefined,
        velo: velo == null ? undefined : velo,
      });
    } else {
      onPitchTap(pd.pitchKey, { pitchType: pitchType || null, velo: velo == null ? null : velo });
    }
  };

  // Every Ball in play exit funnels through here so the detail captured on the tap
  // rides in on the resolved play, whichever branch of the cascade produced it.
  const firePaEndWithDetail = (category, extras) => {
    const info = bipPitchInfo;
    setBipPitchInfo(null);
    onPaEndTap(category, {
      ...extras,
      ...(info?.pitchType ? { pitchType: info.pitchType } : {}),
      ...(info?.velo != null ? { velo: info.velo } : {}),
    });
  };

  const onCascadeTypeSelect = (key) => {
    if (!key) return;
    setCascadeBattedBall(key);
    setCascadeStage('bip_outcome');
  };
  // v1.5 step 7 — Seed runner placements when the diamond opens.
  // Reads cell.stateKey for pre-existing runners and the pending PA's category/hitType
  // for the play type, then auto-advances every runner by the appropriate number of
  // bases. The scorer's job becomes: tap Done if everything looks right, or revise
  // specific runners who didn't follow the default.
  //
  // Default advancement rules:
  //   Single (and FC, Reached on error): everyone +1 base (batter to 1B, runners shift)
  //   Double:                            everyone +2 bases
  //   Triple:                            everyone +3 bases
  //   Home run / In-park HR:             everyone scores (including batter)
  //   Walk / HBP:                        forced runners only (batter to 1B, +1 only when
  //                                       the trailing base would be occupied otherwise)
  //   Out at 1st:                        batter out at 1B, runners stay
  //   Fly Out / Line Out:                batter out (catch — bookkept as out at 1B for
  //                                       schema simplicity), runners stay
  //   Sac Fly:                            batter out, runners +1 base
  //   Sac Bunt:                            batter out at 1B, runners +1 base
  //   Double Play:                        batter out, lead runner forced and out
  //                                       (defaults — scorer must revise for non-standard DPs)
  //   strikeouts / dropped-3rd / other:   batter to 'out_at_home' (the plate), runners stay
  //
  // Returns the placements map: { batter, '1B', '2B', '3B' } where each value is one of:
  //   '1B' | '2B' | '3B' | 'home' | 'out_at_1B' | 'out_at_2B' | 'out_at_3B' | 'out_at_home'
  // Missing keys = that runner doesn't exist (no pre-existing runner at that base).
  // Runners with destinations 'home' or 'out_at_X' disappear from the diamond.
  const seedRunnerPlacements = (paInfo) => {
    const sk = cell?.stateKey || 'empty';
    const has1 = sk === 'loaded' || (sk !== 'empty' && sk.includes('1'));
    const has2 = sk === 'loaded' || (sk !== 'empty' && sk.includes('2'));
    const has3 = sk === 'loaded' || (sk !== 'empty' && sk.includes('3'));

    const category = paInfo?.category;
    const hitType = paInfo?.extras?.hitType;

    const placements = {};

    // Place the batter. Single → 1B, Double → 2B, Triple → 3B, HR → home. Walks
    // and reach-on-error put the batter at 1B. Outs put the batter at the relevant
    // out destination. Strikeouts go to out_at_home (the plate).
    const hitBatterBase = { single: '1B', double: '2B', triple: '3B', home_run: 'home', in_park_hr: 'home' };
    if (category === 'hit') {
      placements.batter = hitBatterBase[hitType] || '1B';
    } else if (category === 'fielders_choice' || category === 'reached_on_error' ||
               category === 'walk' || category === 'hit_by_pitch' ||
               category === 'intentional_walk') {
      placements.batter = '1B';
    } else if (category === 'out_at_1st' || category === 'sac_bunt' ||
               category === 'fly_out' || category === 'line_out' ||
               category === 'pop_fly' ||
               category === 'sac_fly' || category === 'double_play') {
      placements.batter = 'out_at_1B';
    } else if (category === 'strikeout_called' || category === 'strikeout_swinging' || category === 'batter_out') {
      placements.batter = 'out_at_home';
    } else if (category === 'dropped_third_strike') {
      // On a dropped third the batter runs to first either way; if put out, that's
      // at first base (the catcher's throw beats them, or a tag on the way). So the
      // out is at 1B — the batter slides home→1B and is out there, not at the plate.
      placements.batter = paInfo?.extras?.droppedThirdSafe ? '1B' : 'out_at_1B';
    } else {
      // 'other' and any unrecognized category — scorer decides.
      placements.batter = null;
    }

    // Place pre-existing runners.
    // PHILOSOPHY (v1.5): We pre-populate runner destinations ONLY when the play's
    // rules force a specific outcome. For anything that's a judgment call about
    // how aggressively runners advance, we leave runners at their starting bases
    // and let the scorer place them. Real plays diverge from the "everyone +N"
    // assumption frequently enough (runner trips on a double, baserunning error,
    // hold-up on a single, cautious base coach) that auto-advancing causes more
    // problems than it solves.
    //
    // RULES-DRIVEN (auto-populated):
    //   Home run / In-Park HR:     all runners score (forced by rules — every
    //                              baserunner including the batter touches home)
    //   Walk / HBP / IBB:          forced runners only (chain forcing per rules)
    //   Sac Fly:                   runner from 3B scores (definitional — if they
    //                              didn't tag and score, it isn't a sac fly).
    //                              Trail runners stay put.
    //   Force Out (FC w/ explicit forceoutBase):
    //                              the forced runner is out at the forced base
    //                              (rule). 3rd-out-no-run rule applied.
    //                              Trail-runner advancement on the back end is
    //                              left to the scorer (NEW — was assumed before).
    //   Double Play:               batter out at 1B, lead forceable runner out at
    //                              2B. Standard DP default; scorer revises non-
    //                              standard DPs.
    //   Strikeouts / Outs / DP:    runners stay at their bases (already neutral)
    //
    // JUDGMENT-DRIVEN (no advancement assumption — runners stay; scorer places):
    //   Single / Double / Triple:  no auto-advance. Scorer places every runner.
    //   FC (no forceoutBase set):  no auto-advance for runners; only batter at 1B
    //   Reached on error:          no auto-advance. Scorer places every runner.
    //   Sac Bunt:                  no auto-advance. Scorer places (typically all +1
    //                              but can vary).
    //   Other:                     no auto-advance. Scorer places.
    if (category === 'hit' && (hitType === 'home_run' || hitType === 'in_park_hr')) {
      // Home run: all runners score. Forced by rules.
      if (has1) placements['1B'] = 'home';
      if (has2) placements['2B'] = 'home';
      if (has3) placements['3B'] = 'home';
    } else if (category === 'fielders_choice' && paInfo?.extras?.forceoutBase) {
      // FC with explicit force base. The runner forced TO that base is out; every
      // OTHER forced runner advances one base (they were forced and reached safely
      // since the defense made the out elsewhere). A runner reaching HOME (a run)
      // only counts when this isn't the 3rd out — the force-out third-out timing rule
      // means no run scores, so home advances are gated on (outs < 2). For 1st & 3rd,
      // the runner on 3rd isn't force-bound but is advanced home as a sensible default
      // the scorer can slide back if they held.
      const fob = paInfo.extras.forceoutBase;
      const twoOut = (cell?.outs ?? 0) === 2;
      const homeOK = !twoOut; // a run can only score if this force isn't the 3rd out
      if (sk === 'loaded') {
        if (fob === 'home') {
          placements['1B'] = '2B';
          placements['2B'] = '3B';
          placements['3B'] = 'out_at_home';
        } else if (fob === '3B') {
          placements['1B'] = '2B';
          placements['2B'] = 'out_at_3B';
          placements['3B'] = homeOK ? 'home' : '3B';
        } else if (fob === '2B') {
          placements['1B'] = 'out_at_2B';
          placements['2B'] = '3B';
          placements['3B'] = homeOK ? 'home' : '3B';
        }
      } else if (sk === '12') {
        if (fob === '3B') {
          placements['1B'] = '2B';
          placements['2B'] = 'out_at_3B';
        } else if (fob === '2B') {
          placements['1B'] = 'out_at_2B';
          placements['2B'] = '3B';
        }
      } else if (sk === '1') {
        // Runner on first only — forced at 2nd.
        if (fob === '2B') placements['1B'] = 'out_at_2B';
      } else if (sk === '13') {
        // Runner on first forced at 2nd; runner on third advances home as a default
        // (slide back to 3rd if they held). No run on a 3rd-out force.
        if (fob === '2B') {
          placements['1B'] = 'out_at_2B';
          placements['3B'] = homeOK ? 'home' : '3B';
        }
      }
    } else if (category === 'sac_fly') {
      // Sac Fly: definitional — the runner from 3B tags and scores. Trail runners stay.
      if (has1) placements['1B'] = '1B';
      if (has2) placements['2B'] = '2B';
      if (has3) placements['3B'] = 'home';
    } else if (category === 'sac_bunt') {
      // Successful sacrifice bunt: batter out at first, every runner advances one base.
      // 3B → home scores (the out is at first, not a force, so the run counts). These
      // are defaults — the scorer can revise any runner who held or was thrown out.
      if (has1) placements['1B'] = '2B';
      if (has2) placements['2B'] = '3B';
      if (has3) placements['3B'] = 'home';
    } else if (category === 'walk' || category === 'hit_by_pitch' || category === 'intentional_walk') {
      // Forced advancement only — chain forcing per rules.
      if (has1) placements['1B'] = '2B';
      if (has2) placements['2B'] = has1 ? '3B' : '2B';
      if (has3) placements['3B'] = (has1 && has2) ? 'home' : '3B';
    } else if (category === 'hit' && (hitType === 'single' || hitType === 'double' || hitType === 'triple')) {
      // Forced runners advance as many bases as the hitter (default the scorer can
      // adjust — including arming a runner and marking them OUT at the destination).
      // A runner is forced only when there's an unbroken chain of occupied bases
      // behind them down to the batter: 1B always forced on a hit; 2B forced iff 1B
      // occupied; 3B forced iff 1B and 2B occupied (bases loaded). Non-forced runners
      // stay put — the scorer places those, since their advance is a judgment call.
      const n = { single: 1, double: 2, triple: 3 }[hitType];
      const idxToPlacement = (i) => i >= 4 ? 'home' : ({ 1: '1B', 2: '2B', 3: '3B' }[i]);
      const force1 = has1;
      const force2 = has1 && has2;
      const force3 = has1 && has2 && has3;
      if (has1) placements['1B'] = force1 ? idxToPlacement(1 + n) : '1B';
      if (has2) placements['2B'] = force2 ? idxToPlacement(2 + n) : '2B';
      if (has3) placements['3B'] = force3 ? idxToPlacement(3 + n) : '3B';
    } else if (category === 'double_play') {
      const runnerCount = (has1 ? 1 : 0) + (has2 ? 1 : 0) + (has3 ? 1 : 0);
      if (runnerCount === 1) {
        // Exactly ONE runner on base — a double play is unambiguous: the batter and
        // that runner are the two outs. Auto-out both (the runner doubled off / forced
        // at their base) so the scorer doesn't have to move the runner to the bag and
        // mark them out. The scorer just inputs the fielder string and taps Done.
        if (has1) placements['1B'] = 'out_at_1B';
        else if (has2) placements['2B'] = 'out_at_2B';
        else if (has3) placements['3B'] = 'out_at_3B';
        // batter already seeded out_at_1B above.
      } else {
        // MULTIPLE runners — too many combinations to assume (force at home, force at
        // 2B, which runner doubled off, etc.). Leave every runner at its base so the
        // scorer tags each one explicitly. Prevents wrong out-attribution.
        if (has1) placements['1B'] = '1B';
        if (has2) placements['2B'] = '2B';
        if (has3) placements['3B'] = '3B';
      }
    } else if (category === 'out_at_1st') {
      // Ground out, batter retired at first (not a double play). The defense conceded
      // the runners' advance to take the out at first, so forced runners advance one
      // base (runner on first → second, etc.). Same force chain as a walk; non-forced
      // runners stay. A run only counts if this isn't the 3rd out.
      const homeOK = (cell?.outs ?? 0) < 2;
      if (has1) placements['1B'] = '2B';
      if (has2) placements['2B'] = has1 ? '3B' : '2B';
      if (has3) placements['3B'] = (has1 && has2) ? (homeOK ? 'home' : '3B') : '3B';
    } else {
      // ALL OTHER CASES — hits handled above, FC without forceoutBase, reached on
      // error, sac bunt, strikeouts, fly outs, line outs, dropped 3rd, other. Runners
      // stay at their starting bases. Scorer places any movement.
      if (has1) placements['1B'] = '1B';
      if (has2) placements['2B'] = '2B';
      if (has3) placements['3B'] = '3B';
    }

    return placements;
  };

  // v1.5 — Runner-advance variant of seedRunnerPlacements. Used when the diamond
  // opens for SB/CS/Pickoff/BE/Other (between-pitch events, no batter at the plate).
  // Pre-populates runner destinations via the lead-runner default rule. Scorer
  // revises if the actual play differed (multi-runner steals, off-lead pickoff, etc.).
  // Returns a placements map keyed by '1B' | '2B' | '3B' only — no batter entry.
  const seedRunnerPlacementsForRunnerAdvance = (category) => {
    const sk = cell?.stateKey || 'empty';
    const has1 = sk === 'loaded' || (sk !== 'empty' && sk.includes('1'));
    const has2 = sk === 'loaded' || (sk !== 'empty' && sk.includes('2'));
    const has3 = sk === 'loaded' || (sk !== 'empty' && sk.includes('3'));
    const lead = has3 ? '3B' : has2 ? '2B' : has1 ? '1B' : null;
    const runnerCount = (has1 ? 1 : 0) + (has2 ? 1 : 0) + (has3 ? 1 : 0);

    const placements = {};
    // Default: every runner stays put. Lead runner is then revised per category
    // (for SB/BE only — pickoff and CS handle ambiguity case-by-case below).
    if (has1) placements['1B'] = '1B';
    if (has2) placements['2B'] = '2B';
    if (has3) placements['3B'] = '3B';

    if (!lead) return placements; // no runners → nothing to seed

    // v1.5 — Pre-population philosophy:
    //   Stolen Base / Battery Error: pre-populate the lead runner ONLY when
    //     there's a single runner on base. The destination is a base (e.g.
    //     1B → 2B), so the token stays visible and the scorer can revise.
    //   Caught Stealing / Pickoff: NO pre-population — even single-runner.
    //     These destinations are terminal (out_at_X), which hides the runner
    //     token in the diamond rendering, leaving the scorer with no way to
    //     revise if the auto-pop is wrong. Better to leave the runner visible
    //     at their base and let the scorer mark the out manually.
    //   Other: no default, scorer manually places.
    if (runnerCount === 1) {
      if (category === 'stolen_base' || category === 'battery_error' || category === 'defensive_indifference') {
        const advanceMap = { '1B': '2B', '2B': '3B', '3B': 'home' };
        placements[lead] = advanceMap[lead];
      }
    }

    return placements;
  };
  // BIP cascade level 3: outcome chosen. v1.5 — outcome menus are type-specific and
  // each option carries metadata. Most leaf outcomes route into the fielder picker
  // (step 6) rather than firing immediately — the picker captures the fielder
  // sequence, then commits the PA-end when the scorer taps Done.
  // Exceptions:
  //   - Batter out → opens the bip_batter_out sub-cascade (still cascades deeper)
  //   - Foul ball  → logs a foul pitch and exits cascade (not a PA-end)
  const onCascadeOutcomeSelect = (key) => {
    if (!key) return;
    const battedBall = cascadeBattedBall;
    // Find the option metadata in the type-specific menu.
    const outcomes = bipOutcomesByType[battedBall] || bipOutcomesByType.ground_ball;
    const opt = outcomes.find(o => o.key === key);

    if (!opt) {
      // Defensive — shouldn't happen, but if it does fall back to a raw out.
      setCascadeStage(null);
      setCascadeBattedBall(null);
      firePaEndWithDetail('out', { battedBall });
      return;
    }

    // Batter out → opens the type-specific sub-cascade. Don't reset battedBall yet —
    // the sub-cascade needs it to resolve its own menu and stamp the pa_end event.
    if (opt.cascade && key === 'batter_out_cascade') {
      setCascadeStage('bip_batter_out');
      return;
    }

    // Foul ball inside BIP — not a PA-end. The scorer entered the cascade but the
    // ball went foul. Behave exactly like tapping the Foul pitch button: log a
    // foul pitch event, exit the cascade, return to the cell. At-bat continues.
    if (opt.foulPitch) {
      setCascadeStage(null);
      setCascadeBattedBall(null);
      const info = bipPitchInfo;
      setBipPitchInfo(null);
      onPitchTap('foul', info ? { pitchType: info.pitchType, velo: info.velo } : null);
      return;
    }

    // No-fielders short-circuit — Home run inside BIP fires immediately, no picker.
    // Ball left the park; no fielder touched it before it cleared the fence.
    if (opt.noFielders) {
      setCascadeStage(null);
      setCascadeBattedBall(null);
      const extras = { battedBall };
      let category;
      if (opt.hitType) {
        category = 'hit';
        extras.hitType = opt.hitType;
      } else {
        category = key;
      }
      firePaEndWithDetail(category, extras);
      return;
    }

    // Stash the pending PA and open the fielder picker. The PA fires only when the
    // scorer commits via the Done button in the picker.
    const extras = { battedBall };
    let category;
    if (opt.hitType) {
      category = 'hit';
      extras.hitType = opt.hitType;
    } else {
      category = key; // reached_on_error, fielders_choice
    }
    const paInfo = { category, extras };

    // v1.5 — Fielder's choice with bases loaded or 1st & 2nd opens a forceout-location
    // sub-cascade first. The location determines which runner got out and (for bases
    // loaded) whether the run from 3B scored. Other FC states use single-equivalent
    // pre-population by default.
    const sk = cell?.stateKey;
    if (category === 'fielders_choice' && (sk === 'loaded' || sk === '12')) {
      setPendingPa(paInfo);
      setFielderSequence([]);
      setCascadeStage('bip_fc_forceout');
      return;
    }
    // FC with a runner on first as the only forced runner (1st alone, or 1st & 3rd):
    // the force is unambiguous (runner from 1st out at 2nd), so pre-seed that out —
    // the scorer doesn't have to move the runner to the bag. Still open the fielder
    // picker (even when it's the 3rd out) so the scorer can log the fielder string;
    // tapping Done commits and ends the inning via the normal 3rd-out detection.
    if (category === 'fielders_choice' && (sk === '1' || sk === '13')) {
      paInfo.extras = { ...extras, forceoutBase: '2B' };
      setPendingPa(paInfo);
      setFielderSequence([]);
      setRunnerPlacements(seedRunnerPlacements(paInfo));
      setPlacementHistory([]);
      setArmedRunner(null);
      setTouchedRunners(new Set());
      setCascadeStage('bip_fielders');
      return;
    }

    setPendingPa(paInfo);
    setFielderSequence([]);
    setRunnerPlacements(seedRunnerPlacements(paInfo));
    setPlacementHistory([]);
    setArmedRunner(null);
    setTouchedRunners(new Set());
    setCascadeStage('bip_fielders');
  };
  // BIP cascade level 4 (Batter out sub-cascade): out type chosen. Routes into the
  // fielder picker just like the level-3 leaves; the PA fires when Done is tapped.
  const onCascadeBatterOutSelect = (key) => {
    if (!key) return;
    const battedBall = cascadeBattedBall;
    const paInfo = { category: key, extras: { battedBall } };
    setPendingPa(paInfo);
    setFielderSequence([]);
    setRunnerPlacements(seedRunnerPlacements(paInfo));
    setPlacementHistory([]);
    setArmedRunner(null);
    setTouchedRunners(new Set());
    setCascadeStage('bip_fielders');
  };

  // v1.5 — FC forceout-location sub-cascade. Triggered for FC with bases loaded
  // or 1st & 2nd. The location determines who got out and (for bases loaded) whether
  // the run from 3B scored. Forwards into the standard fielder picker.
  const onCascadeFcForceoutSelect = (forceoutBase) => {
    if (!forceoutBase || !pendingPa) return;
    const extras = { ...pendingPa.extras, forceoutBase };
    const paInfo = { category: pendingPa.category, extras };
    setPendingPa(paInfo);
    setRunnerPlacements(seedRunnerPlacements(paInfo));
    setPlacementHistory([]);
    setArmedRunner(null);
    setTouchedRunners(new Set());
    setCascadeStage('bip_fielders');
  };

  // v1.5 — Dropped 3rd strike outcome sub-cascade. Outcome is 'out' or 'safe'.
  // Sets the batter placement accordingly and opens the diamond. The catcher's
  // recovery / throw to 1B is still captured via the fielder picker on the
  // diamond — both outcomes can have a fielder sequence.
  const onCascadeDroppedThirdOutcomeSelect = (outcome) => {
    if (!outcome || !pendingPa) return;
    const extras = { ...pendingPa.extras, droppedThirdSafe: outcome === 'safe' };
    const paInfo = { category: pendingPa.category, extras };
    setPendingPa(paInfo);
    setFielderSequence([]);
    setRunnerPlacements(seedRunnerPlacements(paInfo));
    setPlacementHistory([]);
    setArmedRunner(null);
    setTouchedRunners(new Set());
    setCascadeStage('bip_fielders');
  };
  // Fielder picker handlers.
  // Tap a fielder → toggle. If already in the sequence, remove its most recent
  // occurrence (lets the scorer correct a mis-tap without leaving the screen).
  // Otherwise append to the sequence.
  // The "remove most recent" rule matches the "undo my last action" mental model;
  // sequences with the same fielder touching twice (rare — rundowns) are still
  // achievable by tapping a different fielder in between.
  // Tap a fielder position to append it to the fielderSequence. The same
  // fielder can legitimately appear multiple times in a play (rundowns, long
  // relays — e.g., C-SS-1B-C). The only ambiguous case is a *consecutive*
  // double-tap of the same fielder: a player can't throw to themselves, so
  // a back-to-back tap is almost certainly a mis-tap. Treat that as undo
  // (remove the trailing occurrence). Anything non-consecutive appends.
  const onFielderTap = (positionCode) => {
    setFielderSequence(prev => {
      // If the most recent fielder is the same one being tapped, treat as undo
      // of that last tap. Cheap way to back out an accidental double-tap.
      if (prev.length > 0 && prev[prev.length - 1] === positionCode) {
        return prev.slice(0, -1);
      }
      // Otherwise, append. Allows the same fielder to appear later in the
      // sequence (rundown, relay throws back).
      return [...prev, positionCode];
    });
  };

  // v1.5 step 7 — Tap a runner token (source key: 'batter' | '1B' | '2B' | '3B').
  // Toggle: if it's the currently armed runner, disarm. Otherwise arm it.
  // Either way, the runner is now "touched" — they stop pulsing. Arming is the
  // scorer's "I'm dealing with this runner" gesture, so we treat it as
  // confirmation that the scorer has at least *seen* this runner.
  const onRunnerTokenTap = (sourceKey) => {
    setArmedRunner(prev => prev === sourceKey ? null : sourceKey);
    setTouchedRunners(prev => {
      if (prev.has(sourceKey)) return prev;
      const next = new Set(prev);
      next.add(sourceKey);
      return next;
    });
  };

  // Tap a destination (base, scoring at home, or per-base out) while a runner is
  // armed → place them there. No-op when no runner is armed. After placement the
  // armed runner is disarmed.
  // Pushes to placementHistory so the scorer can undo the most recent placement.
  const onDestinationTap = (destination) => {
    if (!armedRunner) return;
    const prevDestination = runnerPlacements[armedRunner];
    setPlacementHistory(prev => [...prev, { source: armedRunner, prevDestination }]);
    setRunnerPlacements(prev => ({ ...prev, [armedRunner]: destination }));
    setArmedRunner(null);
  };

  // Undo the most recent placement change. Pops the history stack and restores
  // the previous destination for that runner. Disarms any currently-armed runner
  // (visual reset). Disabled (no-op) when history is empty.
  const onPlacementUndo = () => {
    if (placementHistory.length === 0) return;
    const last = placementHistory[placementHistory.length - 1];
    setPlacementHistory(prev => prev.slice(0, -1));
    setRunnerPlacements(prev => ({ ...prev, [last.source]: last.prevDestination }));
    setArmedRunner(null);
  };
  // Done → fire the pending PA with fielderSequence AND runnerMovements attached.
  // runnerMovements is derived from the diamond's runnerPlacements state — each entry
  // maps {from: source-base, to: destination}. The pa_end event carries the full
  // play outcome including who scored / who got out, so getScores() can count runs
  // directly from the pa_end without needing separate score_change events.
  const onFielderDone = () => {
    // v1.5 — Two flows from the diamond:
    //   1. pendingPa set            → pa_end flow (existing): batter included in runners
    //   2. pendingRunnerAdvance set → between_pitch flow (new): no batter, just runners
    // Exactly one of these should be set at any time.

    if (pendingRunnerAdvance) {
      // Between-pitch flow. Build runnerMovements from runnerPlacements (no batter key).
      const { category, pitch } = pendingRunnerAdvance;
      const runnerMovements = [];
      if (runnerPlacements) {
        for (const source of ['1B', '2B', '3B']) {
          if (!(source in runnerPlacements)) continue;
          const dest = runnerPlacements[source];
          if (dest === null || dest === undefined) continue;
          runnerMovements.push({ from: source, to: dest });
        }
      }
      const finalSequence = fielderSequence;

      // v1.5 — Smart PA-end detection. If the picked pitch would end the at-bat
      // (4th ball, 3rd strike), fire the pa_end instead of (just) a between_pitch.
      // The runner action is folded into the pa_end's runnerMovements, with the
      // original runner-action type preserved via extras.runnerActionType for
      // downstream analytics ("how often do runners steal on a strikeout pitch?").
      //
      // Mapping:
      //   pitch=ball         AND balls===3   → walk (no separate steal event)
      //   pitch=called_strike AND strikes===2 → strikeout_called
      //   pitch=swing_miss   AND strikes===2 → strikeout_swinging
      // Foul with 2 strikes does NOT end the PA (rules); stays as between_pitch.
      let paEndCategory = null;
      const balls = count?.balls ?? 0;
      const strikes = count?.strikes ?? 0;
      if (pitch === 'ball' && balls === 3) {
        paEndCategory = 'walk';
      } else if (pitch === 'intentional_ball' && balls === 3) {
        paEndCategory = 'intentional_walk';
      } else if (pitch === 'called_strike' && strikes === 2) {
        paEndCategory = 'strikeout_called';
      } else if ((pitch === 'swing_miss' || pitch === 'bunt_strike') && strikes === 2) {
        paEndCategory = 'strikeout_swinging';
      }

      setCascadeStage(null);
      setCascadeBattedBall(null);
      setPendingRunnerAdvance(null);
      setFielderSequence([]);
      setRunnerPlacements(null);
      setArmedRunner(null);
      setTouchedRunners(new Set());
      setPlacementHistory([]);

      if (paEndCategory) {
        // Fold the runner action into the pa_end. Add the batter's resulting
        // destination to runnerMovements based on the PA outcome:
        //   walk          → batter to 1B (force chain handled by existing walk logic
        //                    is already in the runner advance result? Actually no —
        //                    runner advance only moves the stealing runner; the walk
        //                    might separately force other runners. But the scorer
        //                    already saw the diamond and placed runners as they
        //                    actually ended up — including any forced movement. So
        //                    we just trust the diamond's placements and add the batter.)
        //   strikeout_*   → batter out at home
        const mergedRunnerMovements = [...runnerMovements];
        if (paEndCategory === 'walk') {
          mergedRunnerMovements.push({ from: 'home', to: '1B' });
        } else {
          mergedRunnerMovements.push({ from: 'home', to: 'out_at_home' });
        }
        onPaEndTap(paEndCategory, {
          runnerMovements: mergedRunnerMovements,
          fielderSequence: finalSequence,
          runnerActionType: category, // 'stolen_base' | 'caught_stealing' | 'battery_error'
        });
        return;
      }

      onRunnerAdvanceTap(category, {
        runnerMovements,
        fielderSequence: finalSequence,
        pitch, // 'ball' | 'called_strike' | 'swing_miss' | 'foul' | undefined
      });
      return;
    }

    if (!pendingPa) return;
    const { category, extras } = pendingPa;

    // Build runnerMovements from runnerPlacements. Each entry includes the source
    // (where the runner started — 'home' for batter, '1B'/'2B'/'3B' for runners)
    // and destination (one of '1B' | '2B' | '3B' | 'home' | 'out_at_1B' | 'out_at_2B'
    // | 'out_at_3B' | 'out_at_home'). Null placements are skipped (shouldn't happen
    // after pre-population, but defensive).
    const runnerMovements = [];
    if (runnerPlacements) {
      for (const source of ['batter', '1B', '2B', '3B']) {
        if (!(source in runnerPlacements)) continue;
        const dest = runnerPlacements[source];
        if (dest === null || dest === undefined) continue;
        const from = source === 'batter' ? 'home' : source;
        runnerMovements.push({ from, to: dest });
      }
    }

    setCascadeStage(null);
    setCascadeBattedBall(null);
    setPendingPa(null);
    const finalSequence = fielderSequence;
    setFielderSequence([]);
    setRunnerPlacements(null);
    setArmedRunner(null);
    setTouchedRunners(new Set());
    setPlacementHistory([]);
    firePaEndWithDetail(category, {
      ...extras,
      fielderSequence: finalSequence,
      runnerMovements,
    });
  };
  // Selection from the Runner advance takeover.
  // v1.5 — Routes through pitch sub-cascade (for SB/CS/BE) or directly to the
  // diamond (for Pickoff/Other), instead of firing immediately.
  const onRunnerSelect = (key) => {
    if (!key) return;
    if (key === 'stolen_base' || key === 'caught_stealing' || key === 'battery_error' || key === 'defensive_indifference') {
      // Pitch sub-cascade first — these categories may coincide with a pitch.
      // DI is special: its sub-cascade includes a "No Pitch" option for the case
      // where the defense lets the runner go without a pitch being thrown.
      setPendingRunnerAdvance({ category: key });
      setCascadeStage('runner_pitch');
      return;
    }
    if (key === 'pickoff') {
      // Pickoffs go awry often (errant throw, runner advances on the error), so the
      // outcome isn't always an out. In the idle flow the scorer already tapped a
      // destination (out bin / a base / Back), so the outcome is set — commit now.
      // From the takeover menu, ask the outcome: Out / Safe at bag / Advanced on error.
      if (idleRunnerMove) {
        commitIdleRunnerAdvance(key, undefined);
        return;
      }
      setPendingRunnerAdvance({ category: key });
      setCascadeStage('pickoff_outcome');
      return;
    }
    if (key === 'other') {
      // Other is ambiguous — skip the pitch sub-cascade, go straight to the diamond.
      if (idleRunnerMove) {
        commitIdleRunnerAdvance(key, undefined);
        return;
      }
      setPendingRunnerAdvance({ category: key });
      setRunnerPlacements(seedRunnerPlacementsForRunnerAdvance(key));
      setFielderSequence([]);
      setPlacementHistory([]);
      setArmedRunner(null);
      setCascadeStage('bip_fielders');
      return;
    }
    // Defensive fallback — shouldn't happen.
    setCascadeStage(null);
    onRunnerAdvanceTap(key);
  };

  // Pickoff outcome sub-cascade. After "Pickoff" is chosen from the takeover menu,
  // the scorer picks what actually happened, since pickoffs frequently go awry:
  //   Out       — runner picked off. Single runner → pre-seed the out so the scorer
  //               just logs the throw (1-3 pitcher→1B, 2-4 catcher→2B); multi-runner
  //               → scorer marks which on the diamond.
  //   Safe      — throw made, runner dove back. No out, no advance — commit straight
  //               away with every runner held; no diamond needed.
  //   Advanced  — runner advanced on a throwing error. Open the diamond with runners
  //               at their bases so the scorer slides the runner up and logs the error.
  const onCascadePickoffOutcomeSelect = (outcome) => {
    if (!outcome) return;
    const sk = cell?.stateKey || 'empty';
    const has1 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('1'));
    const has2 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('2'));
    const has3 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('3'));
    const occupied = [];
    if (has1) occupied.push('1B');
    if (has2) occupied.push('2B');
    if (has3) occupied.push('3B');

    if (outcome === 'safe') {
      // No change — every runner stays. Log the attempt and exit.
      setCascadeStage(null);
      setPendingRunnerAdvance(null);
      const runnerMovements = occupied.map(b => ({ from: b, to: b }));
      onRunnerAdvanceTap('pickoff', { runnerMovements, pickoffOutcome: 'safe' });
      return;
    }

    // Out or Advanced → open the diamond to log the fielder sequence (pitcher or
    // catcher throw) and, for Advanced, place the runner's new base.
    const placements = {};
    occupied.forEach(b => { placements[b] = b; });
    if (outcome === 'out' && occupied.length === 1) {
      // Unambiguous single runner — pre-seed the out at their base.
      placements[occupied[0]] = `out_at_${occupied[0]}`;
    }
    setPendingRunnerAdvance({ category: 'pickoff', pickoffOutcome: outcome });
    setRunnerPlacements(placements);
    setFielderSequence([]);
    setPlacementHistory([]);
    setArmedRunner(null);
    setTouchedRunners(new Set());
    setCascadeStage('bip_fielders');
  };

  // v1.5 — Runner-advance pitch sub-cascade. Scorer picked SB/CS/BE; now picks the
  // pitch that coincided. Stores the pitch and routes to the diamond.
  const onCascadeRunnerPitchSelect = (pitchType) => {
    if (!pitchType || !pendingRunnerAdvance) return;
    // DI "No Pitch" path: the defense let the runner go without a pitch thrown.
    // No pitch event logged, just the runner advance — same shape as pickoff.
    const isNoPitch = pitchType === 'no_pitch';
    // 2B inverted flow: the scorer already placed the runner on the idle diamond,
    // so once they've chosen the category (in pendingRunnerAdvance) and the pitch
    // here, the play is fully specified — commit directly, no bip_fielders step.
    if (idleRunnerMove) {
      commitIdleRunnerAdvance(pendingRunnerAdvance.category, isNoPitch ? undefined : pitchType);
      return;
    }
    setPendingRunnerAdvance({ ...pendingRunnerAdvance, pitch: isNoPitch ? undefined : pitchType });
    setRunnerPlacements(seedRunnerPlacementsForRunnerAdvance(pendingRunnerAdvance.category));
    setFielderSequence([]);
    setPlacementHistory([]);
    setArmedRunner(null);
    setCascadeStage('bip_fielders');
  };

  // 2B — Commit the inverted runner-advance flow. The scorer placed exactly one
  // runner on the idle diamond (idleRunnerMove = { from, to }), then chose the
  // category and (for SB/CS/BE) the pitch. We reuse the same commit endpoints as
  // the cascade flow: a pitch that ends the PA folds into a pa_end; otherwise it's
  // a runner-advance (between_pitch). No fielder sequence in the inverted quick
  // flow (the scorer can still use the full cascade entry if they need to log one).
  // Finalize an idle runner-advance commit. Resets diamond state and fires the
  // event (pa_end if the pitch closed the at-bat, else a runner-advance).
  const finalizeIdleCommit = (runnerMovements, category, pitch, paEndCategory) => {
    setCascadeStage(null);
    setCascadeBattedBall(null);
    setPendingRunnerAdvance(null);
    setFielderSequence([]);
    setRunnerPlacements(null);
    setArmedRunner(null);
    setTouchedRunners(new Set());
    setPlacementHistory([]);
    setIdleRunnerMove(null);
    setTrailRunnerPrompt(null);

    if (paEndCategory) {
      const merged = [...runnerMovements];
      if (paEndCategory === 'walk') {
        merged.push({ from: 'home', to: '1B' });
      } else {
        merged.push({ from: 'home', to: 'out_at_home' });
      }
      onPaEndTap(paEndCategory, {
        runnerMovements: merged,
        runnerActionType: category,
      });
      return;
    }
    onRunnerAdvanceTap(category, { runnerMovements, pitch });
  };

  const commitIdleRunnerAdvance = (category, pitch) => {
    const move = idleRunnerMove;
    if (!move) { setCascadeStage(null); return; }

    // Stay-put guard: every runner currently on base who the scorer DID NOT move
    // must be explicitly carried as a no-op move, or the downstream state computer
    // will treat them as "not on base" and they'll disappear. Derive current base
    // occupancy from the cell's stateKey (the same source the idle diamond reads).
    const sk = cell?.stateKey || 'empty';
    const has1 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('1'));
    const has2 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('2'));
    const has3 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('3'));
    const occupied = [];
    if (has1) occupied.push('1B');
    if (has2) occupied.push('2B');
    if (has3) occupied.push('3B');

    const runnerMovements = [{ from: move.from, to: move.to }];
    // Add a stay-put for every occupied base that wasn't the one the scorer moved.
    // No assumptions — but for steals/battery errors we PROMPT (below) about trailing
    // runners, since double steals and runners advancing on a wild pitch are common.
    occupied.forEach(base => {
      if (base !== move.from) {
        runnerMovements.push({ from: base, to: base });
      }
    });

    // Smart PA-end detection — mirror the onFielderDone logic so a steal on ball 4
    // or strike 3 closes the at-bat correctly.
    let paEndCategory = null;
    const balls = count?.balls ?? 0;
    const strikes = count?.strikes ?? 0;
    if (pitch === 'ball' && balls === 3) {
      paEndCategory = 'walk';
    } else if (pitch === 'intentional_ball' && balls === 3) {
      paEndCategory = 'intentional_walk';
    } else if (pitch === 'called_strike' && strikes === 2) {
      paEndCategory = 'strikeout_called';
    } else if ((pitch === 'swing_miss' || pitch === 'bunt_strike') && strikes === 2) {
      paEndCategory = 'strikeout_swinging';
    }

    // Trailing-runner prompt: on a stolen base or battery error with another runner
    // BEHIND the one that moved, ask whether the trailing runner(s) advanced too.
    // We never assume — the scorer confirms. If yes, each trailing runner advances
    // one base, folded into the SAME play. (Forward bases ahead of the mover aren't
    // "trailing" and are left to the scorer.)
    const baseNum = { '1B': 1, '2B': 2, '3B': 3 };
    const moverNum = baseNum[move.from] || 0;
    const trailingBases = occupied.filter(b => b !== move.from && baseNum[b] < moverNum);
    if ((category === 'stolen_base' || category === 'battery_error') && trailingBases.length > 0) {
      setTrailRunnerPrompt({ runnerMovements, category, pitch, paEndCategory, trailingBases });
      return; // wait for the scorer's answer; finalize fires from the prompt
    }

    finalizeIdleCommit(runnerMovements, category, pitch, paEndCategory);
  };

  // Back navigation from any takeover screen:
  //   bip_fielders   → previous stage (bip_outcome or bip_batter_out, depending on entry)
  //                    Discards pendingPa and fielderSequence; scorer can re-pick the outcome.
  //   bip_batter_out → bip_outcome (lets the scorer re-pick outcome — maybe it wasn't an out)
  //   bip_outcome    → bip_type    (lets the scorer re-pick batted-ball type)
  //   bip_type       → pa_top      (lets the scorer pick a different top-level result)
  //   pa_top         → null        (exits takeover, returns to normal cell view)
  //   runner         → null        (exits takeover)
  const onCascadeBack = () => {
    if (cascadeStage === 'bip_fielders') {
      // Determine which stage the scorer came from. If the pending PA's category
      // matches one of the Batter out sub-cascade keys, return to bip_batter_out;
      // otherwise return to bip_outcome.
      // If the pending PA is an FC with a forceoutBase set, the scorer came through
      // the FC forceout sub-cascade — return there instead of the outcome menu.
      // Dropped 3rd strike skipped the BIP cascade entirely — return to pa_top.
      // If pendingRunnerAdvance is set, the scorer came from the runner-advance flow —
      // return to runner_pitch (for SB/CS/BE) or the runner menu (for Pickoff/Other).
      const batterOutCategoryKeys = new Set([
        'out_at_1st', 'fly_out', 'sac_fly', 'line_out', 'sac_bunt', 'double_play',
      ]);
      const fromBatterOut = pendingPa && batterOutCategoryKeys.has(pendingPa.category);
      const fromFcForceout = pendingPa && pendingPa.category === 'fielders_choice' &&
                             pendingPa.extras?.forceoutBase;
      const fromDroppedThird = pendingPa && pendingPa.category === 'dropped_third_strike';
      const fromRunnerAdvance = !!pendingRunnerAdvance;
      setFielderSequence([]);
      setRunnerPlacements(null);
      setArmedRunner(null);
      setTouchedRunners(new Set());
      setPlacementHistory([]);
      if (fromRunnerAdvance) {
        const cat = pendingRunnerAdvance.category;
        if (cat === 'stolen_base' || cat === 'caught_stealing' || cat === 'battery_error') {
          // Strip the pitch selection so re-entering the pitch sub-cascade is clean.
          setPendingRunnerAdvance({ category: cat });
          setCascadeStage('runner_pitch');
        } else if (cat === 'pickoff') {
          // Return to the pickoff outcome menu (strip the chosen outcome).
          setPendingRunnerAdvance({ category: cat });
          setCascadeStage('pickoff_outcome');
        } else {
          setPendingRunnerAdvance(null);
          setCascadeStage('runner');
        }
      } else if (fromFcForceout) {
        // Strip forceoutBase from pendingPa so re-entering the sub-cascade is clean.
        setPendingPa({
          category: pendingPa.category,
          extras: { ...pendingPa.extras, forceoutBase: undefined },
        });
        setCascadeStage('bip_fc_forceout');
      } else if (fromDroppedThird) {
        // Return to the outcome sub-cascade. Strip the droppedThirdSafe flag
        // so re-entering is clean.
        setPendingPa({
          category: pendingPa.category,
          extras: { ...pendingPa.extras, droppedThirdSafe: undefined },
        });
        setCascadeStage('dropped_third_outcome');
      } else {
        setPendingPa(null);
        setCascadeStage(fromBatterOut ? 'bip_batter_out' : 'bip_outcome');
      }
      // Keep battedBall — the previous menu needs it to render.
    } else if (cascadeStage === 'runner_pitch') {
      // Back from pitch sub-cascade returns to the runner-advance menu.
      setPendingRunnerAdvance(null);
      setCascadeStage('runner');
    } else if (cascadeStage === 'dropped_third_outcome') {
      // Back from dropped 3rd outcome sub-cascade returns to the PA menu.
      setPendingPa(null);
      setCascadeStage('pa_top');
    } else if (cascadeStage === 'pickoff_outcome') {
      // Back from the pickoff outcome sub-cascade returns to the runner menu.
      setPendingRunnerAdvance(null);
      setCascadeStage('runner');
    } else if (cascadeStage === 'bip_fc_forceout') {
      // Back from FC forceout sub-cascade returns to the outcome menu.
      setPendingPa(null);
      setCascadeStage('bip_outcome');
    } else if (cascadeStage === 'bip_batter_out') {
      setCascadeStage('bip_outcome');
      // Keep battedBall so the outcome menu re-renders correctly.
    } else if (cascadeStage === 'bip_outcome') {
      setCascadeStage('bip_type');
      setCascadeBattedBall(null);
    } else if (cascadeStage === 'bip_type') {
      setCascadeStage('pa_top');
      setCascadeBattedBall(null);
      setBipPitchInfo(null);
    } else {
      // pa_top or runner
      setCascadeStage(null);
      setCascadeBattedBall(null);
      setBipPitchInfo(null);
      // 2B: if the scorer backed all the way out of the inverted runner-advance
      // flow, discard the staged idle placement so it can't leak into a later play.
      // The idle diamond re-seeds from stateKey when idle mode resumes.
      setIdleRunnerMove(null);
    }
  };

  return (
    <div>
      {/* Header removed — count, outs, batter info, and state now live in the
          combined scoreboard strip panel above. The diamond is purely the field. */}

      {/* When a takeover stage is active, the entire body below is replaced
          by a full-takeover screen with large tap-targets. GameChanger pattern. */}
      {cascadeStage !== null ? (
        <CascadeScreen
          stage={cascadeStage}
          battedBall={cascadeBattedBall}
          paOptions={paOptions}
          battedBallOptions={battedBallOptions}
          bipOutcomeOptions={getCurrentBipOutcomes()}
          batterOutOptions={getCurrentBatterOutOptions()}
          runnerOptions={runnerOptions}
          fielderOptions={fielderOptions}
          fielderSequence={fielderSequence}
          pendingPa={pendingPa}
          runnerPlacements={runnerPlacements}
          touchedRunners={touchedRunners}
          armedRunner={armedRunner}
          placementHistory={placementHistory}
          cellStateKey={cell?.stateKey}
          pendingRunnerAdvance={pendingRunnerAdvance}
          baseJerseys={baseJerseys}
          batterJersey={jerseyNumber}
          onPaTopSelect={onPaTopLevelSelect}
          onTypeSelect={onCascadeTypeSelect}
          onOutcomeSelect={onCascadeOutcomeSelect}
          onBatterOutSelect={onCascadeBatterOutSelect}
          onFcForceoutSelect={onCascadeFcForceoutSelect}
          onDroppedThirdOutcomeSelect={onCascadeDroppedThirdOutcomeSelect}
          onPickoffOutcomeSelect={onCascadePickoffOutcomeSelect}
          onRunnerPitchSelect={onCascadeRunnerPitchSelect}
          onFielderTap={onFielderTap}
          onFielderDone={onFielderDone}
          onRunnerTokenTap={onRunnerTokenTap}
          onDestinationTap={onDestinationTap}
          onPlacementUndo={onPlacementUndo}
          onRunnerSelect={onRunnerSelect}
          onBack={onCascadeBack}
        />
      ) : (
        <>
      {/* 2B — Always-visible idle diamond. Shows the current base runners (from the
          cell's stateKey). Tap a runner to arm it; valid SAFE/OUT destinations light
          up; tapping one stages the move and opens the runner-advance category
          cascade (placement-first inversion). When a runner is armed, the pitch row
          dims — the scorer finishes the runner action first, then records the pitch. */}
      <FieldDiamond
        fielderOptions={fielderOptions}
        fielderSequence={[]}
        onFielderTap={() => {}}
        runnerPlacements={runnerPlacements}
        armedRunner={armedRunner}
        touchedRunners={touchedRunners}
        placementHistory={[]}
        onPlacementUndo={() => {}}
        onRunnerTokenTap={onRunnerTokenTap}
        onDestinationTap={onIdleDestinationTap}
        allowOwnBaseOut={true}
        onMoundTap={() => { setMorePitchOpen(true); setMoreSubStage(null); }}
        onUndo={onUndo}
        canUndo={canUndo}
        batterLabel={jerseyNumber || (batterSlot ? String(batterSlot.slot) : '')}
        onBatterTap={openJerseyEdit}
        batterEditing={jerseyEditing}
        batterDraft={jerseyDraft}
        onBatterDraftChange={(v) => setJerseyDraft(v)}
        onBatterEditCommit={commitJerseyEdit}
        onBatterEditCancel={cancelJerseyEdit}
        pitcherLabel={pitcherNumber ? `#${pitcherNumber}` : ''}
        onPitcherTap={openPitcherEdit}
        pitcherEditing={pitcherEditing}
        pitcherDraft={pitcherDraft}
        onPitcherDraftChange={(v) => setPitcherDraft(v)}
        onPitcherEditCommit={commitPitcherEdit}
        onPitcherEditCancel={cancelPitcherEdit}
        baseJerseys={baseJerseys}
        batterJersey={jerseyNumber}
      />

      {/* "More pitch" cascade — catch-all popover for low-frequency events.
          Opened by tapping the MORE button on the mound. Items: Bunt attempt
          (which expands to Strike/Foul/In Play), Balk (auto-advance +1 each
          runner), Hit by pitch (PA-end via existing handler), Intentional ball
          (counts toward IBB; 4th IB fires intentional_walk PA-end automatically). */}
      {pitchDetail && (() => {
        const isSoftball = sport === 'Softball';
        const types = isSoftball
          ? ['Fastball', 'Changeup', 'Drop', 'Rise', 'Curve', 'Screwball']
          : ['Fastball', 'Curveball', 'Changeup', 'Slider', 'Knuckle', 'Other'];
        const veloMin = isSoftball ? 25 : 30;
        const veloMax = isSoftball ? 80 : 100;
        return (
          <div
            onClick={() => { setPitchDetail(null); setBipPitchInfo(null); }}
            style={{
              position: 'fixed', inset: 0, background: T.scrim,
              display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 65, padding: '20px',
            }}
          >
            <div
              onClick={(e) => e.stopPropagation()}
              style={{
                background: T.paperRaised, borderRadius: '12px', padding: '18px',
                maxWidth: '340px', width: '100%', boxShadow: `0 10px 30px ${T.scrim}`,
              }}
            >
              {pitchDetail.stage === 'type' ? (
                <>
                  <div style={{ fontSize: '11px', fontWeight: 400, color: T.inkMuted, textAlign: 'center', marginBottom: '2px' }}>
                    Pitch type
                  </div>
                  <div style={{ fontSize: '12px', color: T.inkFaint, textAlign: 'center', marginBottom: '14px' }}>
                    Your read — best guess is fine
                  </div>
                  <div style={{
                    display: 'grid', gridTemplateColumns: '1fr 1fr',
                    borderTop: `1px solid ${T.rule}`, borderLeft: `1px solid ${T.rule}`,
                  }}>
                    {types.map(t => (
                      <button
                        key={t}
                        onClick={() => setPitchDetail(pd => ({ ...pd, pitchType: t, stage: 'velo' }))}
                        style={{
                          padding: '16px 8px', background: 'transparent',
                          border: 'none',
                          borderRight: `1px solid ${T.rule}`,
                          borderBottom: `1px solid ${T.rule}`,
                          borderRadius: 0,
                          fontFamily: 'inherit', fontSize: '15px', fontWeight: 400, color: T.ink,
                          cursor: 'pointer', minHeight: '54px',
                        }}
                      >{t}</button>
                    ))}
                  </div>
                  <button
                    onClick={() => commitPitchDetail(null, null)}
                    style={{
                      width: '100%', padding: '11px', marginTop: '12px', background: 'transparent',
                      color: T.inkMuted, border: 'none', fontFamily: 'inherit', fontSize: '13px',
                      fontWeight: 500, cursor: 'pointer',
                    }}
                  >Didn't catch it — log {pitchDetail.pitchKey === 'in_play' ? 'play' : 'pitch'} only</button>
                </>
              ) : (
                <VeloScroller
                  min={veloMin}
                  max={veloMax}
                  initial={lastVelo}
                  pitchTypeLabel={pitchDetail.pitchType}
                  onPick={(v) => commitPitchDetail(pitchDetail.pitchType, v)}
                  onSkip={() => commitPitchDetail(pitchDetail.pitchType, null)}
                />
              )}
            </div>
          </div>
        );
      })()}

      {pitchEndConfirm && (
        <div
          onClick={() => setPitchEndConfirm(null)}
          style={{
            position: 'fixed', inset: 0, background: T.scrim,
            display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 60, padding: '20px',
          }}
        >
          <div
            onClick={(e) => e.stopPropagation()}
            style={{
              background: T.paperRaised, borderRadius: '12px', padding: '20px',
              maxWidth: '320px', width: '100%', boxShadow: `0 10px 30px ${T.scrim}`,
            }}
          >
            <div style={{ fontSize: '15px', fontWeight: 400, color: T.ink, marginBottom: '4px' }}>
              {pitchEndConfirm.kind === 'walk' ? 'Ball four — walk?' : 'Strike three?'}
            </div>
            <div style={{ fontSize: '12px', color: T.inkMuted, marginBottom: '16px', lineHeight: 1.4 }}>
              {pitchEndConfirm.kind === 'walk'
                ? 'Confirm the walk, or cancel if it was a mistap.'
                : 'Confirm the strikeout, or tag a dropped third strike.'}
            </div>
            <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
              {pitchEndConfirm.kind === 'walk' ? (
                <button
                  onClick={() => {
                    setPitchEndConfirm(null);
                    if (pitchDetailOn) setPitchDetail({ pitchKey: 'ball', stage: 'type', pitchType: null, paEnd: 'walk' });
                    else onPaEndTap('walk');
                  }}
                  style={{ width: '100%', padding: '14px', background: T.ink, color: T.paperRaised, border: 'none', borderRadius: '8px', fontFamily: 'inherit', fontSize: '14px', fontWeight: 600, cursor: 'pointer' }}
                >Confirm walk</button>
              ) : (
                <>
                  <button
                    onClick={() => {
                      const cat = pitchEndConfirm.pitch === 'called_strike' ? 'strikeout_called' : 'strikeout_swinging';
                      const pk = pitchEndConfirm.pitch;
                      setPitchEndConfirm(null);
                      if (pitchDetailOn) setPitchDetail({ pitchKey: pk, stage: 'type', pitchType: null, paEnd: cat });
                      else onPaEndTap(cat);
                    }}
                    style={{ width: '100%', padding: '14px', background: T.ink, color: T.paperRaised, border: 'none', borderRadius: '8px', fontFamily: 'inherit', fontSize: '14px', fontWeight: 600, cursor: 'pointer' }}
                  >Confirm strikeout</button>
                  <button
                    onClick={() => { setPitchEndConfirm(null); onPaTopLevelSelect('dropped_third_strike'); }}
                    style={{ width: '100%', padding: '14px', background: 'transparent', color: T.ink, border: `1px solid ${T.rule}`, borderRadius: '8px', fontFamily: 'inherit', fontSize: '14px', fontWeight: 600, cursor: 'pointer' }}
                  >Dropped third strike</button>
                </>
              )}
              <button
                onClick={() => setPitchEndConfirm(null)}
                style={{ width: '100%', padding: '10px', background: 'transparent', color: T.inkMuted, border: 'none', fontFamily: 'inherit', fontSize: '13px', fontWeight: 400, cursor: 'pointer' }}
              >Cancel — mistap</button>
            </div>
          </div>
        </div>
      )}

      {morePitchOpen && (
        <div
          onClick={() => { setMorePitchOpen(false); setMoreSubStage(null); }}
          style={{
            position: 'fixed',
            inset: 0,
            background: T.scrim,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            zIndex: 50,
          }}
        >
          <div
            onClick={(e) => e.stopPropagation()}
            style={{
              background: T.paperRaised,
              borderRadius: '12px',
              padding: '14px',
              minWidth: '260px',
              maxWidth: '320px',
              boxShadow: `0 10px 30px ${T.scrim}`,
            }}
          >
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: '10px' }}>
              <span style={{ fontSize: '11px', fontWeight: 400, color: T.inkMuted }}>
                {moreSubStage === 'bunt_resolve' ? 'Bunt — how?' : moreSubStage === 'end_inning_reason' ? 'End inning — why?' : 'Pitch'}
              </span>
              <button
                onClick={() => { setMorePitchOpen(false); setMoreSubStage(null); }}
                style={{
                  background: 'transparent',
                  border: 'none',
                  color: T.inkMuted,
                  fontSize: '13px',
                  fontWeight: 500,
                  cursor: 'pointer',
                  padding: '4px 8px',
                }}
              >
                Close
              </button>
            </div>

            {moreSubStage === null && (
              <div style={{ display: 'flex', flexDirection: 'column', gap: '6px' }}>
                {/* Primary pitch outcomes — same set as the bottom action row, but
                    now available from the Pitch button as the primary entry. The
                    bottom row remains as a muscle-memory shortcut. */}
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    handleCellPitchTap('ball');
                  }}
                  style={moreItemStyle}
                >
                  Ball
                </button>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    handleCellPitchTap('called_strike');
                  }}
                  style={moreItemStyle}
                >
                  Called strike
                </button>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    handleCellPitchTap('swing_miss');
                  }}
                  style={moreItemStyle}
                >
                  Swing & miss
                </button>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    onPitchTap('foul');
                  }}
                  style={moreItemStyle}
                >
                  Foul
                </button>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    onOpenBipQuick();
                  }}
                  style={moreItemStyle}
                >
                  Ball in play
                </button>

                {/* Divider — edge-case events below */}
                <div style={{
                  borderTop: `1px solid ${T.rule}`,
                  margin: '4px 0',
                }} />

                <button
                  onClick={() => setMoreSubStage('bunt_resolve')}
                  style={moreItemStyle}
                >
                  Bunt attempt
                </button>
                <button
                  onClick={() => {
                    // Balk: auto-advance every runner +1. 3B → home (scores), 2B → 3B, 1B → 2B.
                    const sk = cell?.stateKey || 'empty';
                    const has1 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('1'));
                    const has2 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('2'));
                    const has3 = sk === 'loaded' || (sk !== 'empty' && String(sk).includes('3'));
                    const runnerMovements = [];
                    if (has3) runnerMovements.push({ from: '3B', to: 'home' });
                    if (has2) runnerMovements.push({ from: '2B', to: '3B' });
                    if (has1) runnerMovements.push({ from: '1B', to: '2B' });
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    if (runnerMovements.length > 0) {
                      onRunnerAdvanceTap('balk', { runnerMovements });
                    } else {
                      // No runners on base — balk is still a recordable event but has no
                      // base effect. Log it as a between_pitch with empty movements.
                      onRunnerAdvanceTap('balk', { runnerMovements: [] });
                    }
                  }}
                  style={moreItemStyle}
                >
                  Balk
                </button>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    onPaEndTap('hit_by_pitch');
                  }}
                  style={moreItemStyle}
                >
                  Hit by pitch
                </button>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    onPitchTap('intentional_ball');
                  }}
                  style={moreItemStyle}
                >
                  Intentional ball
                </button>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    onPaTopLevelSelect('intentional_walk');
                  }}
                  style={moreItemStyle}
                >
                  Intentional walk
                </button>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    onPaEndTap('batter_out');
                  }}
                  style={moreItemStyle}
                >
                  Batter out (Interference)
                </button>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    onPaTopLevelSelect('other');
                  }}
                  style={moreItemStyle}
                >
                  Other
                </button>

                {/* Divider — scorer settings */}
                <div style={{ borderTop: `1px solid ${T.rule}`, margin: '4px 0' }} />

                {/* Pitch Detail toggle. Lives here so it can be flipped mid-game —
                    a scorer may want type/velo for one pitcher and not the rest. */}
                <button
                  onClick={() => onTogglePitchDetail && onTogglePitchDetail()}
                  style={{ ...moreItemStyle, display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}
                >
                  <span>Pitch detail (type + speed)</span>
                  <span style={{
                    fontSize: '11px', fontWeight: 400, padding: '3px 8px', borderRadius: '10px',
                    background: pitchDetailOn ? T.ink : T.rule,
                    color: pitchDetailOn ? T.paperRaised : T.inkMuted,
                  }}>{pitchDetailOn ? 'On' : 'Off'}</span>
                </button>

                {/* Divider — inning control (practice / pitch-count mode) */}
                <div style={{ borderTop: `1px solid ${T.rule}`, margin: '4px 0' }} />

                <button
                  onClick={() => setMoreSubStage('end_inning_reason')}
                  style={{ ...moreItemStyle, color: T.ink }}
                >
                  End half inning
                </button>
              </div>
            )}

            {moreSubStage === 'end_inning_reason' && (
              <div style={{ display: 'flex', flexDirection: 'column', gap: '6px' }}>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    onEndHalfInning && onEndHalfInning('pitch_count');
                  }}
                  style={moreItemStyle}
                >
                  Pitch Count
                </button>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    onEndHalfInning && onEndHalfInning('run_limit');
                  }}
                  style={moreItemStyle}
                >
                  Run limit
                </button>
                <button
                  onClick={() => setMoreSubStage(null)}
                  style={{ ...moreItemStyle, background: 'transparent', color: T.inkMuted, border: `1px solid ${T.rule}` }}
                >
                  ← Back
                </button>
              </div>
            )}

            {moreSubStage === 'bunt_resolve' && (
              <div style={{ display: 'flex', flexDirection: 'column', gap: '6px' }}>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    handleCellPitchTap('bunt_strike');
                  }}
                  style={moreItemStyle}
                >
                  Strike (missed)
                </button>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    onPitchTap('bunt_foul');
                  }}
                  style={moreItemStyle}
                >
                  Foul
                </button>
                <button
                  onClick={() => {
                    setMorePitchOpen(false);
                    setMoreSubStage(null);
                    // In-play bunt routes through the BIP cascade; the scorer picks
                    // the outcome (single, sac bunt, FC, etc.) from there.
                    onOpenBipQuick();
                  }}
                  style={moreItemStyle}
                >
                  In Play
                </button>
                <button
                  onClick={() => setMoreSubStage(null)}
                  style={{ ...moreItemStyle, background: 'transparent', color: T.inkMuted, border: `1px solid ${T.rule}` }}
                >
                  ← Back
                </button>
              </div>
            )}
          </div>
        </div>
      )}

      {/* Pitch row */}
      <div style={{
        display: 'flex', alignItems: 'baseline', justifyContent: 'space-between',
        margin: '0 2px 6px',
      }}>
        <span style={{ fontSize: '11px', fontWeight: 400, color: T.inkMuted }}>Pitch</span>
        <button
          type="button"
          onClick={() => { setMorePitchOpen(true); setMoreSubStage(null); }}
          style={{
            background: 'transparent', border: 'none', padding: '4px 2px',
            fontFamily: 'inherit', fontSize: '11px', fontWeight: 400,
            color: T.inkMuted, cursor: 'pointer',
          }}
        >More</button>
      </div>
      {/* One row across the bottom of the field: five equal columns divided by
          hairlines. In play is the fifth and stays the one filled element — it is
          the tap the scorer reaches for most. */}
      <div style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(5, 1fr)',
        borderTop: `1px solid ${T.rule}`,
        borderLeft: `1px solid ${T.rule}`,
        marginBottom: '10px',
        opacity: armedRunner ? 0.4 : 1,
        pointerEvents: armedRunner ? 'none' : 'auto',
        transition: 'opacity 120ms ease',
      }}>
        {pitchButtons.map(b => {
          const flashing = localFlash === `pitch:${b.key}`;
          return (
            <button
              key={b.key}
              onClick={() => { flashLocally(`pitch:${b.key}`); handleCellPitchTap(b.key); }}
              style={{
                ...baseBtnStyle,
                fontSize: '13px',
                padding: '12px 2px',
                ...(flashing ? flashStyle : {}),
              }}
            >
              {b.label}
            </button>
          );
        })}
        {/* Ball in play quick button — skips the PA Result top-level takeover and goes
            straight to the batted-ball-type screen. Lives on the pitch row because it's
            a one-tap shortcut for the most common cascade entry. */}
        <button
          onClick={onOpenBipQuick}
          style={{
            ...baseBtnStyle,
            fontSize: '13px',
            padding: '12px 2px',
            border: 'none',
            background: T.ink,
            color: T.paper,
            fontWeight: 600,
          }}
        >
          In play
        </button>
      </div>

      {/* PA Result and Runner advance dropdowns removed — those cascades now fire only
          from the action buttons (Ball in play, pitch outcomes) and from tapping runners
          on the diamond, which keeps the cell view focused on the field. */}

      {/* 2A+: Pitcher-number chip removed from CellView. Pitcher jersey entry now
          lives in the scoreboard strip (tap either team's pitcher line to edit),
          which also lets the scorer set both teams' pitchers, not just the current
          defender. */}

      {/* Undo now lives inside the FieldDiamond (bottom-right foul territory). */}
        </>
      )}

      {/* Leadoff-mark confirm dialog. Fixed overlay; doesn't affect layout.
          Renders only when scorer taps the batter header. Wording depends on
          how many lineup_marker events already exist for the batting side:
            0 → "Mark this batter as leadoff?" (first marker)
            1 → "Mark this batter as leadoff again?" (second marker — resolves lineup)
            2+ → "Re-anchor leadoff to this batter?" (correction case) */}
      {trailRunnerPrompt && (() => {
        const { runnerMovements, category, pitch, paEndCategory, trailingBases } = trailRunnerPrompt;
        const baseLabel = { '1B': '1st', '2B': '2nd', '3B': '3rd' };
        const nextBase = { '1B': '2B', '2B': '3B', '3B': 'home' };
        const list = trailingBases.map(b => baseLabel[b]).join(' and ');
        const actionWord = category === 'battery_error' ? 'on the wild pitch / passed ball' : 'on the steal';
        const advanceTrailers = () => runnerMovements.map(m =>
          (trailingBases.includes(m.from) && m.to === m.from)
            ? { from: m.from, to: nextBase[m.from] }
            : m
        );
        return (
          <div style={{
            position: 'fixed', inset: 0, background: T.scrim,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            zIndex: 60, padding: '24px',
          }}>
            <div style={{
              background: T.paperRaised, borderRadius: '12px', padding: '24px 22px 20px',
              maxWidth: '340px', width: '100%', boxShadow: `0 8px 32px ${T.ruleStrong}`,
              border: `1px solid ${T.rule}`,
            }}>
              <div style={{ fontSize: '11px', color: T.inkMuted, fontWeight: 400, marginBottom: '8px' }}>
                Trailing runner
              </div>
              <div style={{ fontSize: '16px', lineHeight: 1.4, color: T.ink, marginBottom: '6px', fontWeight: 400 }}>
                Did the runner{trailingBases.length > 1 ? 's' : ''} on {list} advance {actionWord}?
              </div>
              <div style={{ fontSize: '12px', lineHeight: 1.5, color: T.inkSecondary, marginBottom: '20px' }}>
                Yes slides {trailingBases.length > 1 ? 'them' : 'the runner'} up one base on the same play. No leaves {trailingBases.length > 1 ? 'them' : 'the runner'} put — you can still move {trailingBases.length > 1 ? 'them' : 'the runner'} manually.
              </div>
              <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
                <button
                  onClick={() => finalizeIdleCommit(advanceTrailers(), category, pitch, paEndCategory)}
                  style={{
                    background: T.ink, color: T.paperRaised, border: 'none', borderRadius: '8px',
                    padding: '14px', fontFamily: 'inherit', fontSize: '14px', fontWeight: 600, cursor: 'pointer',
                  }}
                >
                  Yes — advanced
                </button>
                <button
                  onClick={() => finalizeIdleCommit(runnerMovements, category, pitch, paEndCategory)}
                  style={{
                    background: 'transparent', color: T.ink, border: `1px solid ${T.ruleStrong}`,
                    borderRadius: '8px', padding: '14px', fontFamily: 'inherit', fontSize: '14px',
                    fontWeight: 400, cursor: 'pointer',
                  }}
                >
                  No — held
                </button>
              </div>
            </div>
          </div>
        );
      })()}

      {leadoffDialogOpen && batterSlot && (
        <div
          onClick={() => setLeadoffDialogOpen(false)}
          style={{
            position: 'fixed',
            inset: 0,
            background: T.scrim,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            zIndex: 50,
            padding: '24px',
          }}
        >
          <div
            onClick={(e) => e.stopPropagation()}
            style={{
              background: T.paperRaised,
              borderRadius: '12px',
              padding: '24px 22px 20px',
              maxWidth: '340px',
              width: '100%',
              boxShadow: `0 8px 32px ${T.ruleStrong}`,
              border: `1px solid ${T.rule}`,
            }}
          >
            <div style={{
              fontSize: '11px',
              color: T.inkMuted,
              fontWeight: 400,
              marginBottom: '8px',
            }}>
              Mark Leadoff
            </div>
            <div style={{
              fontSize: '16px',
              lineHeight: 1.4,
              color: T.ink,
              marginBottom: '6px',
              fontWeight: 400,
            }}>
              {lineupMarkerCount === 0
                ? 'Mark this batter as leadoff?'
                : lineupMarkerCount === 1
                ? 'Mark this batter as leadoff again?'
                : 'Re-anchor leadoff to this batter?'}
            </div>
            <div style={{
              fontSize: '12px',
              lineHeight: 1.5,
              color: T.inkSecondary,
              marginBottom: '20px',
            }}>
              {lineupMarkerCount === 0
                ? 'Anchors slot 1. The next leadoff mark will set the lineup size.'
                : lineupMarkerCount === 1
                ? 'Resolves the lineup size. The header will switch to "Batter X of N" from now on.'
                : 'Re-anchors slot 1 starting from this batter. Previous PAs keep their slot assignments.'}
            </div>
            <div style={{ display: 'flex', gap: '8px' }}>
              <button
                onClick={() => setLeadoffDialogOpen(false)}
                style={{
                  flex: 1,
                  background: 'transparent',
                  border: `1px solid ${T.ruleStrong}`,
                  borderRadius: '8px',
                  padding: '12px',
                  fontSize: '14px',
                  fontWeight: 400,
                  color: T.inkSecondary,
                  cursor: 'pointer',
                  fontFamily: 'inherit',
                }}
              >
                Cancel
              </button>
              <button
                onClick={() => {
                  if (onMarkLeadoff) onMarkLeadoff();
                  setLeadoffDialogOpen(false);
                }}
                style={{
                  flex: 1,
                  background: T.ink,
                  color: T.paperRaised,
                  border: 'none',
                  borderRadius: '8px',
                  padding: '12px',
                  fontSize: '14px',
                  fontWeight: 600,
                  cursor: 'pointer',
                  fontFamily: 'inherit',
                }}
              >
                Mark Leadoff
              </button>
            </div>
          </div>
        </div>
      )}
    </div>
  );
};


function LoopholeMatrixTagger() {
  // Events: every tap is { stateKey, outs, timestamp }
  const [events, setEvents] = useState([]);
  const [lastTap, setLastTap] = useState(null);
  const [currentCell, setCurrentCell] = useState(null); // {stateKey, outs} - persistent highlight, updates on each tap
  const [pitcherChanges, setPitcherChanges] = useState({ home: [], away: [] }); // event indices where new pitchers entered
  // Per-cell display baseline. Each entry is { cellId: eventIndex }. The cell only displays
  // events at index >= baseline. When user moves away from a cell, that cell's baseline is bumped
  // to the current events length, hiding its prior count visually. The full event log in `events`
  // is unchanged; only the visible display counts are affected.
  const [cellBaselines, setCellBaselines] = useState({});
  // Whether the user has dismissed the "ready to begin" start screen for the current game.
  // Resets when a new game starts.
  const [startScreenDismissed, setStartScreenDismissed] = useState(false);
  // v1.2 cell-expansion model:
  //   expandedCell  = { stateKey, outs } when a cell is open, null when on matrix
  //   flashingButton = { row, key } during the ~150ms tap-flash before auto-return, null otherwise
  // The prompt system from v1.1 (pendingPrompt / promptCell / suppressNextPrompt) was removed;
  // capture happens directly inside the expanded cell via dedicated buttons.
  const [expandedCell, setExpandedCell] = useState(null);
  // Lineup-confirmation prompt — fires after a PA when the running slot reaches 10+
  // on a side whose lineup isn't yet resolved (no two markers, no explicit lineupSize).
  // { side: 'home'|'away', proposedSize: number } | null. The modal asks "Is the next
  // batter the leadoff?" — Yes drops a lineup_marker for that side; No dismisses for
  // this PA (the prompt will appear again after the next pa_end if still unresolved).
  const [lineupPrompt, setLineupPrompt] = useState(null);
  // Deferred lineup check — stored when a lineup prompt would fire on an inning-ending
  // PA (the scorer can't answer yet because the other team is about to bat). When the
  // deferred side comes back up to bat, the prompt fires automatically.
  const [pendingLineupCheck, setPendingLineupCheck] = useState(null); // { side, proposedSize } | null
  // Manual lineup-flip dialog — opened by tapping the batter panel. Lets the scorer
  // loop a short lineup (e.g., 7 batters in a scrimmage) at any point, not just at 10+.
  const [manualLineupDialog, setManualLineupDialog] = useState(null); // { side, currentSlot } | null
  const [flashingButton, setFlashingButton] = useState(null);
  const [isLoaded, setIsLoaded] = useState(false);
  const [storageMode, setStorageMode] = useState('checking');

  // 2A+: pitcher-number entry lives in the scoreboard strip. This tracks which
  // side's pitcher line is being edited inline ('home' | 'away' | null) and the
  // in-progress draft. Replaces the disconnected chip that used to sit in CellView.
  const [pitcherStripEdit, setPitcherStripEdit] = useState(null); // { side, draft } | null

  // Game metadata — persisted alongside events
  const [gameMeta, setGameMeta] = useState(null); // null = no game in progress
  // Lineups — UI-only roster of { num, name } per team, in batting order. Names are
  // NEVER written to the JSON; only numbers live in the data. This is purely a human
  // layer so a parent can see which player is which on the diamond. Editable mid-game.
  const [rosters, setRosters] = useState({ home: [], away: [] });
  // Pitchers are tracked separately from the batting order — a pitcher may not bat,
  // and the batting roster is strictly the lineup. Same shape: { num, name } per team.
  const [pitcherRosters, setPitcherRosters] = useState({ home: [], away: [] });
  const [rosterEditor, setRosterEditor] = useState(null); // { team } | null
  const [lineupPanelOpen, setLineupPanelOpen] = useState(false);
  const [lineupTeamView, setLineupTeamView] = useState(null); // null = follow batting side
  // Pitch Detail mode. OFF by default: the one-tap pitch is the app's fastest and most
  // frequent action, and routing every pitch through two extra screens would slow the
  // core loop badly. When on, each pitch asks for type then velocity — both skippable.
  const [pitchDetailOn, setPitchDetailOn] = useState(false);
  // Last velocity entered per pitching side, so the scroller opens near the previous
  // reading instead of the middle of the range. Most pitches land within a few mph of
  // the last one, which turns a long scroll into a nudge.
  const [lastVelo, setLastVelo] = useState({ home: null, away: null });
  const [setupForm, setSetupForm] = useState({
    sport: '',
    format: '',
    homeTeam: '',
    awayTeam: '',
    ageDivision: '',
    host: '',
    homeLineupSize: '', // Deferred — no longer collected at setup. Inferred at runtime
    awayLineupSize: '', // via lineup_marker events (scorer taps batter header on leadoff).
  });

  // Action arming and export state
  const [endArmed, setEndArmed] = useState(false);
  const [exportedAt, setExportedAt] = useState(null); // timestamp of last export, null = current state unsaved
  // Which team is currently batting. Defaults to 'away' (away bats first by convention).
  const [battingSide, setBattingSide] = useState('away');

  // Fire deferred lineup check when the batting side returns. The prompt was stored
  // because it couldn't fire at inning's end (the scorer can't answer while the other
  // team is about to bat). Now that this side is batting again, the scorer can see who
  // came up and answer "is this the leadoff?"
  React.useEffect(() => {
    if (!pendingLineupCheck) return;
    if (battingSide === pendingLineupCheck.side) {
      const t = setTimeout(() => {
        setLineupPrompt(pendingLineupCheck);
        setPendingLineupCheck(null);
      }, 400);
      return () => clearTimeout(t);
    }
  }, [battingSide, pendingLineupCheck]);

  const STORAGE_KEY = 'loophole:current-game';
  const META_KEY = 'loophole:current-meta';
  const EXPORTED_KEY = 'loophole:current-exported';
  const BATTING_KEY = 'loophole:current-batting';
  const CURRENT_CELL_KEY = 'loophole:current-cell';
  const EXPANDED_CELL_KEY = 'loophole:expanded-cell';
  const PITCHER_CHANGES_KEY = 'loophole:pitcher-changes';
  const ROSTERS_KEY = 'loophole:rosters';
  const PITCHER_ROSTERS_KEY = 'loophole:pitcher-rosters';
  const PITCH_DETAIL_KEY = 'loophole:pitch-detail';
  const CELL_BASELINES_KEY = 'loophole:cell-baselines';
  const START_DISMISSED_KEY = 'loophole:start-dismissed';

  // Storage: localStorage as primary (window.storage in artifacts didn't reliably persist)
  const storage = {
    get(key) {
      if (typeof window === 'undefined' || !window.localStorage) return null;
      try {
        return window.localStorage.getItem(key);
      } catch {
        return null;
      }
    },
    set(key, value) {
      if (typeof window === 'undefined' || !window.localStorage) return false;
      try {
        window.localStorage.setItem(key, value);
        return true;
      } catch {
        return false;
      }
    },
    delete(key) {
      if (typeof window === 'undefined' || !window.localStorage) return;
      try { window.localStorage.removeItem(key); } catch {}
    },
  };

  // Load events from localStorage on mount, detect storage availability
  useEffect(() => {
    let cancelled = false;
    let mode = 'memory-only';
    if (typeof window !== 'undefined' && window.localStorage) {
      try {
        window.localStorage.setItem('__loophole_test__', '1');
        window.localStorage.removeItem('__loophole_test__');
        mode = 'local-storage';
      } catch {
        mode = 'memory-only';
      }
    }

    try {
      const raw = storage.get(STORAGE_KEY);
      if (!cancelled && raw) {
        const parsed = JSON.parse(raw);
        if (Array.isArray(parsed)) {
          setEvents(parsed);
        }
      }
      const rawMeta = storage.get(META_KEY);
      if (!cancelled && rawMeta) {
        const parsedMeta = JSON.parse(rawMeta);
        if (parsedMeta && typeof parsedMeta === 'object') {
          setGameMeta(parsedMeta);
        }
      }
      const rawExported = storage.get(EXPORTED_KEY);
      if (!cancelled && rawExported) {
        const parsedExported = parseInt(rawExported, 10);
        if (!isNaN(parsedExported)) {
          setExportedAt(parsedExported);
        }
      }
      const rawBatting = storage.get(BATTING_KEY);
      if (!cancelled && (rawBatting === 'home' || rawBatting === 'away')) {
        setBattingSide(rawBatting);
      }
      const rawCurrentCell = storage.get(CURRENT_CELL_KEY);
      if (!cancelled && rawCurrentCell) {
        try {
          const parsed = JSON.parse(rawCurrentCell);
          if (parsed && parsed.stateKey) setCurrentCell(parsed);
        } catch {}
      }
      const rawExpandedCell = storage.get(EXPANDED_CELL_KEY);
      if (!cancelled && rawExpandedCell) {
        try {
          const parsed = JSON.parse(rawExpandedCell);
          if (parsed && parsed.stateKey) setExpandedCell(parsed);
        } catch {}
      }
      const rawPitcherChanges = storage.get(PITCHER_CHANGES_KEY);
      if (!cancelled && rawPitcherChanges) {
        try {
          const parsed = JSON.parse(rawPitcherChanges);
          if (parsed && Array.isArray(parsed.home) && Array.isArray(parsed.away)) {
            setPitcherChanges(parsed);
          }
        } catch {}
      }
      const rawCellBaselines = storage.get(CELL_BASELINES_KEY);
      if (!cancelled && rawCellBaselines) {
        try {
          const parsed = JSON.parse(rawCellBaselines);
          if (parsed && typeof parsed === 'object') {
            setCellBaselines(parsed);
          }
        } catch {}
      }
      const rawStartDismissed = storage.get(START_DISMISSED_KEY);
      if (!cancelled && rawStartDismissed === 'true') {
        setStartScreenDismissed(true);
      }
      const rawRosters = storage.get(ROSTERS_KEY);
      if (!cancelled && rawRosters) {
        try {
          const parsed = JSON.parse(rawRosters);
          if (parsed && Array.isArray(parsed.home) && Array.isArray(parsed.away)) {
            setRosters(parsed);
          }
        } catch {}
      }
      const rawPitcherRosters = storage.get(PITCHER_ROSTERS_KEY);
      if (!cancelled && rawPitcherRosters) {
        try {
          const parsed = JSON.parse(rawPitcherRosters);
          if (parsed && Array.isArray(parsed.home) && Array.isArray(parsed.away)) {
            setPitcherRosters(parsed);
          }
        } catch {}
      }
      const rawPitchDetail = storage.get(PITCH_DETAIL_KEY);
      if (!cancelled && rawPitchDetail === '1') setPitchDetailOn(true);
    } catch {
      // No saved game or unparseable — start fresh
    } finally {
      if (!cancelled) {
        setStorageMode(mode);
        setIsLoaded(true);
      }
    }
    return () => { cancelled = true; };
  }, []);

  // Write events to localStorage whenever they change (after initial load)
  useEffect(() => {
    if (!isLoaded) return;
    if (events.length === 0) {
      storage.delete(STORAGE_KEY);
    } else {
      storage.set(STORAGE_KEY, JSON.stringify(events));
    }
  }, [events, isLoaded]);

  // Write metadata to localStorage whenever it changes
  useEffect(() => {
    if (!isLoaded) return;
    if (gameMeta === null) {
      storage.delete(META_KEY);
    } else {
      storage.set(META_KEY, JSON.stringify(gameMeta));
    }
  }, [gameMeta, isLoaded]);

  // Persist exportedAt so save state survives refreshes
  useEffect(() => {
    if (!isLoaded) return;
    if (exportedAt === null) {
      storage.delete(EXPORTED_KEY);
    } else {
      storage.set(EXPORTED_KEY, String(exportedAt));
    }
  }, [exportedAt, isLoaded]);

  // Persist which team is at bat
  useEffect(() => {
    if (!isLoaded) return;
    storage.set(BATTING_KEY, battingSide);
  }, [battingSide, isLoaded]);

  // Persist currentCell so the highlight survives refresh
  useEffect(() => {
    if (!isLoaded) return;
    if (currentCell === null) {
      storage.delete(CURRENT_CELL_KEY);
    } else {
      storage.set(CURRENT_CELL_KEY, JSON.stringify(currentCell));
    }
  }, [currentCell, isLoaded]);

  // Persist expandedCell so the diamond's base-out state survives the artifact
  // re-mounting (e.g., closing the tagger to type a message and reopening it).
  useEffect(() => {
    if (!isLoaded) return;
    if (expandedCell === null) {
      storage.delete(EXPANDED_CELL_KEY);
    } else {
      storage.set(EXPANDED_CELL_KEY, JSON.stringify(expandedCell));
    }
  }, [expandedCell, isLoaded]);

  // Persist rosters (UI-only lineups). Kept separate from the game JSON.
  useEffect(() => {
    if (!isLoaded) return;
    if ((rosters.home.length === 0) && (rosters.away.length === 0)) {
      storage.delete(ROSTERS_KEY);
    } else {
      storage.set(ROSTERS_KEY, JSON.stringify(rosters));
    }
  }, [rosters, isLoaded]);

  useEffect(() => {
    if (!isLoaded) return;
    if ((pitcherRosters.home.length === 0) && (pitcherRosters.away.length === 0)) {
      storage.delete(PITCHER_ROSTERS_KEY);
    } else {
      storage.set(PITCHER_ROSTERS_KEY, JSON.stringify(pitcherRosters));
    }
  }, [pitcherRosters, isLoaded]);

  // Pitch Detail is a scorer preference, not game data — it persists across games so
  // the mode doesn't have to be re-enabled every time.
  useEffect(() => {
    if (!isLoaded) return;
    if (pitchDetailOn) storage.set(PITCH_DETAIL_KEY, '1');
    else storage.delete(PITCH_DETAIL_KEY);
  }, [pitchDetailOn, isLoaded]);

  // Persist pitcher changes so per-pitcher counts survive refresh
  useEffect(() => {
    if (!isLoaded) return;
    if (pitcherChanges.home.length === 0 && pitcherChanges.away.length === 0) {
      storage.delete(PITCHER_CHANGES_KEY);
    } else {
      storage.set(PITCHER_CHANGES_KEY, JSON.stringify(pitcherChanges));
    }
  }, [pitcherChanges, isLoaded]);

  // Persist cell baselines so the cleared-cells state survives refresh
  useEffect(() => {
    if (!isLoaded) return;
    if (Object.keys(cellBaselines).length === 0) {
      storage.delete(CELL_BASELINES_KEY);
    } else {
      storage.set(CELL_BASELINES_KEY, JSON.stringify(cellBaselines));
    }
  }, [cellBaselines, isLoaded]);

  // Persist start-screen-dismissed flag so the start screen doesn't reappear on refresh mid-game
  useEffect(() => {
    if (!isLoaded) return;
    if (startScreenDismissed) {
      storage.set(START_DISMISSED_KEY, 'true');
    } else {
      storage.delete(START_DISMISSED_KEY);
    }
  }, [startScreenDismissed, isLoaded]);

  // Derived: only pitch events. Used everywhere that needs pitch counts as opposed to
  // counting all events (which now include pa_end, between_pitch, pitcher_change, inning_change).
  // !e.type back-compat for legacy stored events that didn't have a type field.
  const pitchEvents = events.filter(e => e.type === 'pitch' || !e.type);
  const totalPA = pitchEvents.length;

  // Total count for canonical data (used in email body, JSON, export). Always full pitch event log.
  const getCellTotalCount = (stateKey, outs) =>
    pitchEvents.filter(e => e.stateKey === stateKey && e.outs === outs).length;

  // Display count for the live matrix UI. Respects per-cell baseline so the visible
  // count clears when the user moves to a different cell or swipes the slider.
  // NOTE: baseline indexes refer to the full events array, not pitchEvents.
  const getCellCount = (stateKey, outs) => {
    const cellId = `${stateKey}-${outs}`;
    const baseline = cellBaselines[cellId] || 0;
    return events.slice(baseline).filter(e => (e.type === 'pitch' || !e.type) && e.stateKey === stateKey && e.outs === outs).length;
  };

  // v1.2: Cell tap opens the expanded cell view. Pitches are NOT logged here —
  // they're logged from inside the expanded view via the Ball/Strike/Foul buttons.
  // Cell-baseline bumping still happens on cell change so the previous cell's
  // visible count clears when navigating to a different cell.
  // v1.5: Auto-score detection. When the scorer taps a new cell after a state-
  // changing event (pa_end or between_pitch), compute runs from the state delta
  // and fire score_change events automatically.
  const handleTap = useCallback((stateKey, outs) => {
    const isCellChange = currentCell && (currentCell.stateKey !== stateKey || currentCell.outs !== outs);
    if (isCellChange) {
      const prevCellId = `${currentCell.stateKey}-${currentCell.outs}`;
      setCellBaselines(prev => ({
        ...prev,
        [prevCellId]: events.length,
      }));
      // Auto-score: derive runs from the state delta and fire score_change events.
      // Only triggers when there's a state-changing event since the last cell tap.
      if (shouldAutoScore(stateKey, outs)) {
        const scoreResult = computeRunsForDelta(events, stateKey, outs);
        if (scoreResult && scoreResult.runs > 0) {
          const now = Date.now();
          // Fire one score_change event per run so undo pops them one-at-a-time.
          const newEvents = [];
          for (let r = 0; r < scoreResult.runs; r++) {
            newEvents.push({
              type: 'score_change',
              side: scoreResult.side,
              delta: 1,
              auto: true,             // tag as auto-detected so postgame can audit
              timestamp: now + r,
            });
          }
          setEvents(prev => [...prev, ...newEvents]);
          setExportedAt(null);
        }
      }
    }
    setExpandedCell({ stateKey, outs });
  }, [currentCell, events]);

  // Returns to the matrix view from inside an expanded cell.

  // Computes the live ball/strike count for the currently expanded cell's open PA.
  // v1.2 fix: Count is PA-scoped, not cell-scoped. Walks forward from the most
  // recent PA boundary (pa_end or inning_change), accumulating balls and strikes
  // regardless of which cell each pitch was tagged in. This is important because
  // the same PA can span multiple cells when a runner advances mid-PA — the count
  // must carry forward.
  // Forward walk (not backward) is required for the foul-ball rule: a foul counts
  // as a strike only when strikes < 2 *at the moment the foul was thrown*. A
  // backward walk evaluates against the running tally, which produces wrong
  // results (e.g. a foul late in the PA gets counted because the backward tally
  // hasn't accumulated the 2 strikes yet).
  // The optional `eventList` arg lets callers compute the count against a candidate
  // event list (used by handlePitchTap to pre-check caps without committing).
  const getExpandedCellCount = (eventList) => {
    const list = eventList || events;
    if (!expandedCell) return { balls: 0, strikes: 0 };
    // Find the index right after the most recent PA boundary; if none, start at 0.
    let startIdx = 0;
    for (let i = list.length - 1; i >= 0; i--) {
      const e = list[i];
      if (e.type === 'pa_end' || e.type === 'inning_change') {
        startIdx = i + 1;
        break;
      }
    }
    // Walk forward from there, accumulating count in natural order.
    let balls = 0;
    let strikes = 0;
    for (let i = startIdx; i < list.length; i++) {
      const e = list[i];
      if (e.type !== 'pitch' && e.type) continue; // skip non-pitch events (between_pitch, pitcher_change)
      // Balls: regular ball or intentional ball (IBB counted in 'IB' attempts)
      if (e.pitch === 'ball' || e.pitch === 'intentional_ball') balls++;
      // Strikes from swings/called: includes bunt-attempt miss
      else if (e.pitch === 'called_strike' || e.pitch === 'swing_miss' || e.pitch === 'bunt_strike') strikes++;
      // Fouls (regular or bunt-attempt foul): counted as strike only while < 2
      else if ((e.pitch === 'foul' || e.pitch === 'bunt_foul') && strikes < 2) strikes++;
      // 'in_play' pitches don't count toward balls/strikes — they're PA-end markers
    }
    return { balls, strikes };
  };

  // Computes which slot in the lineup is currently at the plate for the side that's
  // Returns the current batter's slot information for the side currently
  // batting. Two paths:
  //
  // (A) Legacy / pre-set lineup size — gameMeta has homeLineupSize/awayLineupSize.
  //     Slot = (pa_end count for this side) mod lineupSize + 1.
  //
  // (B) Inferred from lineup_marker events — gameMeta lineup size is empty.
  //     A lineup_marker is the scorer's manual "this batter is leadoff" tap.
  //     - 0 markers for this side  → lineup unknown. Return { slot: paCount+1, lineupSize: null }
  //                                  so the header can render "Batter #N" without denominator.
  //     - 1 marker  → anchored but unresolved. Slot = (pa_ends since marker) + 1.
  //                   lineupSize still null until the second marker arrives.
  //     - 2+ markers → resolved. lineupSize = pa_end count between the two most
  //                   recent markers. Slot wraps via mod from the most recent marker.
  //
  // Returns null only when there's no game in progress / no expanded cell.
  // Returns { slot: int, lineupSize: int|null } otherwise. lineupSize=null
  // signals "show running count only, no denominator."
  const getCurrentBatterSlot = () => {
    if (!gameMeta || !expandedCell) return null;

    // Path A — explicit lineup size set in gameMeta.
    const lineupSizeStr = battingSide === 'home'
      ? (gameMeta.homeLineupSize || gameMeta.lineupSize)
      : (gameMeta.awayLineupSize || gameMeta.lineupSize);
    if (lineupSizeStr) {
      const lineupSize = parseInt(lineupSizeStr, 10);
      if (lineupSize) {
        let paCount = 0;
        for (const e of events) {
          if (e.type === 'pa_end' && e.batting === battingSide) paCount++;
        }
        const slot = (paCount % lineupSize) + 1;
        return { slot, lineupSize };
      }
    }

    // Path A2 — a batting-order roster was entered. Its length IS the lineup size,
    // so the order loops on its own and each slot maps to a roster row. This lets the
    // entered numbers auto-fill at the plate without needing a leadoff marker.
    const slotRoster = rosters[battingSide] || [];
    if (slotRoster.length > 0) {
      let paCount = 0;
      for (const e of events) {
        if (e.type === 'pa_end' && e.batting === battingSide) paCount++;
      }
      const slot = (paCount % slotRoster.length) + 1;
      return { slot, lineupSize: slotRoster.length };
    }

    // Path B — infer from lineup_marker events.
    // Collect markers and pa_ends for this side in chronological order.
    const sideMarkers = [];
    let totalPaCount = 0;
    for (let i = 0; i < events.length; i++) {
      const e = events[i];
      if (e.type === 'lineup_marker' && e.side === battingSide) {
        // Capture pa_end count up to (but not including) this marker so we can
        // resolve lineup size from the gap between consecutive markers.
        sideMarkers.push({ index: i, paCountAtMarker: totalPaCount });
      }
      if (e.type === 'pa_end' && e.batting === battingSide) totalPaCount++;
    }

    if (sideMarkers.length === 0) {
      // No markers — return a running count so the header shows "Batter #N".
      return { slot: totalPaCount + 1, lineupSize: null };
    }

    if (sideMarkers.length === 1) {
      // Anchored at slot 1 but lineup size unknown. Slot = pa_ends since the
      // marker + 1 (the marker itself is the leadoff hitter, slot 1).
      const paSinceMarker = totalPaCount - sideMarkers[0].paCountAtMarker;
      return { slot: paSinceMarker + 1, lineupSize: null };
    }

    // 2+ markers — resolve lineup size from the gap between the two most recent.
    const lastMarker = sideMarkers[sideMarkers.length - 1];
    const prevMarker = sideMarkers[sideMarkers.length - 2];
    const inferredLineupSize = lastMarker.paCountAtMarker - prevMarker.paCountAtMarker;
    // Safety fallback: if for some reason the gap is 0 (shouldn't happen — would
    // mean two markers placed back-to-back without any PAs between), pretend
    // we only have one marker and return unresolved.
    if (inferredLineupSize < 1) {
      const paSinceMarker = totalPaCount - lastMarker.paCountAtMarker;
      return { slot: paSinceMarker + 1, lineupSize: null };
    }
    const paSinceLastMarker = totalPaCount - lastMarker.paCountAtMarker;
    const slot = (paSinceLastMarker % inferredLineupSize) + 1;
    return { slot, lineupSize: inferredLineupSize };
  };

  // Helper used by the header tap-to-mark-leadoff dialog: how many markers
  // exist for the currently-batting side. Drives the dialog wording (first vs
  // second marker) and the "leadoff" header chip visibility.
  const getLineupMarkerCount = (side) => {
    let n = 0;
    for (const e of events) {
      if (e.type === 'lineup_marker' && e.side === side) n++;
    }
    return n;
  };

  // Fired when the scorer confirms "this batter is leadoff" in the header dialog.
  // Appends a lineup_marker event for the currently-batting side, timestamped now.
  const markLeadoff = () => {
    if (!gameMeta || !battingSide) return;
    setEvents(prev => [...prev, {
      type: 'lineup_marker',
      side: battingSide,
      timestamp: Date.now(),
    }]);
  };

  // Computes current score for each side by walking the event log.
  // Two sources contribute to the score:
  //   1. score_change events — manual tap-to-increment overrides (and legacy auto-detected
  //      runs from the state-delta logic, pre-Step-4a).
  //   2. pa_end events with runnerMovements — each {to: 'home'} entry is a run scored
  //      by the batting side. This is the canonical auto-score source post-Step-4a;
  //      the diamond Done captures who scored explicitly.
  //   3. between_pitch events with runnerMovements — same rule. Steals of home and
  //      battery errors that score runners from 3rd produce runs here.
  // Source of truth is the event log — no separate state. Undo works automatically.
  const getScores = () => {
    let home = 0, away = 0;
    for (const e of events) {
      if (e.type === 'score_change') {
        if (e.side === 'home') home += e.delta;
        else if (e.side === 'away') away += e.delta;
      } else if ((e.type === 'pa_end' || e.type === 'between_pitch') && e.runnerMovements) {
        // Count runners who ended up at home (scored).
        const scored = e.runnerMovements.filter(m => m.to === 'home').length;
        if (scored > 0) {
          if (e.batting === 'home') home += scored;
          else if (e.batting === 'away') away += scored;
        }
      } else if (e.type === 'pitch' && e.runnerAction) {
        // Folded runner action on a pitch (SB/CS/BE/DI with a coinciding pitch).
        const scored = e.runnerAction.filter(m => m.to === 'home').length;
        if (scored > 0) {
          if (e.batting === 'home') home += scored;
          else if (e.batting === 'away') away += scored;
        }
      }
    }
    return { home, away };
  };


  // v1.5 — Jersey number tracking for lineup verification.
  // Stored on gameMeta as: jerseyNumbers: { home: { '1': '18', '3': '4', ... }, away: {...} }
  // Keyed by lineup slot. Scorer enters numbers lazily as new batters come up;
  // subsequent rotations through the same slot show the saved number for verification.
  // Persists via the same gameMeta save path that handles team names, format, etc.
  const getJerseyNumber = (side, slot) => {
    const sideMap = gameMeta && gameMeta.jerseyNumbers ? gameMeta.jerseyNumbers[side] : null;
    const manual = sideMap ? (sideMap[String(slot)] || null) : null;
    if (manual) return manual;
    // Fall back to the entered lineup: if the scorer typed this slot's number into
    // the roster, surface it at the plate so they never re-enter it. A manual edit
    // at the sphere (stored above) still wins, e.g. for a pinch hitter.
    const roster = rosters[side] || [];
    const entry = roster[slot - 1];
    return (entry && entry.num) ? String(entry.num) : null;
  };
  const setJerseyNumber = (side, slot, value) => {
    if (!gameMeta) return;
    // Substitution detection: if the slot has a recorded pa_end this game, the
    // slot has been "in use," so a jersey change here is a sub (not an initial
    // lineup entry or a typo fix before the slot ever batted). Emit a
    // batter_change event so the game report shows the new number from this
    // point forward — past pitch/pa_end rows keep their original batterNumber.
    const trimmed = (value || '').trim();
    const existingMap = (gameMeta.jerseyNumbers && gameMeta.jerseyNumbers[side]) || {};
    const previous = (existingMap[String(slot)] || '').trim();
    if (trimmed && previous && trimmed !== previous) {
      // Check whether this slot has ever had a pa_end. We compute the slot for
      // each pa_end on this side using the same logic as getCurrentBatterSlot,
      // simplified here: walk events in order, tracking running slot per side.
      const lineupSizeStr = side === 'home'
        ? (gameMeta.homeLineupSize || gameMeta.lineupSize)
        : (gameMeta.awayLineupSize || gameMeta.lineupSize);
      let slotHadPaEnd = false;
      if (lineupSizeStr) {
        const lineupSize = parseInt(lineupSizeStr, 10);
        if (lineupSize) {
          let paCount = 0;
          for (const e of events) {
            if (e.type === 'pa_end' && e.batting === side) {
              const s = (paCount % lineupSize) + 1;
              if (s === slot) { slotHadPaEnd = true; break; }
              paCount++;
            }
          }
        }
      } else {
        // Marker-based slot derivation: track running slot per side via markers
        let paAtLastMarker = 0;
        let sidePaCount = 0;
        let inferredLineupSize = null;
        let prevMarkerPa = null;
        for (const e of events) {
          if (e.type === 'lineup_marker' && e.side === side) {
            if (prevMarkerPa !== null) {
              const gap = sidePaCount - prevMarkerPa;
              if (gap > 0) inferredLineupSize = gap;
            }
            prevMarkerPa = sidePaCount;
            paAtLastMarker = sidePaCount;
          }
          if (e.type === 'pa_end' && e.batting === side) {
            const paSinceMarker = sidePaCount - paAtLastMarker;
            const s = inferredLineupSize
              ? (paSinceMarker % inferredLineupSize) + 1
              : paSinceMarker + 1;
            if (s === slot) { slotHadPaEnd = true; break; }
            sidePaCount++;
          }
        }
      }
      if (slotHadPaEnd) {
        setEvents(prev => [...prev, {
          type: 'batter_change',
          side,
          slot,
          newNumber: trimmed,
          timestamp: Date.now(),
        }]);
      }
    }
    setGameMeta(prev => {
      const existing = prev.jerseyNumbers || { home: {}, away: {} };
      const sideMap = { ...(existing[side] || {}) };
      if (trimmed) {
        sideMap[String(slot)] = trimmed;
      } else {
        delete sideMap[String(slot)];
      }
      return {
        ...prev,
        jerseyNumbers: {
          ...existing,
          [side]: sideMap,
        },
      };
    });
  };

  // Resolve the jersey number of the batter currently at the plate, for stamping
  // onto pitch / pa_end events. Returns the saved jersey for the current lineup
  // slot of the batting side, or null if none entered. Self-contained per row:
  // every pitch of a PA carries it, and it updates when a new batter comes up
  // (because the slot advances on pa_end).
  const getCurrentBatterNumber = () => {
    const bs = getCurrentBatterSlot();
    if (!bs || bs.slot == null) return null;
    return getJerseyNumber(battingSide, bs.slot);
  };

  // A hitter's line for the game (hits-for-at-bats), keyed by jersey number. Walks,
  // HBP, and sacrifices are not at-bats, so they don't count against the line. Used to
  // show "(1-3)" next to a name. Returns null if the number is unknown.
  const getHitterLine = (side, num) => {
    if (num == null || num === '') return null;
    const s = String(num);
    let hits = 0, atBats = 0;
    for (const e of events) {
      if (e.type !== 'pa_end' || e.batting !== side) continue;
      if (String(e.batterNumber == null ? '' : e.batterNumber) !== s) continue;
      const cat = e.category;
      const nonAB = cat === 'walk' || cat === 'intentional_walk' || cat === 'hit_by_pitch'
        || cat === 'sac_fly' || cat === 'sac_bunt' || cat === 'catcher_interference';
      if (!nonAB) atBats++;
      if (cat === 'hit') hits++;
    }
    return { hits, atBats };
  };

  // v1.5 — Pitcher jersey number tracking. Stored on pitcher_change events as an
  // optional jerseyNumber field. getCurrentPitcherNumber walks the event log
  // backward to find the latest pitcher_change for the side. If none exists, the
  // starter hasn't been logged yet — returns null and the scorer can tap to enter.
  // setCurrentPitcherNumber attaches the jerseyNumber to the latest pitcher_change
  // for the side; if no pitcher_change exists, fires a synthetic one (the starter).
  const getCurrentPitcherNumber = (side) => {
    for (let i = events.length - 1; i >= 0; i--) {
      const e = events[i];
      if (e.type === 'pitcher_change' && e.side === side) {
        return e.jerseyNumber || null;
      }
    }
    // Fall back to the lineup: pitchers come in one at a time, so the most recently
    // added pitcher is the one on the mound. Entering them populates the diamond,
    // exactly like the batting order auto-fills the plate.
    const plist = pitcherRosters[side] || [];
    for (let i = plist.length - 1; i >= 0; i--) {
      if (plist[i] && plist[i].num) return String(plist[i].num);
    }
    return null;
  };
  const setCurrentPitcherNumber = (side, value) => {
    const trimmed = (value || '').trim();
    // Find the latest pitcher_change for this side.
    let latestIdx = -1;
    let latestJersey = null;
    for (let i = events.length - 1; i >= 0; i--) {
      const e = events[i];
      if (e.type === 'pitcher_change' && e.side === side) {
        latestIdx = i;
        latestJersey = e.jerseyNumber || null;
        break;
      }
    }
    if (latestIdx >= 0 && latestJersey && trimmed && trimmed !== latestJersey) {
      // Jersey changed to a DIFFERENT number → new pitcher (auto pitching change).
      // Fire a new pitcher_change event with the new jersey, and reset baselines
      // so the new pitcher's pitch count starts from zero.
      const defendingSide = side;
      setEvents(prev => [...prev, {
        type: 'pitcher_change',
        side: defendingSide,
        jerseyNumber: trimmed,
        timestamp: Date.now(),
      }]);
      // Update pitcherChanges index so pitch-count derivation knows where to split.
      setPitcherChanges(prev => ({
        ...prev,
        [defendingSide]: [...(prev[defendingSide] || []), events.length],
      }));
      // Reset cell baseline for the current cell so the new pitcher's count starts at 0.
      if (currentCell) {
        const cellId = `${currentCell.stateKey}-${currentCell.outs}`;
        setCellBaselines(prev => ({
          ...prev,
          [cellId]: events.length + 1,
        }));
      }
    } else if (latestIdx >= 0) {
      // Update in place (correction or first-time jersey entry on existing event).
      setEvents(prev => prev.map((e, i) => {
        if (i !== latestIdx) return e;
        if (trimmed) return { ...e, jerseyNumber: trimmed };
        const { jerseyNumber, ...rest } = e;
        return rest;
      }));
    } else if (trimmed) {
      // No pitcher_change yet — fire one to mark the starter.
      setEvents(prev => [...prev, {
        type: 'pitcher_change',
        side,
        jerseyNumber: trimmed,
        timestamp: Date.now(),
        synthetic: true, // marks this as a "starter" entry rather than a mid-game change
      }]);
    }
  };

  // v1.5 — Auto-score helper. Counts how many runner-bases are occupied in a stateKey.
  // stateKey values are strings: 'empty', '1', '2', '3', '12', '13', '23', 'loaded'.
  const popcountRunners = (stateKey) => {
    if (!stateKey || stateKey === 'empty') return 0;
    if (stateKey === 'loaded') return 3;
    return String(stateKey).length;
  };

  // v1.5 step 7 — Pure derivation of the post-play base-out state from a
  // runnerMovements array. This is the foundation that lets the diamond commit
  // replace the manual matrix tap. Given:
  //   priorStateKey — the pre-play state ('empty' | '1' | '2' | ... | 'loaded')
  //   priorOuts     — outs before the play (0, 1, or 2)
  //   movements     — array of { from, to } entries
  //                   from: 'home' (batter) | '1B' | '2B' | '3B'
  //                   to:   'home' (scored) | '1B' | '2B' | '3B' | 'out'
  // Returns: { stateKey, outs, runsScored, inningEnded } where
  //   stateKey    — new state in the existing string format
  //   outs        — new out count (capped at 3)
  //   runsScored  — count of movements with to === 'home'
  //   inningEnded — true if outs reached 3
  //
  // This function is pure and side-effect-free; it's the canonical end-state
  // derivation logic that the diamond commit path and (eventually) postgame
  // analysis both rely on.
  //
  // Note: this function does NOT validate that the movements are consistent with
  // the prior state. That's the caller's responsibility (the diamond UI ensures
  // every runner has a destination before allowing commit).
  // Track which jersey number sits on each base, carried as runners advance. Only
  // runners who reached base AS A HITTER get a number (from the pa_end's batterNumber);
  // it follows them around the bases. Format-seeded runners (the Wildcard bases-loaded
  // inning start) have no recorded jersey, so they stay null → the token shows the base
  // label instead. Recomputed from the event log so it stays correct through undo.
  const baseJerseys = React.useMemo(() => {
    let bases = { '1B': null, '2B': null, '3B': null };
    const apply = (movements, batterNum) => {
      const mentioned = new Set((movements || []).map(m => m.from));
      const next = { '1B': null, '2B': null, '3B': null };
      for (const b of ['1B', '2B', '3B']) {
        if (!mentioned.has(b) && bases[b]) next[b] = bases[b]; // unmentioned = stayed put
      }
      for (const m of (movements || [])) {
        const jersey = m.from === 'home' ? (batterNum || null) : (bases[m.from] || null);
        if (m.to === '1B' || m.to === '2B' || m.to === '3B') next[m.to] = jersey;
        // 'home' (scored) and 'out_at_X' — runner leaves the bases, no assignment.
      }
      bases = next;
    };
    for (const e of (events || [])) {
      if (e.type === 'inning_change') {
        bases = { '1B': null, '2B': null, '3B': null }; // inning-start runners unknown
      } else if (e.type === 'pa_end' && e.runnerMovements) {
        apply(e.runnerMovements, e.batterNumber);
      } else if (e.type === 'between_pitch' && e.runnerMovements) {
        apply(e.runnerMovements, null);
      } else if (e.type === 'pitch' && e.runnerAction) {
        apply(e.runnerAction, null);
      }
    }
    return bases;
  }, [events]);

  const deriveEndStateFromMovements = (priorStateKey, priorOuts, movements) => {
    const occupied = { '1B': false, '2B': false, '3B': false };
    let outsAdded = 0;
    let runsScored = 0;
    for (const m of (movements || [])) {
      if (m.to === 'home')                                      runsScored++;
      else if (typeof m.to === 'string' && m.to.startsWith('out_at_')) outsAdded++;
      else if (m.to === '1B' || m.to === '2B' || m.to === '3B') {
        // Last-write-wins — shouldn't happen in valid input but defensive.
        occupied[m.to] = true;
      }
    }
    // Compose new stateKey from occupied bases. Stable canonical order: 1, 2, 3,
    // 'loaded' when all three.
    let stateKey;
    if (occupied['1B'] && occupied['2B'] && occupied['3B']) stateKey = 'loaded';
    else {
      let s = '';
      if (occupied['1B']) s += '1';
      if (occupied['2B']) s += '2';
      if (occupied['3B']) s += '3';
      stateKey = s === '' ? 'empty' : s;
    }
    const newOuts = Math.min(3, priorOuts + outsAdded);
    return {
      stateKey,
      outs: newOuts,
      runsScored,
      inningEnded: newOuts >= 3,
    };
  };

  // Safety net — never leave the diamond gone with a game in progress. The matrix
  // view was removed, so if expandedCell is ever null while events exist (e.g., the
  // artifact re-mounted when the tagger was closed and reopened), there's no way to
  // click back in. Reconstruct the current base-out state from the event log so the
  // diamond always reappears in the right situation.
  const deriveCurrentCellFromEvents = () => {
    const startKey = gameMeta?.format === 'Wildcard' ? 'loaded' : 'empty';
    if (!events || events.length === 0) return { stateKey: startKey, outs: 0 };
    for (let i = events.length - 1; i >= 0; i--) {
      const e = events[i];
      if (e.type === 'inning_change') {
        return { stateKey: startKey, outs: 0 };
      }
      if ((e.type === 'pa_end' || e.type === 'between_pitch') && e.runnerMovements) {
        const d = deriveEndStateFromMovements(e.stateKey, e.outs, e.runnerMovements);
        return d.inningEnded ? { stateKey: startKey, outs: 0 } : { stateKey: d.stateKey, outs: d.outs };
      }
      if (e.type === 'pitch' || !e.type) {
        if (e.runnerAction) {
          const d = deriveEndStateFromMovements(e.stateKey, e.outs, e.runnerAction);
          return d.inningEnded ? { stateKey: startKey, outs: 0 } : { stateKey: d.stateKey, outs: d.outs };
        }
        if (e.stateKey != null && e.outs != null) return { stateKey: e.stateKey, outs: e.outs };
      }
    }
    return { stateKey: startKey, outs: 0 };
  };

  useEffect(() => {
    if (!isLoaded) return;
    const gameActive = events.length > 0 || (gameMeta && startScreenDismissed);
    if (gameActive && expandedCell == null) {
      setExpandedCell(deriveCurrentCellFromEvents());
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [events, expandedCell, isLoaded, gameMeta, startScreenDismissed]);
  //   1. Runners on base before the play
  //   2. Whether the batter joined the basepaths (pa_end only) or didn't (between_pitch)
  //   3. Outs added during the play
  //   4. Runners on base after the play
  // Math:
  //   people_entered = runners_before + (1 if pa_end else 0)
  //   people_left    = people_entered - runners_after
  //   runs_scored    = people_left - outs_added
  //
  // Returns { side, runs } if any runs scored, or null if 0 runs / no state event.
  // 'side' is which team scored (the batting side of the last state event).
  const computeRunsForDelta = (eventLog, newStateKey, newOuts) => {
    // Find the last state-changing event (pa_end or between_pitch), skipping
    // inning_change / pitcher_change / pitch / score_change.
    let lastStateEvent = null;
    let inningChangedSince = false;
    for (let i = eventLog.length - 1; i >= 0; i--) {
      const e = eventLog[i];
      if (e.type === 'inning_change') {
        // If an inning_change has fired since the last state event, the new cell
        // tap is for the NEXT half-inning — don't compute runs against it.
        inningChangedSince = true;
        continue;
      }
      if (e.type === 'pa_end' || e.type === 'between_pitch') {
        lastStateEvent = e;
        break;
      }
      // Skip any other event types (pitch, pitcher_change, score_change).
    }
    if (!lastStateEvent || inningChangedSince) return null;
    // v1.5 step 4a — If the pa_end already carries runnerMovements, the diamond
    // captured the runs explicitly via getScores(). Skip the state-delta inference
    // to avoid double-counting.
    if (lastStateEvent.runnerMovements && lastStateEvent.runnerMovements.length > 0) return null;

    const runnersBefore = popcountRunners(lastStateEvent.stateKey);
    const runnersAfter = popcountRunners(newStateKey);
    const outsBefore = lastStateEvent.outs;
    const outsAdded = Math.max(0, (newOuts ?? outsBefore) - outsBefore);
    // For pa_end the batter is a new entity on the basepaths during the play.
    // For between_pitch (steal/pickoff) there's no new batter — the same PA continues.
    const batterDelta = (lastStateEvent.type === 'pa_end') ? 1 : 0;
    const peopleEntered = runnersBefore + batterDelta;
    const peopleLeft = peopleEntered - runnersAfter;
    const runs = peopleLeft - outsAdded;
    if (runs <= 0) return null;
    return { side: lastStateEvent.batting, runs };
  };

  // Helper: should the auto-score fire on this cell tap? Only fires when the new
  // cell represents a state change from the previous cell, AND the previous cell
  // (currentCell) state matches the last state-changing event's stateKey. This
  // prevents auto-scoring when the scorer is just browsing cells or correcting
  // an earlier mistake.
  // NOTE: This helper drives the current state-delta auto-score logic. Step 6 will
  // remove it in favor of the diamond-derived runs (count to:'home' in
  // runnerMovements). Kept here for now so the existing flow continues to work
  // through Steps 1-5.
  const shouldAutoScore = (newStateKey, newOuts) => {
    if (!currentCell) return false;
    if (currentCell.stateKey === newStateKey && currentCell.outs === newOuts) return false;
    return true;
  };

  // v1.5 — Step 1 (Diamond runner movement schema & state computation).
  // -----------------------------------------------------------------------------
  // Helpers for converting between the stateKey string ('empty', '1', '12', etc.)
  // and a set of occupied bases, and for computing the new state from a list of
  // runnerMovements. These are pure functions — no React state dependencies — so
  // they can be unit-tested or moved to a util module later without rewrites.

  // Parse a stateKey into an array of occupied base codes ['1B','2B','3B'] subset.
  // Handles the canonical keys: 'empty', '1', '2', '3', '12', '13', '23', 'loaded'.
  const parseStateKey = (stateKey) => {
    if (!stateKey || stateKey === 'empty') return [];
    if (stateKey === 'loaded') return ['1B', '2B', '3B'];
    const out = [];
    const s = String(stateKey);
    if (s.includes('1')) out.push('1B');
    if (s.includes('2')) out.push('2B');
    if (s.includes('3')) out.push('3B');
    return out;
  };

  // Inverse of parseStateKey. Takes a set/array of occupied base codes and returns
  // the canonical stateKey string. Ordered consistently so '12' is always '12' not
  // '21' regardless of input ordering.
  const composeStateKey = (bases) => {
    const set = new Set(bases);
    const has1 = set.has('1B');
    const has2 = set.has('2B');
    const has3 = set.has('3B');
    if (has1 && has2 && has3) return 'loaded';
    let out = '';
    if (has1) out += '1';
    if (has2) out += '2';
    if (has3) out += '3';
    return out || 'empty';
  };

  // Apply a list of runnerMovements to the previous state. Each movement is
  // { from: 'home'|'1B'|'2B'|'3B', to: 'home'|'1B'|'2B'|'3B'|'out' }.
  //   from === 'home' indicates the batter (entering the basepaths).
  //   to === 'home'   indicates the runner scored.
  //   to === 'out'    indicates the runner was out.
  //   Other to values place the runner on that base.
  // Returns { stateKey, outs, runs } for the resulting state.
  //
  // The function does NOT validate that movements are physically possible (e.g.,
  // two runners ending on the same base) — the diamond UI is responsible for
  // preventing those cases. Defensive coding: if two runners land on the same
  // base, the set semantics fold them together (last write wins implicitly).
  const applyRunnerMovements = (prevStateKey, prevOuts, movements) => {
    // Start with the runners currently on base (excluding any who are about to
    // move — we'll re-place them based on movements).
    const movedFroms = new Set(movements.map(m => m.from));
    const initialBases = parseStateKey(prevStateKey).filter(b => !movedFroms.has(b));
    const endingBases = new Set(initialBases);
    let outsAdded = 0;
    let runs = 0;
    for (const m of movements) {
      if (m.to === 'home') runs += 1;
      else if (m.to === 'out') outsAdded += 1;
      else if (m.to === '1B' || m.to === '2B' || m.to === '3B') endingBases.add(m.to);
    }
    return {
      stateKey: composeStateKey(endingBases),
      outs: prevOuts + outsAdded,
      runs,
    };
  };

  // Seed the diamond with default runnerMovements based on the cascade outcome.
  // The scorer can override these in the diamond UI before committing.
  //
  // Hit types pre-advance the batter to the corresponding base AND advance all
  // existing runners the same number of bases (e.g., on a double, every runner
  // moves +2 bases). This is the most useful default for the common case; the
  // scorer adjusts if reality differed (e.g., runner held on a double).
  //
  // Forced advances:
  //   walk / hit_by_pitch — runners forced from 1B → 2B → 3B → home only when
  //                          the preceding base was occupied. Batter → 1B.
  //
  // Outs:
  //   Strikeouts / Out-at-1st / Fly Out / Line Out — batter → 'out', runners
  //                          default to staying put.
  //   Sac Fly — batter → 'out', runner on 3B → home (the sac fly's defining
  //                          movement). Other runners stay put.
  //   Sac Bunt — batter → 'out', runners on 1B and/or 2B advance one base.
  //   Double Play — batter → 'out', one forced runner → 'out' (typically the
  //                          runner on 1B if present; otherwise default to
  //                          stay-put and let the scorer adjust).
  //
  // Reached on error / Fielder's choice / Other — minimal defaults; batter on 1B,
  //                          runners stay put. Scorer is expected to adjust.
  const defaultRunnerMovements = (prevStateKey, category, hitType) => {
    const movements = [];
    const occupied = parseStateKey(prevStateKey);
    const baseOrder = { 'home': 0, '1B': 1, '2B': 2, '3B': 3 };
    const baseAt = ['home', '1B', '2B', '3B'];

    // How many bases does this hit advance everyone by? Null for non-hit categories.
    const hitAdvance = (
      hitType === 'single' ? 1 :
      hitType === 'double' ? 2 :
      hitType === 'triple' ? 3 :
      (hitType === 'home_run' || hitType === 'in_park_hr') ? 4 :
      null
    );

    // ===== Hit categories =====
    if (category === 'hit' && hitAdvance !== null) {
      // Batter advances `hitAdvance` bases from home. 4 = score (home run).
      const batterToIdx = Math.min(4, hitAdvance);
      movements.push({ from: 'home', to: batterToIdx >= 4 ? 'home' : baseAt[batterToIdx] });
      // Each existing runner advances the same number of bases (capped at home).
      for (const base of occupied) {
        const fromIdx = baseOrder[base];
        const toIdx = Math.min(4, fromIdx + hitAdvance);
        movements.push({ from: base, to: toIdx >= 4 ? 'home' : baseAt[toIdx] });
      }
      return movements;
    }

    // ===== Forced advances (Walk, HBP) =====
    if (category === 'walk' || category === 'hit_by_pitch') {
      // Batter goes to 1B regardless. Then forced cascade: 1B→2B only if 1B was
      // occupied, 2B→3B only if both 1B and 2B were occupied, 3B→home only if
      // bases were loaded.
      movements.push({ from: 'home', to: '1B' });
      const had1 = occupied.includes('1B');
      const had2 = occupied.includes('2B');
      const had3 = occupied.includes('3B');
      if (had1) movements.push({ from: '1B', to: '2B' });
      if (had1 && had2) movements.push({ from: '2B', to: '3B' });
      if (had1 && had2 && had3) movements.push({ from: '3B', to: 'home' });
      // Non-forced runners stay put (no movement entry needed).
      return movements;
    }

    // ===== Sac Fly =====
    if (category === 'sac_fly') {
      movements.push({ from: 'home', to: 'out' });
      // Runner on 3B scores. Other runners default to stay-put (scorer adjusts
      // if a tag-up advance happened from 2B too).
      if (occupied.includes('3B')) movements.push({ from: '3B', to: 'home' });
      return movements;
    }

    // ===== Sac Bunt =====
    if (category === 'sac_bunt') {
      movements.push({ from: 'home', to: 'out' });
      // Successful sacrifice — every runner advances one base. 3B → home scores (the
      // batter is out at first, not a force, so the run counts). Scorer can revise.
      if (occupied.includes('1B')) movements.push({ from: '1B', to: '2B' });
      if (occupied.includes('2B')) movements.push({ from: '2B', to: '3B' });
      if (occupied.includes('3B')) movements.push({ from: '3B', to: 'home' });
      return movements;
    }

    // ===== Double Play =====
    if (category === 'double_play') {
      movements.push({ from: 'home', to: 'out' });
      // Default: forced runner on 1B is the second out. Scorer adjusts for other
      // DP types (e.g., 5-4-3 with bases loaded → runner on 2B forced at 3B but
      // the scorer can revise).
      if (occupied.includes('1B')) movements.push({ from: '1B', to: 'out' });
      return movements;
    }

    // ===== Other batter-out categories =====
    if (
      category === 'strikeout_called' ||
      category === 'strikeout_swinging' ||
      category === 'dropped_third_strike' ||
      category === 'out_at_1st' ||
      category === 'fly_out' ||
      category === 'line_out'
    ) {
      movements.push({ from: 'home', to: 'out' });
      return movements;
    }

    // ===== Non-batter-out, non-hit categories (FC, ROE, Other) =====
    if (category === 'fielders_choice') {
      // Default: batter reaches 1B safely, runner on 1B → out. Scorer will adjust
      // for FC plays where a different runner was chosen.
      movements.push({ from: 'home', to: '1B' });
      if (occupied.includes('1B')) movements.push({ from: '1B', to: 'out' });
      return movements;
    }
    if (category === 'reached_on_error') {
      // Batter on 1B by default. Runners stay put — the error type determines
      // additional advancement, which the scorer captures via the diamond.
      movements.push({ from: 'home', to: '1B' });
      return movements;
    }

    // ===== Fallback (Other, batter_interference) =====
    // Best-effort default: batter on 1B, no other movement. Scorer adjusts.
    movements.push({ from: 'home', to: '1B' });
    return movements;
  };

  // Logs a single pitch event in the currently expanded cell.
  // Stays in the cell — does NOT auto-return.
  // v1.2 cap rule: balls cap at 3, strikes cap at 2. Taps that would exceed
  // the cap are rejected entirely (no event logged). Walk and Strikeout PA-ends
  // must be tapped explicitly. Foul at 2 strikes is silently absorbed (no count change,
  // but the pitch IS still logged for cell-count purposes — it was a real pitch).
  const handlePitchTap = useCallback((pitchKey, detail = null) => {
    if (!expandedCell) return;
    const { balls, strikes } = getExpandedCellCount();
    // Reject taps that would push the legal count past its boundary.
    if ((pitchKey === 'ball' || pitchKey === 'intentional_ball') && balls >= 3) return;
    if ((pitchKey === 'called_strike' || pitchKey === 'swing_miss' || pitchKey === 'bunt_strike') && strikes >= 2) return;
    // Foul (regular or bunt) is always allowed — it's a real pitch even if it doesn't increment strikes at 2.

    const { stateKey, outs } = expandedCell;
    const newEvent = {
      type: 'pitch',
      pitch: pitchKey,
      stateKey,
      outs,
      batting: battingSide,
      batterNumber: getCurrentBatterNumber(),
      timestamp: Date.now(),
    };
    // Optional scorer-observed pitch detail. Both fields are estimates from the
    // stands, not measured readings — pitchType is the tagger's read, veloEstimate
    // is a scroller guess. Recorded only when Pitch Detail mode is on, and either
    // field can be absent if the scorer skipped it.
    if (detail) {
      if (detail.pitchType) newEvent.pitchType = detail.pitchType;
      if (detail.velo != null) newEvent.veloEstimate = detail.velo;
      newEvent.veloSource = 'scorer_estimate';
    }
    setEvents(prev => [...prev, newEvent]);
    setCurrentCell({ stateKey, outs });
    setExportedAt(null);
    setLastTap(`${stateKey}-${outs}`);
    setTimeout(() => setLastTap(null), 180);
  }, [expandedCell, battingSide, events, gameMeta]);

  // Logs a PA-end event in the currently expanded cell.
  // Flashes the tapped button for ~150ms, then auto-returns to the matrix.
  // v1.3 implicit-pitch rule: EVERY PA-end logs an implicit closing pitch.
  // The closing-pitch TYPE depends on the category, because different PA-end
  // categories tell us different things about how the PA ended:
  //   walk                 → closing pitch is `ball`         (ball 4)
  //   strikeout_called     → closing pitch is `called_strike` (caught looking on K3)
  //   strikeout_swinging   → closing pitch is `swing_miss`    (swung through K3)
  //   dropped_third_strike → closing pitch is `swing_miss`    (typically a missed swing
  //                          the catcher couldn't handle; called K3 dropped third
  //                          strikes also exist but are rare; logging swing_miss for
  //                          v1.3 — can refine later if a finer split is needed)
  //   hit, out, fielding_error, other → closing pitch is `in_play` (live-ball outcome)
  // Note: walk's implicit ball bypasses the cap — the scorer is declaring the walk
  // happened, which means a 4th ball was thrown regardless of the count display.
  // v1.4: PA-end can carry an `extras` object with sub-classification fields, currently
  // { battedBall: 'ground_ball' | 'fly_ball' | 'line_drive' | 'bunt' } when the
  // outcome came from the Ball-in-Play cascade. Empty/absent for non-BIP categories.
  const handlePaEndTap = useCallback((category, extras = {}) => {
    if (!expandedCell) return;
    const { stateKey, outs } = expandedCell;
    setFlashingButton({ row: 'pa', key: category });

    // Closing-pitch type per category. v1.4: hit / out / reached_on_error all log
    // an in_play closing pitch (they came through the Ball-in-Play cascade so a real
    // ball was put in play). Walk → ball. Strikeout types → called or swing-miss.
    // Dropped third strike → swing-miss. Other → in_play (could be HBP etc; in_play is
    // a reasonable default since the PA ended on a real pitch the batter didn't take).
    // Intentional walk → null (no pitch logged at all — IBB is a no-pitch event in
    // youth baseball where the batter is awarded 1B without anything thrown).
    let closingPitch;
    if (category === 'intentional_walk' || category === 'batter_out') closingPitch = null;
    else if (category === 'walk') closingPitch = 'ball';
    else if (category === 'hit_by_pitch') closingPitch = 'hbp';
    else if (category === 'strikeout_called') closingPitch = 'called_strike';
    else if (category === 'strikeout_swinging' || category === 'dropped_third_strike') closingPitch = 'swing_miss';
    else closingPitch = 'in_play';

    // v1.5 step 5 (partial) — Auto-derive runnerMovements for categories that don't
    // route through the diamond (walks, HBP, strikeouts). These are deterministic
    // from the pre-play state, so we can compute them here and benefit from the
    // same auto-advance behavior as diamond-committed plays.
    // The diamond's seedRunnerPlacements has the same logic; we mirror the relevant
    // branches here so the resulting pa_end events are uniform.
    if (!extras.runnerMovements) {
      const has1 = stateKey === 'loaded' || (stateKey !== 'empty' && stateKey.includes('1'));
      const has2 = stateKey === 'loaded' || (stateKey !== 'empty' && stateKey.includes('2'));
      const has3 = stateKey === 'loaded' || (stateKey !== 'empty' && stateKey.includes('3'));
      let derived = null;
      if (category === 'walk' || category === 'intentional_walk' || category === 'hit_by_pitch') {
        // Forced runners only. Intentional walks use the same forced-advance logic
        // as regular walks — the difference is just the absence of a closing pitch.
        derived = [];
        if (has1) derived.push({ from: '1B', to: '2B' });
        if (has2) derived.push({ from: '2B', to: has1 ? '3B' : '2B' });
        if (has3) derived.push({ from: '3B', to: (has1 && has2) ? 'home' : '3B' });
        derived.push({ from: 'home', to: '1B' });
      } else if (category === 'strikeout_called' || category === 'strikeout_swinging' || category === 'batter_out') {
        // Batter out at home plate, runners stay. Batter_out covers interference
        // calls where the batter is out and runners return to their base.
        derived = [];
        if (has1) derived.push({ from: '1B', to: '1B' });
        if (has2) derived.push({ from: '2B', to: '2B' });
        if (has3) derived.push({ from: '3B', to: '3B' });
        derived.push({ from: 'home', to: 'out_at_home' });
      } else if (category === 'hit' &&
                 (extras.hitType === 'home_run' || extras.hitType === 'in_park_hr')) {
        // Home run — every runner including the batter scores. Reached here when
        // the HR leaf bypassed the diamond (noFielders short-circuit).
        derived = [];
        if (has1) derived.push({ from: '1B', to: 'home' });
        if (has2) derived.push({ from: '2B', to: 'home' });
        if (has3) derived.push({ from: '3B', to: 'home' });
        derived.push({ from: 'home', to: 'home' });
      }
      // 'other' is intentionally left out — too ambiguous to default; scorer can
      // navigate manually for unusual cases.
      if (derived) extras = { ...extras, runnerMovements: derived };
    }

    const now = Date.now();
    const paEvent = {
      type: 'pa_end',
      category, // see PA-end category enumeration (v1.5)
      stateKey,
      outs,
      batting: battingSide,
      batterNumber: getCurrentBatterNumber(),
      timestamp: now + 1,
    };
    // Spread extras onto the pa_end event when present.
    //   battedBall      — batted-ball type from BIP cascade (ground_ball, fly_ball, etc.)
    //   hitType         — hit base advancement (single, double, triple, in_park_hr)
    //   fielderSequence — array of position codes in order touched/caught (v1.5 step 6)
    //   runnerMovements — array of {from, to} per affected runner including batter
    //                     (v1.5 step 7 — diamond runner placement; replaces matrix tap
    //                     as source of truth for end state). `from` is one of
    //                     'home' | '1B' | '2B' | '3B' (where the runner was), `to` is
    //                     'home' | '1B' | '2B' | '3B' | 'out' (where they ended up).
    //                     Batter movements always have from === 'home'.
    if (extras.battedBall) paEvent.battedBall = extras.battedBall;
    if (extras.hitType) paEvent.hitType = extras.hitType;
    if (extras.fielderSequence && extras.fielderSequence.length > 0) {
      paEvent.fielderSequence = extras.fielderSequence;
    }
    if (extras.runnerMovements && extras.runnerMovements.length > 0) {
      paEvent.runnerMovements = extras.runnerMovements;
    }
    if (extras.forceoutBase) {
      paEvent.forceoutBase = extras.forceoutBase;
    }
    // v1.5 — When a runner action (stolen base / caught stealing / battery error)
    // coincided with the PA-ending pitch, the runner action is folded into this
    // pa_end (no separate between_pitch event). runnerActionType marks the type
    // for downstream analytics (e.g. "K + steal of 2nd").
    if (extras.runnerActionType) {
      paEvent.runnerActionType = extras.runnerActionType;
    }

    // Build event list. For intentional walks the closing pitch is null — no pitch
    // event is logged (IBB is a no-pitch event in youth ball).
    const newEvents = closingPitch === null
      ? [paEvent]
      : [
          {
            type: 'pitch',
            pitch: closingPitch,
            stateKey,
            outs,
            batting: battingSide,
            batterNumber: getCurrentBatterNumber(),
            timestamp: now,
            // Scorer-observed detail for the PA-ending pitch, when Pitch Detail mode
            // collected it before the result was committed.
            ...(extras.pitchType ? { pitchType: extras.pitchType } : {}),
            ...(extras.velo != null ? { veloEstimate: extras.velo, veloSource: 'scorer_estimate' } : {}),
          },
          paEvent,
        ];
    setEvents(prev => [...prev, ...newEvents]);
    // v1.2: Drop the current-cell highlight on PA-end. The matrix should not
    // visually point the scorer to the just-active cell as a suggestion for
    // the next state — the scorer should pick where to go next.
    setCurrentCell(null);
    setExportedAt(null);

    // Pre-compute whether this PA ends the half-inning. We need this before the
    // lineup prompt check so we can suppress the prompt on inning-ending PAs —
    // the scorer can't answer "is the next batter the leadoff?" because the next
    // batter is on the OTHER team. The full state derivation runs below.
    const hasRunnerMovements = extras.runnerMovements && extras.runnerMovements.length > 0;
    let paInningEnded = false;
    if (hasRunnerMovements) {
      const _derivedForPrompt = deriveEndStateFromMovements(stateKey, outs, extras.runnerMovements);
      paInningEnded = _derivedForPrompt.inningEnded;
    }

    // Lineup-confirmation prompt: after a PA ends, if the side's lineup isn't yet
    // resolved AND the *next* batter would be slot 10+, ask "Is this the leadoff?"
    // Cap at proposed lineup size 15 — past that, fall back to manual marker entry.
    // On inning-ending PAs, DEFER the check — the scorer can't answer until the
    // team bats again. The deferred check fires via an effect on battingSide.
    (() => {
      const lineupSizeStr = battingSide === 'home'
        ? (gameMeta?.homeLineupSize || gameMeta?.lineupSize)
        : (gameMeta?.awayLineupSize || gameMeta?.lineupSize);
      if (lineupSizeStr) return;
      let sidePaEnds = 0;
      let sideMarkers = 0;
      let paAtLastMarker = 0;
      const all = [...events, ...newEvents];
      for (const e of all) {
        if (e.type === 'lineup_marker' && e.side === battingSide) {
          sideMarkers++;
          paAtLastMarker = sidePaEnds;
        }
        if (e.type === 'pa_end' && e.batting === battingSide) sidePaEnds++;
      }
      if (sideMarkers >= 2) return;
      const paSinceMarker = sidePaEnds - paAtLastMarker;
      const nextSlot = paSinceMarker + 1;
      if (nextSlot >= 10 && nextSlot <= 15) {
        if (paInningEnded) {
          // Defer — can't answer now, other team is about to bat
          setPendingLineupCheck({ side: battingSide, proposedSize: nextSlot - 1 });
        } else {
          setLineupPrompt({ side: battingSide, proposedSize: nextSlot - 1 });
        }
      }
    })();

    // v1.5 step 4b + auto-swipe — Compute next state. Three paths:
    //   1. inningEnded     → fire inning_change, flip battingSide, reset baselines,
    //                        auto-land in starting cell of new half-inning.
    //                        (Scorer can still undo to revert.)
    //   2. not inningEnded → auto-advance into derived cell, scorer continues.
    //   3. no runnerMovements (legacy walks/HBP/strikeouts pre-step-5) →
    //      return to matrix (existing behavior).
    let nextExpandedCell = null;
    let inningEnded = paInningEnded;
    if (hasRunnerMovements && !paInningEnded) {
      const derived = deriveEndStateFromMovements(stateKey, outs, extras.runnerMovements);
      nextExpandedCell = { stateKey: derived.stateKey, outs: derived.outs };
    }

    if (inningEnded) {
      // Auto-swipe: flip side, fire the inning_change event, reset baselines, land in
      // the new starting cell. The slider visually follows the battingSide state.
      const newSide = battingSide === 'away' ? 'home' : 'away';
      const startingStateKey = gameMeta?.format === 'Wildcard' ? 'loaded' : 'empty';
      setBattingSide(newSide);
      setEvents(prev => [...prev, {
        type: 'inning_change',
        newBattingSide: newSide,
        reason: 'three_outs',
        timestamp: now + 2, // after pa_end and its paired pitch
      }]);
      setCellBaselines(() => {
        const next = {};
        getBaseStatesForFormat(gameMeta?.format).forEach(state => {
          OUT_COLUMNS.forEach(outs => {
            next[`${state.key}-${outs}`] = events.length + newEvents.length + 1; // pa_end + inning_change
          });
        });
        return next;
      });
      nextExpandedCell = { stateKey: startingStateKey, outs: 0 };
    }

    setTimeout(() => {
      setFlashingButton(null);
      setExpandedCell(nextExpandedCell);
    }, 150);
  }, [expandedCell, battingSide, gameMeta, events.length]);

  // Manual half-inning flip (practice / pitch-count mode). In scrimmages, an inning
  // can end on a pitch-count or run limit rather than 3 outs. This fires an
  // inning_change with an explicit reason so practice innings stay distinguishable
  // from real (three_outs) innings in the data. Mirrors the auto-flip logic.
  const endHalfInning = useCallback((reason) => {
    const newSide = battingSide === 'away' ? 'home' : 'away';
    const startingStateKey = gameMeta?.format === 'Wildcard' ? 'loaded' : 'empty';
    const now = Date.now();
    setBattingSide(newSide);
    setEvents(prev => [...prev, {
      type: 'inning_change',
      newBattingSide: newSide,
      reason: reason || 'manual',
      timestamp: now,
    }]);
    setCellBaselines(() => {
      const next = {};
      getBaseStatesForFormat(gameMeta?.format).forEach(state => {
        OUT_COLUMNS.forEach(outs => {
          next[`${state.key}-${outs}`] = events.length + 1; // +1 for the inning_change
        });
      });
      return next;
    });
    setCurrentCell(null);
    setExportedAt(null);
    setExpandedCell({ stateKey: startingStateKey, outs: 0 });
  }, [battingSide, gameMeta, events.length]);

  // Logs a runner-advance event in the currently expanded cell.
  // If extras.runnerMovements is absent (legacy path), fall back to lead-runner
  // default derivation — preserved for any code path that hasn't yet routed through
  // the diamond.
  const handleRunnerAdvanceTap = useCallback((category, extras = {}) => {
    if (!expandedCell) return;
    const { stateKey, outs } = expandedCell;
    setFlashingButton({ row: 'runner', key: category });

    // All call paths go through the diamond, which produces runnerMovements
    // explicitly. We trust the diamond's output rather than re-deriving here.
    // (Legacy lead-runner-default fallback was removed in v1.5 cleanup — it
    // never fired in practice and contradicted the "no base-running assumptions"
    // policy if it had.)
    const runnerMovements = extras.runnerMovements || null;

    const now = Date.now();

    // Build the events to append. Two paths:
    //   1. A pitch was captured (SB/CS/BE/DI via sub-cascade): fold the runner
    //      action INTO the pitch event as attributes (runnerAction, runnerActionType,
    //      fielderSequence). No separate between_pitch — one event per play.
    //   2. No pitch (pickoff, balk, DI-no-pitch, other): standalone between_pitch.
    const newEvents = [];
    if (extras.pitch) {
      // Pitch with runner action folded in
      const pitchEvent = {
        type: 'pitch',
        pitch: extras.pitch,
        stateKey,
        outs,
        batting: battingSide,
        batterNumber: getCurrentBatterNumber(),
        runnerActionType: category,
        timestamp: now,
      };
      if (runnerMovements && runnerMovements.length > 0) {
        pitchEvent.runnerAction = runnerMovements;
      }
      if (extras.fielderSequence && extras.fielderSequence.length > 0) {
        pitchEvent.fielderSequence = extras.fielderSequence;
      }
      newEvents.push(pitchEvent);
    } else {
      // No pitch — standalone between_pitch event
      const betweenPitchEvent = {
        type: 'between_pitch',
        category,
        stateKey,
        outs,
        batting: battingSide,
        timestamp: now,
      };
      if (runnerMovements && runnerMovements.length > 0) {
        betweenPitchEvent.runnerMovements = runnerMovements;
      }
      if (extras.fielderSequence && extras.fielderSequence.length > 0) {
        betweenPitchEvent.fielderSequence = extras.fielderSequence;
      }
      newEvents.push(betweenPitchEvent);
    }

    setEvents(prev => [...prev, ...newEvents]);
    setCurrentCell(null);
    setExportedAt(null);

    // Compute next cell from derived state, mirroring the pa_end auto-advance path.
    // Three paths:
    //   1. inningEnded     → auto-swipe slider (fire inning_change, flip battingSide,
    //                        reset baselines, land in starting cell)
    //   2. not inningEnded → auto-advance into derived cell
    //   3. no runnerMovements (e.g. 'other' with no manual placement) → return to matrix
    let nextExpandedCell = null;
    let inningEnded = false;
    if (runnerMovements && runnerMovements.length > 0) {
      const derived = deriveEndStateFromMovements(stateKey, outs, runnerMovements);
      if (derived.inningEnded) {
        inningEnded = true;
      } else {
        nextExpandedCell = { stateKey: derived.stateKey, outs: derived.outs };
      }
    }

    if (inningEnded) {
      const newSide = battingSide === 'away' ? 'home' : 'away';
      const startingStateKey = gameMeta?.format === 'Wildcard' ? 'loaded' : 'empty';
      setBattingSide(newSide);
      setEvents(prev => [...prev, {
        type: 'inning_change',
        newBattingSide: newSide,
        reason: 'three_outs',
        timestamp: now + 2,
      }]);
      setCellBaselines(() => {
        const next = {};
        getBaseStatesForFormat(gameMeta?.format).forEach(state => {
          OUT_COLUMNS.forEach(outs => {
            next[`${state.key}-${outs}`] = events.length + newEvents.length + 1; // between_pitch + inning_change (+ pitch if present, already counted)
          });
        });
        return next;
      });
      nextExpandedCell = { stateKey: startingStateKey, outs: 0 };
    }

    setTimeout(() => {
      setFlashingButton(null);
      setExpandedCell(nextExpandedCell);
    }, 150);
  }, [expandedCell, battingSide, gameMeta, events.length]);

  const handleUndo = () => {
    if (events.length === 0) return;
    // v1.2 pair-aware undo: every PA-end logs an implicit closing pitch immediately
    // before it (timestamp +1ms apart). When the user taps Undo on a pa_end, they
    // expect both events to be removed in a single undo — otherwise undoing a Walk
    // takes two undo taps, and the intermediate state is incoherent (a stray ball
    // pitch with no PA-end after it). Detect the pair and pop both together.
    // v1.5 auto-swipe: 3rd-out commits auto-fire an inning_change after the pa_end.
    // Extend the pair logic to pop the inning_change too so undo reverts the whole
    // third-out commit atomically.
    // Intentional walk has no closing pitch, so the pair is just pa_end alone (or
    // pa_end + inning_change if it produced a 3rd out — unusual but possible if
    // bases loaded IBB scored a run AND was somehow the 3rd out; not realistic but
    // the logic stays correct).
    const lastEvent = events[events.length - 1];
    let popCount = 1;
    // When undo reverses an inning_change (a third-out commit), we must re-open the
    // cell where that third out actually happened — otherwise the scorer is stranded
    // in the new half-inning's starting cell. Capture it here from the play beneath
    // the inning_change. Stays null for all other undos (which intentionally keep the
    // current expanded cell so the scorer can immediately re-tap to correct).
    let restoreCellAfterInningUndo = null;
    // Step 1: detect auto-fired inning_change on top of a pa_end pair.
    if (lastEvent.type === 'inning_change' && events.length >= 2) {
      const beneath = events[events.length - 2];
      // The play that produced the third out carries the cell it happened in.
      // A folded pitch (type='pitch' with runnerAction) can also produce a 3rd out
      // (e.g., caught stealing), so include it alongside pa_end and between_pitch.
      if ((beneath.type === 'pa_end' || beneath.type === 'between_pitch' || (beneath.type === 'pitch' && beneath.runnerAction)) && beneath.stateKey) {
        restoreCellAfterInningUndo = { stateKey: beneath.stateKey, outs: beneath.outs };
      }
      if (beneath.type === 'pa_end' || beneath.type === 'between_pitch' || (beneath.type === 'pitch' && beneath.runnerAction)) {
        popCount = 2;
        // Step 2: if the pa_end has a paired closing pitch beneath it, pop that too.
        if (beneath.type === 'pa_end' && events.length >= 3) {
          const prior = events[events.length - 3];
          const isPaired =
            (prior.type === 'pitch' || !prior.type) &&
            prior.batting === beneath.batting &&
            prior.stateKey === beneath.stateKey &&
            prior.outs === beneath.outs &&
            Math.abs((beneath.timestamp || 0) - (prior.timestamp || 0)) <= 50;
          if (isPaired) popCount = 3;
        }
      }
    } else if (lastEvent.type === 'pa_end' && events.length >= 2) {
      const prior = events[events.length - 2];
      const isPaired =
        (prior.type === 'pitch' || !prior.type) &&
        prior.batting === lastEvent.batting &&
        prior.stateKey === lastEvent.stateKey &&
        prior.outs === lastEvent.outs &&
        Math.abs((lastEvent.timestamp || 0) - (prior.timestamp || 0)) <= 50;
      if (isPaired) popCount = 2;
    }
    const newLength = events.length - popCount;
    const remainingEvents = events.slice(0, newLength);
    setEvents(remainingEvents);
    setExportedAt(null);

    // Retreat the persistent highlight to the most recent prior pitch event's cell, if any
    const lastPitch = [...remainingEvents].reverse().find(e => e.type === 'pitch' || !e.type);
    if (lastPitch) {
      setCurrentCell({ stateKey: lastPitch.stateKey, outs: lastPitch.outs });
    } else {
      setCurrentCell(null);
    }

    // Reconstruct cellBaselines from scratch by replaying the cell-change logic
    // over the remaining events. This avoids the bug where baselines accumulate
    // and don't roll back as the user undoes through the event history.
    const newBaselines = {};
    let lastCell = null;
    remainingEvents.forEach((evt, idx) => {
      if (evt.type === 'pitch' || !evt.type) {
        // If this pitch moved to a different cell, the previous cell's baseline gets bumped
        if (lastCell && (lastCell.stateKey !== evt.stateKey || lastCell.outs !== evt.outs)) {
          const prevCellId = `${lastCell.stateKey}-${lastCell.outs}`;
          newBaselines[prevCellId] = idx;
        }
        lastCell = { stateKey: evt.stateKey, outs: evt.outs };
      } else if (evt.type === 'pitcher_change') {
        // Pitching change bumps the current cell's baseline (new pitcher fresh count)
        if (lastCell) {
          const cellId = `${lastCell.stateKey}-${lastCell.outs}`;
          newBaselines[cellId] = idx;
        }
      } else if (evt.type === 'inning_change') {
        // Inning change bumps every cell's baseline. We approximate by bumping for any
        // cells that have a value or are currently in use; for simplicity, bump all known cells.
        getBaseStatesForFormat(gameMeta?.format).forEach(state => {
          OUT_COLUMNS.forEach(outs => {
            newBaselines[`${state.key}-${outs}`] = idx;
          });
        });
        lastCell = null;
      }
    });
    setCellBaselines(newBaselines);

    // Remove any pitcher-change records that point past the new event count
    setPitcherChanges(prev => ({
      home: prev.home.filter(idx => idx <= newLength),
      away: prev.away.filter(idx => idx <= newLength),
    }));

    // Sync the puck/battingSide to whatever the remaining events imply.
    // Walk the remaining events from the end, looking for the most recent event
    // that tells us the batting side. This handles undoing an inning_change
    // (puck slides back to the prior side) and any other case where the
    // remaining log implies a different side than is currently displayed.
    let inferredSide = null;
    for (let i = remainingEvents.length - 1; i >= 0; i--) {
      const evt = remainingEvents[i];
      if (evt.type === 'inning_change' && evt.newBattingSide) {
        inferredSide = evt.newBattingSide;
        break;
      }
      if (evt.batting) {
        inferredSide = evt.batting;
        break;
      }
    }
    if (inferredSide) {
      setBattingSide(inferredSide);
    } else {
      // No remaining events imply a side — back to the game's default
      setBattingSide('away');
    }

    // Always restore expandedCell to the cell of the most recent remaining event.
    // Since the matrix is gone and the diamond is always on screen, undo is a true
    // "rewind" — the diamond must show the base-out state before the undone play.
    // For inning-change undos, restoreCellAfterInningUndo takes precedence (it
    // carries the exact third-out cell). For all other undos, we derive from the
    // most recent event that carries stateKey.
    if (restoreCellAfterInningUndo) {
      setExpandedCell(restoreCellAfterInningUndo);
      setCurrentCell(restoreCellAfterInningUndo);
    } else {
      // Restore to the state the UNDONE action was tagged in — i.e., the first popped
      // event (events[newLength]). This is the key fix for the "undo cascades back a
      // whole play" bug: a pitch tagged AFTER a prior pa_end carries the post-play
      // base-out state, so undoing just that pitch should leave the diamond in the
      // post-play state (runners where they ended up) — NOT snap back to the pa_end's
      // recorded starting state. Falling back to the most-recent-remaining event would
      // read a pa_end's *starting* stateKey and visually revert the prior play.
      let restoreCell = null;
      const firstUndone = events[newLength];
      if (firstUndone && firstUndone.stateKey != null && firstUndone.outs != null) {
        restoreCell = { stateKey: firstUndone.stateKey, outs: firstUndone.outs };
      } else {
        // The undone event has no base-out state (pitcher_change, etc.) — fall back to
        // the most recent remaining event that does.
        for (let i = remainingEvents.length - 1; i >= 0; i--) {
          const evt = remainingEvents[i];
          if (evt.stateKey != null && evt.outs != null) {
            restoreCell = { stateKey: evt.stateKey, outs: evt.outs };
            break;
          }
        }
      }
      if (restoreCell) {
        setExpandedCell(restoreCell);
        setCurrentCell(restoreCell);
      }
    }
    setFlashingButton(null);
  };

  // Pitching change — log that a new pitcher entered for the team currently on defense.
  // The team on defense is the OPPOSITE of the team currently batting.
  // Also bumps the current cell's display baseline so the visible count resets to zero
  // for the new pitcher. The highlight stays on the same cell since the situation hasn't changed.

  // v1.2 note: hasUncapturedPA / handleNewBatter / handlePromptAnswer / handlePromptSkip
  // were removed in the cell-expansion rewrite. PA-end events are captured directly
  // via handlePaEndTap() inside the expanded cell view. The "uncaptured PA" concept
  // no longer exists — pitches without a pa_end after them are just orphan pitches
  // in their cell, which is fine for matrix counts and acceptable for v1.2 analytics.

  const handleEndGame = () => {
    if (events.length === 0 && !gameMeta) return;
    if (!endArmed) {
      setEndArmed(true);
      setTimeout(() => setEndArmed(false), 5000);
      // v1.2: no PA-prompt safety net. If the game ends with orphan pitches (final batter
      // never had a PA-end captured), they remain orphan pitches in the log. Acceptable.
      return;
    }
    setEvents([]);
    setGameMeta(null);
    setExportedAt(null);
    setBattingSide('away');
    setCurrentCell(null);
    setPitcherChanges({ home: [], away: [] });
    setCellBaselines({});
    setStartScreenDismissed(false);
    setExpandedCell(null);
    setFlashingButton(null);
    setEmailPayload(null);
    setSavePerspectiveChoice('idle');
    setRosters({ home: [], away: [] });
    setPitcherRosters({ home: [], away: [] });
    setRosterEditor(null);
    setLineupPanelOpen(false);
    setLineupTeamView(null);
    setSetupForm({
      sport: '',
      format: '',
      homeTeam: '',
      awayTeam: '',
      ageDivision: '',
      host: '',
      homeLineupSize: '',
      awayLineupSize: '',
    });
    setEndArmed(false);
    setPreviewUrl(null);
  };

  // True when there are pitches recorded but the current state has not been exported
  const isUnsaved = events.length > 0 && !exportedAt;

  // Email preview state — shown inline because programmatic mailto: is blocked in sandboxed iframes
  const [emailPayload, setEmailPayload] = useState(null);
  const [emailFlash, setEmailFlash] = useState(false);
  const [copyFlash, setCopyFlash] = useState(null);
  const emailPanelRef = useRef(null);
  // 'idle' | 'choosing' — when 'choosing', user picks which team's perspective to save

  // Slugify team names for filename safety: lowercase, alphanumerics + hyphens.
  const slugify = (s) => (s || 'team')
    .toString()
    .toLowerCase()
    .trim()
    .replace(/[^a-z0-9]+/g, '_')
    .replace(/^_+|_+$/g, '') || 'team';

  // Build the share payload. Used by Web Share API: a summary in the body text
  // for at-a-glance verification, plus a JSON file attached for canonical data.
  // The image attachment was retired — the JSON carries everything LoopHohl needs.
  const buildShareData = () => {
    const dateStr = new Date().toISOString().slice(0, 10);
    const matchup = gameMeta
      ? `${gameMeta.awayTeam} at ${gameMeta.homeTeam}`
      : 'Untitled Game';
    const subject = gameMeta
      ? `Wildcard Fingerprint — ${matchup} — ${gameMeta.sport} ${gameMeta.ageDivision} ${gameMeta.format} — ${dateStr}`
      : `Wildcard Fingerprint — ${dateStr}`;

    const homeBattingCount = pitchEvents.filter(e => e.batting === 'home').length;
    const awayBattingCount = pitchEvents.filter(e => e.batting === 'away').length;
    const paEndCount = events.filter(e => e.type === 'pa_end').length;
    const betweenPitchCount = events.filter(e => e.type === 'between_pitch').length;

    // Compute line score from runnerMovements (counts `to:'home'` per half-inning
    // plus score_change deltas). Walks the event log once.
    const lineScore = (() => {
      const halves = [];
      let current = { side: null, runs: 0 };
      for (const e of events) {
        if (e.type === 'inning_change') {
          if (current.side !== null) halves.push(current);
          current = { side: e.newBattingSide, runs: 0 };
          continue;
        }
        if (e.batting && current.side === null) current.side = e.batting;
        if ((e.type === 'pa_end' || e.type === 'between_pitch') && e.runnerMovements) {
          for (const m of e.runnerMovements) {
            if (m.to === 'home') current.runs++;
          }
        }
        if (e.type === 'pitch' && e.runnerAction) {
          for (const m of e.runnerAction) {
            if (m.to === 'home') current.runs++;
          }
        }
        if (e.type === 'score_change' && e.side === current.side) {
          current.runs += (e.delta || 0);
        }
      }
      if (current.side !== null) halves.push(current);
      let home = 0, away = 0;
      const lines = [];
      halves.forEach((h, i) => {
        const inning = Math.floor(i / 2) + 1;
        const half = i % 2 === 0 ? 'Top' : 'Bot';
        const team = h.side === 'home' ? (gameMeta?.homeTeam || 'home') : (gameMeta?.awayTeam || 'away');
        lines.push(`  ${half} ${inning}: ${team} ${h.runs}`);
        if (h.side === 'home') home += h.runs;
        else if (h.side === 'away') away += h.runs;
      });
      return { lines: lines.join('\n'), home, away };
    })();

    const summaryText = [
      'WildCard fingerprint',
      '====================',
      gameMeta ? `${gameMeta.awayTeam} ${lineScore.away} — ${lineScore.home} ${gameMeta.homeTeam}` : '',
      gameMeta ? `${gameMeta.sport}   ${gameMeta.ageDivision}   ${gameMeta.format}   ${gameMeta.host}` : '',
      gameMeta ? `Date: ${dateStr}` : '',
      '',
      `Pitches: ${pitchEvents.length} (${awayBattingCount} ${gameMeta?.awayTeam || 'away'} batting   ${homeBattingCount} ${gameMeta?.homeTeam || 'home'} batting)`,
      `Plate appearances: ${paEndCount}   Between-pitch events: ${betweenPitchCount}`,
      '',
      'Line score:',
      lineScore.lines || '  (no innings tagged)',
      '',
      '— Full event-level data in attached JSON.',
    ].filter(l => l !== null && l !== undefined).join('\n');

    const jsonPayload = {
      schema: 'loophohl-fingerprint-v1',
      capturedAt: new Date().toISOString(),
      game: gameMeta || null,
      totals: {
        pitches: pitchEvents.length,
        homeBatting: homeBattingCount,
        awayBatting: awayBattingCount,
        plateAppearances: paEndCount,
        betweenPitchEvents: betweenPitchCount,
      },
      events: events,
    };

    const awaySlug = slugify(gameMeta?.awayTeam);
    const homeSlug = slugify(gameMeta?.homeTeam);
    const filename = `wildcard_${dateStr}_${awaySlug}_at_${homeSlug}.json`;

    return { subject, summaryText, jsonPayload, filename };
  };

  const handleShareFingerprint = async () => {
    if (events.length === 0) return;
    const { subject, jsonPayload, filename } = buildShareData();
    const jsonString = JSON.stringify(jsonPayload, null, 2);

    // Real origin (app.loophohl.com): the Web Share API can carry an actual file, so
    // the JSON goes out as a genuine .json attachment. This is the fix for the mail
    // body truncating around 56KB and silently dropping the tail of a long game.
    try {
      const file = new File([jsonString], filename, { type: 'application/json' });
      if (navigator.canShare && navigator.canShare({ files: [file] })) {
        await navigator.share({ files: [file], title: subject });
        setExportedAt(new Date().toISOString());
        setEmailFlash(true);
        setTimeout(() => setEmailFlash(false), 1500);
        return;
      }
    } catch (err) {
      // User dismissed the share sheet — not an error, just stop here.
      if (err && err.name === 'AbortError') return;
      // Anything else falls through to the download path below.
    }

    // Fallback: trigger a real file download. Works on desktop and any browser
    // without file-share support. The scorer attaches the saved file to an email.
    try {
      const blob = new Blob([jsonString], { type: 'application/json' });
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = filename;
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      setTimeout(() => URL.revokeObjectURL(url), 1000);
      setExportedAt(new Date().toISOString());
      setEmailFlash(true);
      setTimeout(() => setEmailFlash(false), 1500);
      return;
    } catch (err) {
      // Last resort — surface the inline panel so the data is never trapped.
    }

    setEmailPayload({ subject, filename, jsonString, fallback: true });
    setExportedAt(new Date().toISOString());
    setEmailFlash(true);
    setTimeout(() => setEmailFlash(false), 1500);
  };

  // Legacy handleEmailData / handleCopyEmailBody were removed when the share-as-JSON-file
  // flow replaced the mailto-with-JSON-in-body approach. The Web Share API handles
  // everything in handleShareFingerprint above; the fallback path triggers a blob
  // download. No mailto, no clipboard copy, no manual image attach.

  const handleStartGame = () => {
    setGameMeta({
      sport: setupForm.sport,
      homeTeam: setupForm.homeTeam.trim(),
      awayTeam: setupForm.awayTeam.trim(),
      ageDivision: setupForm.ageDivision,
      format: setupForm.format,
      host: setupForm.host.trim(),
      homeLineupSize: setupForm.homeLineupSize,
      awayLineupSize: setupForm.awayLineupSize,
      startedAt: new Date().toISOString(),
    });
    // v1.5 — Auto-land in the starting cell. Saves the scorer one tap; the matrix
    // peek is still available via "Return to Matrix" from the cell view.
    //   Wildcard format → 'loaded', 0 outs (the format's whole point — bases always loaded)
    //   Traditional      → 'empty', 0 outs
    const startingStateKey = setupForm.format === 'Wildcard' ? 'loaded' : 'empty';
    setExpandedCell({ stateKey: startingStateKey, outs: 0 });

    // Register setup-entered starting pitchers (first in each team's pitcher list) so
    // their number shows on the mound from the first pitch and a later pitching change
    // splits the count cleanly. Synthetic — no count split, the starter runs from pitch 1.
    const starterEvents = [];
    ['home', 'away'].forEach(side => {
      const first = (pitcherRosters[side] || []).find(p => p.num && p.num.trim());
      if (first) {
        starterEvents.push({
          type: 'pitcher_change', side, jerseyNumber: first.num.trim(),
          timestamp: Date.now(), synthetic: true,
        });
      }
    });
    if (starterEvents.length) setEvents(prev => [...prev, ...starterEvents]);
  };

  const isSetupValid =
    setupForm.sport &&
    setupForm.format &&
    setupForm.homeTeam.trim() &&
    setupForm.awayTeam.trim() &&
    setupForm.ageDivision &&
    setupForm.host.trim();

  // Helper: draw a rounded rectangle without depending on the modern roundRect API
  const roundRectPath = (ctx, x, y, w, h, r) => {
    const radius = Math.min(r, w / 2, h / 2);
    ctx.beginPath();
    ctx.moveTo(x + radius, y);
    ctx.lineTo(x + w - radius, y);
    ctx.quadraticCurveTo(x + w, y, x + w, y + radius);
    ctx.lineTo(x + w, y + h - radius);
    ctx.quadraticCurveTo(x + w, y + h, x + w - radius, y + h);
    ctx.lineTo(x + radius, y + h);
    ctx.quadraticCurveTo(x, y + h, x, y + h - radius);
    ctx.lineTo(x, y + radius);
    ctx.quadraticCurveTo(x, y, x + radius, y);
    ctx.closePath();
  };

  // Helper: draw a rotated diamond (filled or outline)
  const drawDiamond = (ctx, cx, cy, size, filled) => {
    ctx.save();
    ctx.translate(cx, cy);
    ctx.rotate(Math.PI / 4);
    ctx.lineWidth = 1.5;
    ctx.strokeStyle = T.ink;
    if (filled) {
      ctx.fillStyle = T.ink;
      ctx.fillRect(-size / 2, -size / 2, size, size);
    } else {
      ctx.strokeRect(-size / 2, -size / 2, size, size);
    }
    ctx.restore();
  };


      const rosterEditorOverlay = rosterEditor && (() => {
        const team = rosterEditor.team;
        const teamName = team === 'home'
          ? (gameMeta?.homeTeam || setupForm.homeTeam || 'Home')
          : (gameMeta?.awayTeam || setupForm.awayTeam || 'Away');
        const rows = rosters[team] || [];
        const setRows = (updater) => setRosters(prev => ({
          ...prev,
          [team]: typeof updater === 'function' ? updater(prev[team] || []) : updater,
        }));
        const pRows = pitcherRosters[team] || [];
        const setPRows = (updater) => setPitcherRosters(prev => ({
          ...prev,
          [team]: typeof updater === 'function' ? updater(prev[team] || []) : updater,
        }));
        return (
          <div style={{
            position: 'fixed', inset: 0, background: T.scrim,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            zIndex: 85, padding: '20px',
          }}>
            <div style={{
              background: T.paperRaised, borderRadius: '12px', padding: '20px',
              maxWidth: '360px', width: '100%', maxHeight: '80vh', overflowY: 'auto',
              boxShadow: `0 10px 30px ${T.scrim}`,
            }}>
              <div style={{
                display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '4px',
              }}>
                <div style={{ fontSize: '11px', fontWeight: 400, color: T.inkMuted }}>
                  {teamName} Lineup
                </div>
                <button
                  onClick={() => setRosterEditor(null)}
                  style={{ background: 'transparent', border: 'none', color: T.inkMuted, fontSize: '18px', cursor: 'pointer', padding: '0 4px' }}
                >✕</button>
              </div>
              <div style={{ fontSize: '11px', color: T.inkMuted, lineHeight: 1.4, marginBottom: '14px' }}>
                Numbers go in the data; names stay on this device only. Order = batting order.
              </div>

              <div style={{ display: 'flex', flexDirection: 'column', gap: '6px', marginBottom: '12px' }}>
                {rows.length === 0 && (
                  <div style={{ fontSize: '12px', color: T.inkFaint, fontStyle: 'italic', padding: '8px 0' }}>
                    No players yet — add the batting order below.
                  </div>
                )}
                {rows.map((row, idx) => (
                  <div key={idx} style={{ display: 'flex', gap: '6px', alignItems: 'center' }}>
                    <span style={{ fontSize: '11px', color: T.inkFaint, width: '16px', textAlign: 'right' }}>{idx + 1}</span>
                    <input
                      value={row.num || ''}
                      onChange={(e) => setRows(rs => rs.map((r, i) => i === idx ? { ...r, num: e.target.value.replace(/[^0-9]/g, '').slice(0, 3) } : r))}
                      placeholder="#"
                      inputMode="numeric"
                      style={{
                        width: '46px', boxSizing: 'border-box', padding: '8px', fontSize: '13px',
                        border: `1px solid ${T.ruleStrong}`, borderRadius: '6px', fontFamily: 'inherit',
                        textAlign: 'center', background: T.paperRaised,
                      }}
                    />
                    <input
                      value={row.name || ''}
                      onChange={(e) => setRows(rs => rs.map((r, i) => i === idx ? { ...r, name: e.target.value } : r))}
                      placeholder="Name"
                      style={{
                        flex: 1, boxSizing: 'border-box', padding: '8px', fontSize: '13px',
                        border: `1px solid ${T.ruleStrong}`, borderRadius: '6px', fontFamily: 'inherit',
                        background: T.paperRaised,
                      }}
                    />
                    <button
                      onClick={() => setRows(rs => rs.filter((_, i) => i !== idx))}
                      style={{ background: 'transparent', border: 'none', color: T.inkMuted, fontSize: '16px', cursor: 'pointer', padding: '0 4px' }}
                    >✕</button>
                  </div>
                ))}
              </div>

              <button
                onClick={() => setRows(rs => [...rs, { num: '', name: '' }])}
                style={{
                  width: '100%', padding: '10px', background: 'transparent', border: `1px dashed ${T.ruleStrong}`,
                  borderRadius: '8px', color: T.inkSecondary, fontFamily: 'inherit', fontSize: '13px', fontWeight: 500,
                  cursor: 'pointer', marginBottom: '12px',
                }}
              >+ Add Player</button>

              {/* Pitchers — tracked separately from the batting order. Numbers feed
                  the data; names surface on the top pitcher panel as F. Lastname. */}
              <div style={{
                fontSize: '11px', fontWeight: 400, color: T.inkMuted, margin: '4px 0 8px', borderTop: `1px solid ${T.rule}`, paddingTop: '14px',
              }}>
                Pitchers
              </div>
              <div style={{ display: 'flex', flexDirection: 'column', gap: '6px', marginBottom: '12px' }}>
                {pRows.length === 0 && (
                  <div style={{ fontSize: '12px', color: T.inkFaint, fontStyle: 'italic', padding: '4px 0' }}>
                    No pitchers yet — add their numbers and names.
                  </div>
                )}
                {pRows.map((row, idx) => (
                  <div key={idx} style={{ display: 'flex', gap: '6px', alignItems: 'center' }}>
                    <input
                      value={row.num || ''}
                      onChange={(e) => setPRows(rs => rs.map((r, i) => i === idx ? { ...r, num: e.target.value.replace(/[^0-9]/g, '').slice(0, 3) } : r))}
                      placeholder="#"
                      inputMode="numeric"
                      style={{
                        width: '46px', boxSizing: 'border-box', padding: '8px', fontSize: '13px',
                        border: `1px solid ${T.ruleStrong}`, borderRadius: '6px', fontFamily: 'inherit',
                        textAlign: 'center', background: T.paperRaised,
                      }}
                    />
                    <input
                      value={row.name || ''}
                      onChange={(e) => setPRows(rs => rs.map((r, i) => i === idx ? { ...r, name: e.target.value } : r))}
                      placeholder="Name"
                      style={{
                        flex: 1, boxSizing: 'border-box', padding: '8px', fontSize: '13px',
                        border: `1px solid ${T.ruleStrong}`, borderRadius: '6px', fontFamily: 'inherit',
                        background: T.paperRaised,
                      }}
                    />
                    <button
                      onClick={() => setPRows(rs => rs.filter((_, i) => i !== idx))}
                      style={{ background: 'transparent', border: 'none', color: T.inkMuted, fontSize: '16px', cursor: 'pointer', padding: '0 4px' }}
                    >✕</button>
                  </div>
                ))}
              </div>

              <button
                onClick={() => setPRows(rs => [...rs, { num: '', name: '' }])}
                style={{
                  width: '100%', padding: '10px', background: 'transparent', border: `1px dashed ${T.ruleStrong}`,
                  borderRadius: '8px', color: T.inkSecondary, fontFamily: 'inherit', fontSize: '13px', fontWeight: 500,
                  cursor: 'pointer', marginBottom: '12px',
                }}
              >+ Add Pitcher</button>

              <button
                onClick={() => {
                  // Drop fully-empty rows on close so they don't accumulate.
                  const cleanedP = pRows.filter(r => (r.num && r.num.trim()) || (r.name && r.name.trim()));
                  setRows(rs => rs.filter(r => (r.num && r.num.trim()) || (r.name && r.name.trim())));
                  setPRows(cleanedP);
                  // Pitchers come in one at a time — the last one entered is the one on
                  // the mound. Mid-game, register them so the pitch count splits to the
                  // new pitcher (setCurrentPitcherNumber no-ops if unchanged).
                  if (gameMeta) {
                    let lastNum = null;
                    for (let i = cleanedP.length - 1; i >= 0; i--) {
                      if (cleanedP[i].num && cleanedP[i].num.trim()) { lastNum = cleanedP[i].num.trim(); break; }
                    }
                    if (lastNum) setCurrentPitcherNumber(team, lastNum);
                  }
                  setRosterEditor(null);
                }}
                style={{
                  width: '100%', padding: '14px', background: T.ink, color: T.paperRaised, border: 'none',
                  borderRadius: '8px', fontFamily: 'inherit', fontSize: '14px', fontWeight: 600, cursor: 'pointer',
                }}
              >Done</button>
              {(rows.length > 0 || pRows.length > 0) && (
                <button
                  onClick={() => { setRows([]); setPRows([]); }}
                  style={{
                    width: '100%', marginTop: '8px', padding: '10px', background: 'transparent',
                    border: 'none', color: T.inkFaint, fontFamily: 'inherit', fontSize: '12px',
                    fontWeight: 500, cursor: 'pointer', textDecoration: 'underline',
                  }}
                >Clear all</button>
              )}
            </div>
          </div>
        );
      })();

  // Setup screen — shown when no game has been started
  if (isLoaded && !gameMeta) {
    const fieldStyle = {
      width: '100%',
      background: T.paperRaised,
      border: `1px solid ${T.ruleStrong}`,
      borderRadius: '3px',
      padding: '14px 12px',
      color: T.ink,
      fontFamily: 'inherit',
      fontSize: '14px',
      outline: 'none',
      boxSizing: 'border-box',
    };
    const labelStyle = {
      display: 'block',
      fontSize: '11px',
      color: T.inkMuted,
      marginBottom: '6px',
      fontWeight: 400,
    };
    const segmentStyle = (active) => ({
      flex: 1,
      background: active ? T.inkMuted : T.paperRaised,
      border: `1px solid ${active ? T.inkMuted : T.ruleStrong}`,
      color: active ? T.paperRaised : T.inkSecondary,
      padding: '12px 8px',
      fontSize: '11px',
      fontWeight: 400,
      cursor: 'pointer',
      fontFamily: 'inherit',
      borderRadius: '3px',
    });

    const ages = ['8U', '9U', '10U', '11U', '12U', '13U', '14U', 'High School', 'College', 'Professional'];
    const formats = ['Wildcard', 'Traditional'];

    return (
      <div style={{
        minHeight: '100vh',
        background: T.paper,
        backgroundImage: `
          linear-gradient(${T.rule} 1px, transparent 1px),
          linear-gradient(90deg, ${T.rule} 1px, transparent 1px)
        `,
        backgroundSize: '32px 32px',
        fontFamily: 'inherit',
        color: T.ink,
        padding: '24px 16px 40px',
      }}>
        <div style={{ maxWidth: '420px', margin: '0 auto' }}>
          <div style={{ marginBottom: '24px' }}>
            <div style={{
              fontSize: '22px',
              fontWeight: 400,
              lineHeight: 1,
            }}>
              WILD<span style={{ color: T.ink }}>CARD</span>
            </div>
            <div style={{
              fontSize: '11px',
              color: T.inkMuted,
              marginTop: '4px',
            }}>
              New fingerprint
            </div>
          </div>

          <div style={{
            background: T.paperRaised,
            border: `1px solid ${T.rule}`,
            borderRadius: '6px',
            padding: '20px',
          }}>
            <div style={{ marginBottom: '16px' }}>
              <label style={labelStyle}>Sport</label>
              <div style={{ display: 'flex', gap: '6px' }}>
                {['Baseball', 'Softball'].map(s => (
                  <button
                    key={s}
                    onClick={() => setSetupForm({ ...setupForm, sport: s })}
                    style={segmentStyle(setupForm.sport === s)}
                  >
                    {s}
                  </button>
                ))}
              </div>
            </div>

            <div style={{ marginBottom: '16px' }}>
              <label style={labelStyle}>Format</label>
              <div style={{ display: 'flex', gap: '6px' }}>
                {formats.map(fmt => (
                  <button
                    key={fmt}
                    onClick={() => setSetupForm({ ...setupForm, format: fmt })}
                    style={segmentStyle(setupForm.format === fmt)}
                  >
                    {fmt}
                  </button>
                ))}
              </div>
            </div>

            <div style={{ marginBottom: '16px' }}>
              <label style={labelStyle}>Home Team</label>
              <input
                type="text"
                value={setupForm.homeTeam}
                onChange={(e) => setSetupForm({ ...setupForm, homeTeam: e.target.value })}
                placeholder="e.g. Tigers"
                style={fieldStyle}
              />
            </div>

            <div style={{ marginBottom: '16px' }}>
              <label style={labelStyle}>Away Team</label>
              <input
                type="text"
                value={setupForm.awayTeam}
                onChange={(e) => setSetupForm({ ...setupForm, awayTeam: e.target.value })}
                placeholder="e.g. Hawks"
                style={fieldStyle}
              />
            </div>

            <div style={{ marginBottom: '16px' }}>
              <label style={labelStyle}>Age Division</label>
              <select
                value={setupForm.ageDivision}
                onChange={(e) => setSetupForm({ ...setupForm, ageDivision: e.target.value })}
                style={{
                  ...fieldStyle,
                  appearance: 'none',
                  WebkitAppearance: 'none',
                  MozAppearance: 'none',
                  backgroundImage: `url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 12 12'><path fill='%237a7468' d='M3 4.5l3 3 3-3z'/></svg>")`,
                  backgroundRepeat: 'no-repeat',
                  backgroundPosition: 'right 12px center',
                  backgroundSize: '12px',
                  paddingRight: '32px',
                  cursor: 'pointer',
                  color: setupForm.ageDivision ? T.ink : T.inkFaint,
                }}
              >
                <option value="" disabled>Select age division</option>
                {ages.map(age => (
                  <option key={age} value={age}>{age}</option>
                ))}
              </select>
            </div>

            <div style={{ marginBottom: '20px' }}>
              <label style={labelStyle}>Host / Tournament</label>
              <input
                type="text"
                value={setupForm.host}
                onChange={(e) => setSetupForm({ ...setupForm, host: e.target.value })}
                placeholder="e.g. Perfect Game, USSSA, local league"
                style={fieldStyle}
              />
            </div>

            <div style={{ marginBottom: '20px' }}>
              <label style={labelStyle}>Lineups <span style={{ textTransform: 'none', color: T.inkFaint }}>(optional)</span></label>
              <div style={{ display: 'flex', gap: '8px' }}>
                {['away', 'home'].map(team => {
                  const tn = (team === 'home' ? setupForm.homeTeam : setupForm.awayTeam).trim();
                  const count = (rosters[team] || []).filter(r => r.num || r.name).length;
                  return (
                    <button
                      key={team}
                      onClick={() => setRosterEditor({ team })}
                      style={{
                        flex: 1, padding: '12px 10px', background: T.paperRaised,
                        border: `1px solid ${T.ruleStrong}`, borderRadius: '6px',
                        color: T.ink, fontFamily: 'inherit', fontSize: '12px', fontWeight: 500,
                        cursor: 'pointer', textAlign: 'left', lineHeight: 1.3,
                      }}
                    >
                      <div style={{ fontSize: '11px', color: T.inkFaint }}>
                        {team === 'home' ? 'Home' : 'Away'}
                      </div>
                      <div style={{ marginTop: '2px' }}>{tn || (team === 'home' ? 'Home' : 'Away')}</div>
                      <div style={{ fontSize: '11px', color: count ? T.ink : T.inkFaint, marginTop: '2px' }}>
                        {count ? `${count} player${count === 1 ? '' : 's'}` : 'Add players'}
                      </div>
                    </button>
                  );
                })}
              </div>
            </div>

            <button
              onClick={handleStartGame}
              disabled={!isSetupValid}
              style={{
                width: '100%',
                background: isSetupValid ? T.ink : T.rule,
                border: 'none',
                color: isSetupValid ? T.paperRaised : T.inkFaint,
                padding: '16px',
                borderRadius: '3px',
                fontSize: '12px',
                fontWeight: 500,
                cursor: isSetupValid ? 'pointer' : 'not-allowed',
                fontFamily: 'inherit',
              }}
            >
              Start tagging
            </button>

            <div style={{
              marginTop: '14px',
              fontSize: '11px',
              color: T.inkFaint,
              lineHeight: 1.6,
              textAlign: 'center',
            }}>
              Each team can produce their own fingerprint from their own device.
            </div>
          </div>

          {/* Powered by Loophohl tagline */}
          <div style={{
            marginTop: '24px',
            textAlign: 'center',
            fontSize: '11px',
            color: T.inkFaint,
          }}>
            Powered by Loophohl
          </div>
        </div>
        {rosterEditorOverlay}
      </div>
    );
  }

  return (
    <div style={{
      minHeight: '100vh',
      background: T.paper,
      fontFamily: 'inherit',
      color: T.ink,
      padding: '8px 10px 12px',
      userSelect: 'none',
      WebkitTapHighlightColor: 'transparent',
    }}>
      {/* ── Thin brand bar ───────────────────────────────────────────────
          Consolidated in 2A. Replaces the tall branding block: wordmark + small
          fingerprint mark + SAVED status on one line. */}
      <div style={{
        maxWidth: '560px',
        margin: '0 auto 10px',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
      }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
          <img
            src={gameMeta?.sport === 'Softball' ? FINGERPRINT_SOFTBALL : FINGERPRINT_RED}
            alt=""
            style={{ width: '24px', height: '24px', display: 'block' }}
          />
          <span style={{ fontSize: '15px', fontWeight: 400, lineHeight: 1 }}>
            WILD<span style={{ color: T.ink }}>CARD</span>
          </span>
        </div>
        <span style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: '4px',
          fontSize: '11px',
          color: storageMode === 'memory-only' ? T.paper : T.ink,
        }}>
          <span style={{
            display: 'inline-block',
            width: '5px',
            height: '5px',
            borderRadius: '50%',
            background: storageMode === 'memory-only' ? T.ink : 'transparent',
          }} />
          {storageMode === 'local-storage' && 'Saved'}
          {storageMode === 'memory-only' && 'Not saving'}
          {storageMode === 'checking' && '···'}
        </span>
      </div>

      {/* ── Consolidated scoreboard strip ─────────────────────────────────
          Replaces the two pitching panels + score row + batting-side slider.
          Per team: runs · active pitcher # (P-of-game, jersey) · current-pitcher
          pitch count · team total pitches. Center pill: inning (TOP/BOT + number).
          Bottom bar: possession, READ-ONLY — derived from the event stream, flips
          automatically on the third out (the inning_change event). Undo reverses it. */}
      {gameMeta && (() => {
        const scores = getScores();
        // Pitch counts (same derivations as the old panels).
        const homeTeamPitches = pitchEvents.filter(e => e.batting === 'away').length;
        const awayTeamPitches = pitchEvents.filter(e => e.batting === 'home').length;
        const homeLastChangeIdx = pitcherChanges.home.length > 0 ? pitcherChanges.home[pitcherChanges.home.length - 1] : 0;
        const awayLastChangeIdx = pitcherChanges.away.length > 0 ? pitcherChanges.away[pitcherChanges.away.length - 1] : 0;
        const currentHomePitcher = events.slice(homeLastChangeIdx).filter(e => (e.type === 'pitch' || !e.type) && e.batting === 'away').length;
        const currentAwayPitcher = events.slice(awayLastChangeIdx).filter(e => (e.type === 'pitch' || !e.type) && e.batting === 'home').length;
        const homePitcherNumber = pitcherChanges.home.length + 1;
        const awayPitcherNumber = pitcherChanges.away.length + 1;
        const homeJersey = getCurrentPitcherNumber('home');
        const awayJersey = getCurrentPitcherNumber('away');

        // Inning label (same derivation the slider used).
        const halfInningsCompleted = events.filter(e => e.type === 'inning_change').length;
        const inningNumber = Math.floor(halfInningsCompleted / 2) + 1;
        const half = battingSide === 'away' ? 'Top' : 'Bot';

        const homePitching = battingSide === 'away';
        const battingTeam = battingSide === 'away' ? gameMeta.awayTeam : gameMeta.homeTeam;
        const count = getExpandedCellCount();
        const outs = expandedCell ? expandedCell.outs : 0;

        // Format a roster name as "F. Lastname" (first initial + last name).
        const formatInitialLast = (name) => {
          if (!name) return '';
          const parts = String(name).trim().split(/\s+/).filter(Boolean);
          if (parts.length === 0) return '';
          if (parts.length === 1) return parts[0];
          return `${parts[0][0].toUpperCase()}. ${parts[parts.length - 1]}`;
        };
        // Look up a pitcher's name by number — checks the team's pitcher list first,
        // then the batting roster (a pitcher who also hits is in the lineup).
        const pitcherNameFor = (side) => {
          const numStr = String(getCurrentPitcherNumber(side) || '');
          if (!numStr) return '';
          const list = [...(pitcherRosters[side] || []), ...(rosters[side] || [])];
          const entry = list.find(r => r.num && String(r.num) === numStr);
          return entry ? formatInitialLast(entry.name) : '';
        };
        const pitcherLine = (num, jersey, cur, total, name) => {
          const id = (name && jersey) ? `${name} #${jersey}` : (jersey ? `#${jersey}` : (name || ''));
          return `${id}${id ? '  ' : ''}${cur}p`;
        };

        // Renders the pitcher line as a tappable label that becomes an inline input
        // when this side is being edited. Tapping commits the jersey number for that
        // side via setCurrentPitcherNumber; the rest of the line (pitch counts) stays
        // derived and read-only. `align` controls text alignment for the home (right) side.
        const renderPitcherLine = (side, num, jersey, cur, total, align) => {
          const editing = pitcherStripEdit && pitcherStripEdit.side === side;
          if (editing) {
            return (
              <input
                autoFocus
                type="text"
                inputMode="numeric"
                value={pitcherStripEdit.draft}
                onChange={(e) => {
                  const cleaned = e.target.value.replace(/[^0-9]/g, '').slice(0, 3);
                  setPitcherStripEdit({ side, draft: cleaned });
                }}
                onBlur={() => {
                  setCurrentPitcherNumber(side, pitcherStripEdit.draft);
                  setPitcherStripEdit(null);
                }}
                onKeyDown={(e) => {
                  if (e.key === 'Enter') {
                    setCurrentPitcherNumber(side, pitcherStripEdit.draft);
                    setPitcherStripEdit(null);
                  } else if (e.key === 'Escape') {
                    setPitcherStripEdit(null);
                  }
                }}
                placeholder="jersey #"
                style={{
                  width: '64px',
                  fontSize: '11px',
                  padding: '2px 4px',
                  border: `1px solid ${T.rule}`,
                  borderRadius: '4px',
                  background: T.paperRaised,
                  color: T.ink,
                  textAlign: align === 'right' ? 'right' : 'left',
                  outline: 'none',
                  marginTop: '1px',
                }}
              />
            );
          }
          // Read-only — pitcher number is now edited via the pitcher sphere on the
          // diamond (the top panel is no longer a button).
          return (
            <div style={pitcherSubStyle}>
              {pitcherLine(num, jersey, cur, total, pitcherNameFor(side))}
            </div>
          );
        };

        // Each top panel reflects possession: the team on offense shows the current
        // hitter (initial. last #number), the team on defense shows the pitcher. They
        // flip automatically when the inning turns over (battingSide drives it).
        const renderTeamSubline = (side, num, jersey, cur, total, align) => {
          if (side === battingSide) {
            const bs = getCurrentBatterSlot();
            const slot = bs ? bs.slot : null;
            const hitterNum = slot ? getJerseyNumber(side, slot) : null;
            const entry = slot ? (rosters[side] || [])[slot - 1] : null;
            const name = entry ? formatInitialLast(entry.name) : '';
            let text;
            if (name && hitterNum) text = `${name}  #${hitterNum}`;
            else if (name) text = name;
            else if (hitterNum) text = `#${hitterNum}`;
            else text = 'At bat';
            const hl = getHitterLine(side, hitterNum);
            if (hl && hl.atBats > 0) text += ` (${hl.hits}-${hl.atBats})`;
            return <div style={{ ...pitcherSubStyle, color: T.ink, fontWeight: 400 }}>{text}</div>;
          }
          return renderPitcherLine(side, num, jersey, cur, total, align);
        };

        const teamCellStyle = {
          flex: 1,
          padding: '8px 12px',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'space-between',
          minWidth: 0,
        };
        const teamNameStyle = {
          fontSize: '11px',
          fontWeight: 400,
          color: T.inkMuted,
          whiteSpace: 'nowrap',
          overflow: 'hidden',
          textOverflow: 'ellipsis',
        };
        const pitcherSubStyle = {
          fontSize: '11px',
          color: T.inkMuted,
          marginTop: '2px',
          whiteSpace: 'nowrap',
        };
        const scoreNumStyle = {
          fontSize: '32px',
          fontWeight: 300,
          fontFamily: 'inherit',
          color: T.ink,
          lineHeight: 1,
          fontVariantNumeric: 'tabular-nums',
        };

        return (
          <div style={{
            maxWidth: '560px',
            margin: '0 auto 4px',
            background: 'transparent',
            borderTop: `1px solid ${T.rule}`,
            borderBottom: `1px solid ${T.rule}`,
          }}>
            <div style={{ display: 'flex', alignItems: 'stretch' }}>
              {/* Away team — display only (pitcher # is edited via the mound sphere) */}
              <div
                style={{ ...teamCellStyle, borderRight: `1px solid ${T.rule}` }}
              >
                <div style={{ minWidth: 0 }}>
                  <div style={teamNameStyle}>{gameMeta.awayTeam}</div>
                  {renderTeamSubline('away', awayPitcherNumber, awayJersey, currentAwayPitcher, awayTeamPitches, 'left')}
                </div>
                <span style={scoreNumStyle}>{scores.away}</span>
              </div>
              {/* Inning + count pill — arrow shows half (▲ top / ▼ bottom), with the
                  live ball/strike/out count below. */}
              <div style={{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'center',
                padding: '4px 10px',
                background: 'transparent',
                borderRight: `1px solid ${T.rule}`,
                minWidth: '96px',
              }}>
                <div style={{ display: 'flex', alignItems: 'center', gap: '4px' }}>
                  <span style={{ fontSize: '11px', fontWeight: 500, color: T.ink, lineHeight: 1 }}>{half === 'Top' ? '▲' : '▼'}</span>
                  <span style={{ fontSize: '13px', fontWeight: 400, color: T.ink, whiteSpace: 'nowrap' }}>{ordinalInning(inningNumber)}</span>
                </div>
                <div style={{ display: 'flex', alignItems: 'baseline', gap: '2px', marginTop: '3px', fontVariantNumeric: 'tabular-nums' }}>
                  <span style={{ fontSize: '11px', fontWeight: 400, color: T.inkMuted }}>B</span>
                  <span style={{ fontSize: '14px', fontWeight: 400, color: T.ink }}>{count.balls}</span>
                  <span style={{ width: '10px', display: 'inline-block' }} />
                  <span style={{ fontSize: '11px', fontWeight: 400, color: T.inkMuted }}>S</span>
                  <span style={{ fontSize: '14px', fontWeight: 400, color: T.ink }}>{count.strikes}</span>
                  <span style={{ width: '10px', display: 'inline-block' }} />
                  <span style={{ fontSize: '11px', fontWeight: 400, color: T.inkMuted }}>O</span>
                  <span style={{ fontSize: '14px', fontWeight: 400, color: T.ink }}>{outs}</span>
                </div>
              </div>
              {/* Home team — display only (pitcher # is edited via the mound sphere) */}
              <div
                style={{ ...teamCellStyle, borderLeft: `1px solid ${T.rule}` }}
              >
                <span style={scoreNumStyle}>{scores.home}</span>
                <div style={{ minWidth: 0, textAlign: 'right' }}>
                  <div style={teamNameStyle}>{gameMeta.homeTeam}</div>
                  {renderTeamSubline('home', homePitcherNumber, homeJersey, currentHomePitcher, homeTeamPitches, 'right')}
                </div>
              </div>
            </div>
          </div>
        );
      })()}

      {/* v1.2: The top action bar (Undo / Pitching Change / New Batter) was removed.
          Undo and Pitching Change now live inside the expanded cell view.
          New Batter was deprecated entirely — PA-end events are captured directly
          via the Hit/Walk/Out/Fielding Error/Other buttons in the expanded cell.
          The prompt UI block was also removed — capture is direct, not retroactive. */}

      {/* Game surface — always shows ExpandedCellView (diamond + actions) */}
      <div
        style={{
          background: 'transparent',
          border: 'none',
          borderRadius: 0,
          padding: '4px 0 0',
          maxWidth: '560px',
          margin: '0 auto',
          position: 'relative',
        }}
      >
        {expandedCell && (
          <ExpandedCellView
            cell={expandedCell}
            count={getExpandedCellCount()}
            batterSlot={getCurrentBatterSlot()}
            lineupMarkerCount={getLineupMarkerCount(battingSide)}
            onMarkLeadoff={markLeadoff}
            jerseyNumber={(() => {
              const bs = getCurrentBatterSlot();
              return bs ? getJerseyNumber(battingSide, bs.slot) : null;
            })()}
            onJerseyNumberChange={(value) => {
              const bs = getCurrentBatterSlot();
              if (bs) setJerseyNumber(battingSide, bs.slot, value);
            }}
            pitcherNumber={getCurrentPitcherNumber(battingSide === 'home' ? 'away' : 'home')}
            onPitcherNumberChange={(value) => {
              setCurrentPitcherNumber(battingSide === 'home' ? 'away' : 'home', value);
            }}
            flashingButton={flashingButton}
            onPitchTap={handlePitchTap}
            onPaEndTap={handlePaEndTap}
            onRunnerAdvanceTap={handleRunnerAdvanceTap}
            onEndHalfInning={endHalfInning}
            baseJerseys={baseJerseys}
            onUndo={handleUndo}
            canUndo={events.length > 0}
            pitchDetailOn={pitchDetailOn}
            onTogglePitchDetail={() => setPitchDetailOn(v => !v)}
            sport={gameMeta?.sport}
            lastVelo={lastVelo[battingSide === 'away' ? 'home' : 'away']}
            onVeloRecorded={(v) => setLastVelo(prev => ({
              ...prev,
              [battingSide === 'away' ? 'home' : 'away']: v,
            }))}
          />
        )}
      </div>

      {/* Lineup panel — UI-only names mapped to jersey numbers, with live status
          (at bat / on base) for the batting team. Collapsed by default; names never
          enter the JSON. Lets a parent glance and find their player on the field. */}
      {gameMeta && (() => {
        const team = lineupTeamView || battingSide;
        const teamName = team === 'home' ? (gameMeta.homeTeam || 'Home') : (gameMeta.awayTeam || 'Away');
        const roster = rosters[team] || [];
        const isBatting = team === battingSide;
        const currentBatterNum = (() => {
          const bs = getCurrentBatterSlot();
          return bs ? getJerseyNumber(battingSide, bs.slot) : null;
        })();
        const pitcherNum = getCurrentPitcherNumber(team);
        const statusFor = (num) => {
          if (!num) return null;
          const s = String(num);
          if (isBatting) {
            if (currentBatterNum && String(currentBatterNum) === s) return { label: 'At bat', color: T.ink };
            for (const b of ['3B', '2B', '1B']) {
              if (baseJerseys[b] && String(baseJerseys[b]) === s) return { label: 'On ' + b, color: T.ink };
            }
          } else if (pitcherNum && String(pitcherNum) === s) {
            return { label: 'Pitching', color: T.ink };
          }
          return null;
        };
        return (
          <div style={{
            maxWidth: '560px', margin: '8px auto 0',
            border: `1px solid ${T.rule}`, borderRadius: '6px',
            background: T.paperRaised, overflow: 'hidden',
          }}>
            {/* Header bar — tap to expand/collapse */}
            <div
              onClick={() => setLineupPanelOpen(o => !o)}
              style={{
                display: 'flex', alignItems: 'center', justifyContent: 'space-between',
                padding: '8px 14px', cursor: 'pointer',
              }}
            >
              <div style={{ fontSize: '11px', color: T.inkMuted, fontWeight: 400 }}>
                {lineupPanelOpen ? '▾' : '▸'} Lineup — {teamName}
                {isBatting && <span style={{ color: T.inkMuted }}>  at bat</span>}
              </div>
              <div style={{ fontSize: '11px', color: T.inkFaint }}>
                {roster.length ? `${roster.length}` : 'tap to add'}
              </div>
            </div>

            {lineupPanelOpen && (
              <div style={{ padding: '0 14px 14px' }}>
                {/* Team toggle + edit */}
                <div style={{ display: 'flex', gap: '6px', marginBottom: '10px' }}>
                  <button
                    onClick={() => setLineupTeamView('away')}
                    style={{
                      flex: 1, padding: '7px', fontSize: '11px', fontWeight: 400, borderRadius: '5px', cursor: 'pointer', fontFamily: 'inherit',
                      border: `1px solid ${T.rule}`,
                      background: team === 'away' ? T.ink : 'transparent',
                      color: team === 'away' ? T.paperRaised : T.inkSecondary,
                    }}
                  >{(gameMeta.awayTeam || 'Away')}</button>
                  <button
                    onClick={() => setLineupTeamView('home')}
                    style={{
                      flex: 1, padding: '7px', fontSize: '11px', fontWeight: 400, borderRadius: '5px', cursor: 'pointer', fontFamily: 'inherit',
                      border: `1px solid ${T.rule}`,
                      background: team === 'home' ? T.ink : 'transparent',
                      color: team === 'home' ? T.paperRaised : T.inkSecondary,
                    }}
                  >{(gameMeta.homeTeam || 'Home')}</button>
                  <button
                    onClick={() => setRosterEditor({ team })}
                    style={{
                      padding: '7px 12px', fontSize: '11px', fontWeight: 400, borderRadius: '5px', cursor: 'pointer', fontFamily: 'inherit',
                      border: `1px solid ${T.rule}`, background: 'transparent', color: T.ink,
                    }}
                  >Edit</button>
                </div>

                {roster.length === 0 ? (
                  <div style={{ fontSize: '12px', color: T.inkFaint, fontStyle: 'italic', padding: '6px 0' }}>
                    No lineup for {teamName} yet — tap Edit to add numbers and names.
                  </div>
                ) : (
                  <div style={{ display: 'flex', flexDirection: 'column' }}>
                    {roster.map((r, idx) => {
                      const st = statusFor(r.num);
                      return (
                        <div key={idx} style={{
                          display: 'flex', alignItems: 'center', gap: '10px',
                          padding: '7px 4px',
                          borderBottom: idx < roster.length - 1 ? `1px solid ${T.rule}` : 'none',
                          background: st ? T.rule : 'transparent',
                        }}>
                          <span style={{ fontSize: '11px', color: T.inkFaint, width: '14px', textAlign: 'right' }}>{idx + 1}</span>
                          <span style={{
                            fontSize: '12px', fontWeight: 400, color: T.ink,
                            minWidth: '34px', fontFamily: 'inherit',
                          }}>{r.num ? `#${r.num}` : '—'}</span>
                          <span style={{ flex: 1, fontSize: '13px', color: T.ink }}>
                            {r.name || <span style={{ color: T.inkPlaceholder }}>—</span>}
                            {(() => {
                              const hl = getHitterLine(team, r.num);
                              return (hl && hl.atBats > 0)
                                ? <span style={{ color: T.inkMuted, fontSize: '11px', marginLeft: '5px' }}>({hl.hits}-{hl.atBats})</span>
                                : null;
                            })()}
                          </span>
                          {st && (
                            <span style={{
                              fontSize: '11px', fontWeight: 400, color: T.paperRaised, background: st.color, padding: '3px 7px', borderRadius: '10px',
                            }}>{st.label}</span>
                          )}
                        </div>
                      );
                    })}
                  </div>
                )}
              </div>
            )}
          </div>
        );
      })()}
      <div style={{
        maxWidth: '560px',
        margin: '8px auto 0',
        display: 'flex',
        gap: '8px',
      }}>
          <button
            onClick={handleShareFingerprint}
            disabled={events.length === 0}
            style={{
              flex: 1,
              background: emailFlash ? T.ink : 'transparent',
              border: `1px solid ${events.length === 0 ? T.rule : T.ink}`,
              color: events.length === 0 ? T.inkPlaceholder : (emailFlash ? T.ink : T.ink),
              padding: '11px 8px',
              borderRadius: '3px',
              fontFamily: 'inherit',
              fontSize: '11px',
              fontWeight: emailFlash ? 700 : 600,
              cursor: events.length === 0 ? 'not-allowed' : 'pointer',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              gap: '5px',
              transition: 'all 200ms ease',
            }}
          >
            <Mail size={12} /> {emailFlash ? (emailPayload?.fallback ? 'Opened ↓' : 'Shared ↑') : 'Share fingerprint'}
          </button>
          <button
            onClick={handleEndGame}
            disabled={events.length === 0 && !gameMeta}
            style={{
              flex: 1,
              background: endArmed ? T.ink : 'transparent',
              border: `1px solid ${endArmed ? (isUnsaved ? T.ink : T.ink) : (isUnsaved ? T.rule : T.ruleStrong)}`,
              color: (events.length === 0 && !gameMeta)
                ? T.inkPlaceholder
                : (endArmed ? T.paperRaised : (isUnsaved ? T.inkMuted : T.ink)),
              padding: '11px 8px',
              borderRadius: '3px',
              fontFamily: 'inherit',
              fontSize: '11px',
              fontWeight: endArmed ? 700 : 600,
              cursor: (events.length === 0 && !gameMeta) ? 'not-allowed' : 'pointer',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              gap: '2px',
              transition: 'all 120ms ease',
              lineHeight: 1.2,
            }}
          >
            {endArmed
              ? (isUnsaved ? <><span>Not Saved</span><span style={{ fontSize: '11px', opacity: 0.9 }}>End anyway?</span></> : 'Confirm')
              : 'End game'}
          </button>
      </div>



      {/* Inline panel — shown when Web Share isn't available or failed. Gives
          the scorer two reliable paths off-phone: (1) Copy JSON, paste into
          email/Notes/Slack, or (2) Open Mail pre-addressed (summary in body —
          mailto can't carry attachments, JSON would be too long). Plus a
          retry button in case Web Share probed wrong the first time.
          On desktop, this panel doubles as a verification view. */}
      {emailPayload && emailPayload.fallback && (
        <div ref={emailPanelRef} style={{
          maxWidth: '560px',
          margin: '16px auto 0',
          background: T.paperRaised,
          border: `1px solid ${T.rule}`,
          borderRadius: '6px',
          padding: '14px',
        }}>
          <div style={{
            fontSize: '11px',
            color: T.inkMuted,
            marginBottom: '12px',
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
          }}>
            <span>Send Fingerprint</span>
            <button
              onClick={() => setEmailPayload(null)}
              style={{
                background: 'transparent',
                border: 'none',
                color: T.inkMuted,
                fontSize: '14px',
                cursor: 'pointer',
                padding: '0 4px',
              }}
            >
              ✕
            </button>
          </div>

          {/* Two actions only. The .json file attach doesn't work in the sandboxed
              iframe, so the reliable path is: copy the JSON below, open mail, paste it
              into the body. Email body is a single line — no summary, no preamble. */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', marginBottom: '12px' }}>
            <button
              onClick={async () => {
                try {
                  await navigator.clipboard.writeText(emailPayload.jsonString);
                } catch {
                  const ta = document.getElementById('wildcard-json-textarea');
                  if (ta) { ta.focus(); ta.select(); try { document.execCommand('copy'); } catch {} }
                }
                setCopyFlash('json');
                setTimeout(() => setCopyFlash(null), 1500);
              }}
              style={{
                width: '100%', padding: '12px', background: T.ink, border: 'none',
                borderRadius: '5px', color: T.paperRaised, fontSize: '12px', fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit',
              }}
            >
              {copyFlash === 'json' ? '\u2713 JSON Copied' : 'Copy JSON'}
            </button>
            <a
              href={`mailto:brandon@loophohl.com?subject=${encodeURIComponent(emailPayload.subject)}&body=${encodeURIComponent('Paste the WildCard JSON below:')}`}
              style={{
                width: '100%', boxSizing: 'border-box', padding: '12px', background: 'transparent',
                border: `1px solid ${T.ink}`, borderRadius: '5px', color: T.ink, fontSize: '12px',
                fontWeight: 500, cursor: 'pointer', fontFamily: 'inherit',
                textAlign: 'center', textDecoration: 'none', display: 'block',
              }}
            >
              Open Mail
            </a>
          </div>

          <div style={{ fontSize: '11px', color: T.inkSecondary, lineHeight: 1.5, marginBottom: '12px' }}>
            Tap <em>Copy JSON</em>, then <em>Open Mail</em> and paste it into the body. Send to <strong>brandon@loophohl.com</strong>.
          </div>

          <div style={{
            fontSize: '11px', color: T.inkMuted, marginBottom: '6px', wordBreak: 'break-all',
          }}>
            {emailPayload.filename}
          </div>
          <textarea
            id="wildcard-json-textarea"
            readOnly
            value={emailPayload.jsonString}
            onFocus={(e) => e.target.select()}
            onClick={(e) => e.target.select()}
            style={{
              width: '100%', boxSizing: 'border-box', background: T.paper,
              border: `1px solid ${T.rule}`, borderRadius: '3px', padding: '10px',
              fontSize: '11px', color: T.inkSecondary, fontFamily: 'inherit', height: '160px',
              resize: 'vertical', whiteSpace: 'pre', overflowX: 'auto',
            }}
          />
        </div>
      )}

      {/* Color scale legend removed — no longer relevant without the matrix view */}

      <div style={{
        maxWidth: '560px',
        margin: '12px auto 4px',
        textAlign: 'center',
        fontSize: '11px',
        color: T.inkFaint,
      }}>
        Powered by Loophohl
      </div>

      {/* Lineup confirmation prompt — appears after PAs that push the running slot
          past 9 on a side whose lineup isn't yet resolved. Yes drops a lineup_marker
          for that side (which anchors slot 1 from this point); No dismisses for this
          PA only (will ask again after the next pa_end up to slot 15). */}
      {lineupPrompt && (
        <div
          style={{
            position: 'fixed',
            inset: 0,
            background: T.scrim,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            zIndex: 80,
            padding: '20px',
          }}
        >
          <div
            style={{
              background: T.paperRaised,
              borderRadius: '12px',
              padding: '20px',
              maxWidth: '320px',
              width: '100%',
              boxShadow: `0 10px 30px ${T.scrim}`,
            }}
          >
            <div style={{ fontSize: '11px', fontWeight: 400, color: T.inkMuted, marginBottom: '10px' }}>
              Lineup Check
            </div>
            <div style={{ fontSize: '15px', color: T.ink, lineHeight: 1.4, marginBottom: '16px' }}>
              That's {lineupPrompt.proposedSize} batters for the{' '}
              <strong>{lineupPrompt.side === 'home' ? (gameMeta?.homeTeam || 'home') : (gameMeta?.awayTeam || 'away')}</strong>.
              Is the next batter the leadoff hitter?
            </div>
            <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
              <button
                onClick={() => {
                  const side = lineupPrompt.side;
                  const size = lineupPrompt.proposedSize;
                  setEvents(prev => [...prev, {
                    type: 'lineup_marker',
                    side,
                    timestamp: Date.now(),
                  }]);
                  // Set the explicit lineup size on gameMeta so the prompt never
                  // fires again for this side. The early-exit check in the prompt
                  // logic reads homeLineupSize / awayLineupSize.
                  setGameMeta(prev => ({
                    ...prev,
                    [side === 'home' ? 'homeLineupSize' : 'awayLineupSize']: String(size),
                  }));
                  setLineupPrompt(null);
                }}
                style={{
                  background: T.ink,
                  color: T.paperRaised,
                  border: 'none',
                  borderRadius: '8px',
                  padding: '14px',
                  fontFamily: 'inherit',
                  fontSize: '14px',
                  fontWeight: 400,
                  cursor: 'pointer',
                }}
              >
                Yes — leadoff
              </button>
              <button
                onClick={() => setLineupPrompt(null)}
                style={{
                  background: 'transparent',
                  color: T.ink,
                  border: `1px solid ${T.ruleStrong}`,
                  borderRadius: '8px',
                  padding: '14px',
                  fontFamily: 'inherit',
                  fontSize: '14px',
                  fontWeight: 400,
                  cursor: 'pointer',
                }}
              >
                No — keep going
              </button>
            </div>
          </div>
        </div>
      )}

      {/* Manual lineup-flip dialog — opened by tapping the batter panel. For short
          lineups in scrimmages (e.g., 7 batters), the scorer flips the order here
          rather than waiting for the auto-prompt at batter 10+. */}
      {rosterEditorOverlay}

      {manualLineupDialog && (() => {
        const side = manualLineupDialog.side;
        const proposedSize = manualLineupDialog.currentSlot - 1;
        const teamName = side === 'home' ? (gameMeta?.homeTeam || 'home') : (gameMeta?.awayTeam || 'away');
        const canFlip = proposedSize >= 1;
        return (
          <div style={{
            position: 'fixed', inset: 0, background: T.scrim,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            zIndex: 80, padding: '20px',
          }}>
            <div style={{
              background: T.paperRaised, borderRadius: '12px', padding: '20px',
              maxWidth: '320px', width: '100%', boxShadow: `0 10px 30px ${T.scrim}`,
            }}>
              <div style={{ fontSize: '11px', fontWeight: 400, color: T.inkMuted, marginBottom: '10px' }}>
                Lineup
              </div>
              {canFlip ? (
                <div style={{ fontSize: '15px', color: T.ink, lineHeight: 1.4, marginBottom: '16px' }}>
                  Is this the leadoff hitter, with the order starting over? This sets{' '}
                  <strong>{teamName}</strong>'s lineup to <strong>{proposedSize}</strong> batter{proposedSize === 1 ? '' : 's'}.
                </div>
              ) : (
                <div style={{ fontSize: '15px', color: T.ink, lineHeight: 1.4, marginBottom: '16px' }}>
                  Tap this when the leadoff hitter comes up again to start the order over. The first batter needs to bat at least once first.
                </div>
              )}
              <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
                {canFlip && (
                  <button
                    onClick={() => {
                      setEvents(prev => [...prev, { type: 'lineup_marker', side, timestamp: Date.now() }]);
                      setGameMeta(prev => ({
                        ...prev,
                        [side === 'home' ? 'homeLineupSize' : 'awayLineupSize']: String(proposedSize),
                      }));
                      setManualLineupDialog(null);
                    }}
                    style={{
                      background: T.ink, color: T.paperRaised, border: 'none', borderRadius: '8px',
                      padding: '14px', fontFamily: 'inherit', fontSize: '14px', fontWeight: 600, cursor: 'pointer',
                    }}
                  >
                    Yes — start order over ({proposedSize})
                  </button>
                )}
                <button
                  onClick={() => setManualLineupDialog(null)}
                  style={{
                    background: 'transparent', color: T.ink, border: `1px solid ${T.ruleStrong}`,
                    borderRadius: '8px', padding: '14px', fontFamily: 'inherit', fontSize: '14px',
                    fontWeight: 400, cursor: 'pointer',
                  }}
                >
                  {canFlip ? 'No \u2014 keep going' : 'OK'}
                </button>
              </div>
            </div>
          </div>
        );
      })()}
    </div>
  );
}

// ---------------------------------------------------------------------------
// Launcher card — wraps the tagger behind a single tap so the user lands on a
// branded surface first instead of dropping straight into game setup. Keeps
// the warm card-stock palette and brand red accent. No chrome, no preamble.
//
// Behavior:
// - Renders the card by default
// - On "Open Tagger", swaps to the full tagger component
// - Detects a pre-existing game in localStorage and surfaces it on the card
//   so the user knows they're resuming, not starting fresh
// ---------------------------------------------------------------------------

const LAUNCHER_BG = T.paper;
const LAUNCHER_CARD_BG = T.paperRaised;
const LAUNCHER_RED = T.ink;
const LAUNCHER_INK = T.ink;
const LAUNCHER_MUTED = T.inkMuted;

function WildCardLauncher() {
  const [opened, setOpened] = useState(false);
  const [resumeInfo, setResumeInfo] = useState(null);

  // Probe localStorage for an in-progress game so the card can offer "Resume"
  // instead of "Open" when there's something to come back to. Keys here mirror
  // what the tagger writes — adjust if the tagger storage layer is renamed.
  useEffect(() => {
    try {
      const rawMeta = localStorage.getItem('loophohl_gameMeta');
      const rawEvents = localStorage.getItem('loophohl_events');
      if (!rawMeta || !rawEvents) return;
      const meta = JSON.parse(rawMeta);
      const events = JSON.parse(rawEvents);
      if (!Array.isArray(events) || events.length === 0) return;
      setResumeInfo({
        away: meta?.awayTeam || 'Away',
        home: meta?.homeTeam || 'Home',
        format: meta?.format || 'Wildcard',
        eventCount: events.length,
      });
    } catch (e) {
      // Stale or malformed storage — no resume offer, no error surfaced.
    }
  }, []);

  if (opened) {
    return <LoopholeMatrixTagger />;
  }

  return (
    <div style={{
      minHeight: '100vh',
      background: LAUNCHER_BG,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: '24px',
      fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif',
    }}>
      <div style={{
        width: '100%',
        maxWidth: '420px',
        background: LAUNCHER_CARD_BG,
        borderRadius: '16px',
        padding: '40px 28px 32px',
        boxShadow: `0 1px 2px ${T.rule}, 0 8px 32px ${T.rule}`,
        border: `1px solid ${T.rule}`,
        textAlign: 'center',
      }}>
        {/* Brand mark */}
        <div style={{ marginBottom: '24px' }}>
          <img
            src={FINGERPRINT_RED}
            alt="LoopHohl"
            style={{ width: '72px', height: '72px', display: 'inline-block' }}
          />
        </div>

        {/* Wordmark */}
        <div style={{
          fontSize: '28px',
          fontWeight: 300,
          color: LAUNCHER_INK,
          marginBottom: '4px',
        }}>
          WildCard
        </div>
        <div style={{
          fontSize: '11px',
          color: LAUNCHER_MUTED,
          marginBottom: '32px',
        }}>
          Pitch-level tagger
        </div>

        {/* Resume banner — only renders when prior session detected */}
        {resumeInfo && (
          <div style={{
            background: T.paperRaised,
            border: `1px solid ${T.rule}`,
            borderRadius: '10px',
            padding: '12px 14px',
            marginBottom: '20px',
            textAlign: 'left',
          }}>
            <div style={{
              fontSize: '11px',
              color: LAUNCHER_RED,
              fontWeight: 400,
              marginBottom: '4px',
            }}>
              Game in progress
            </div>
            <div style={{ fontSize: '14px', color: LAUNCHER_INK, fontWeight: 400 }}>
              {resumeInfo.away} @ {resumeInfo.home}
            </div>
            <div style={{ fontSize: '11px', color: LAUNCHER_MUTED, marginTop: '2px' }}>
              {resumeInfo.format}  {resumeInfo.eventCount} event{resumeInfo.eventCount === 1 ? '' : 's'} logged
            </div>
          </div>
        )}

        {/* Primary action */}
        <button
          onClick={() => setOpened(true)}
          style={{
            width: '100%',
            background: LAUNCHER_RED,
            color: T.paperRaised,
            border: 'none',
            borderRadius: '10px',
            padding: '16px',
            fontSize: '16px',
            fontWeight: 400,
            cursor: 'pointer',
            boxShadow: `0 1px 2px ${T.rule}`,
          }}
        >
          {resumeInfo ? 'Resume Game' : 'Open Tagger'}
        </button>

        {/* Footer */}
        <div style={{
          marginTop: '28px',
          fontSize: '11px',
          color: LAUNCHER_MUTED,
        }}>
          LoopHohl   app.loophohl.com
        </div>
      </div>
    </div>
  );
}

export default WildCardLauncher;
