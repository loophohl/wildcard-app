import React from 'react';
import ReactDOM from 'react-dom/client';
import WildCardLauncher from './LoopholeMatrixTagger.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WildCardLauncher />
  </React.StrictMode>
);
